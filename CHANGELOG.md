# Changelog

このプロジェクトは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の形式と [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。バージョンはルートと各ワークスペースの `package.json` で管理し、ログイン画面の下部に表示されます。Docker イメージのラベル（`org.opencontainers.image.version`）は `docker build --build-arg VERSION=…` で渡します（`docs/deployment.md`）。

## [Unreleased]

### 修正

- 幅の狭い画面（スマートフォン）で、上部バーのボタンの文字が画面からはみ出し、ページ全体が横にずれていた。狭い画面では文字を隠し、アイコンだけにした（読み上げには残る）
- 表が画面より広いとき、表の見出しの読み上げ用の文字が表の外に出て、ページ全体が横にずれていた
- テーブルの作成で、名前だけを入れた 2 行目以降のカラムがあると「次へ」が押せなかった（型が未選択のまま）。新しい行は文字列型で始まるようにし、型の入力欄の幅も詰めた
- テーブルの作成で、カラムごとのコメントを入力できなかった。カラムの行にコメント欄を足した
- 下に注記のある入力欄（インポートの「先頭から飛ばす件数」、名前の変更・移動・コピーなど）が、注記のない欄より上にずれ、ボタンが入力欄より下に落ちていた。上端をそろえた
- ドロップダウンの矢印が文字に重なることがあった。右の余白を確保した
- カラムの追加・変更、テーブルの作成、セントラルカラムの「型」が、入力済みの文字で候補が絞られて選べなかった（データリスト）。型名のドロップダウンと、長さ・値・属性（`(255)` `UNSIGNED` など）の入力欄に分けた。型名は数値・文字列・日付と時刻などの種類ごとに並べ、MySQL / PostgreSQL それぞれで実際に使える型（空間型、範囲型、ネットワーク型を含む）を選べる。一覧にない型名や既存の定義はそのまま保たれる
- PostgreSQL で列の型を変えるとき、暗黙の変換がない型（文字列から整数など）は `USING 列::型` を付けて実行するようにした。付けていなかったため、テキスト列の値が整数だけでも `INT` に変えられなかった
- 履歴と共有ブックマークの保存が**原子的**になった。同じアカウントの 2 つのタブで同時に実行しても履歴が消えず、2 つのアカウントが同時に同じ名前を共有しても持ち主が入れ替わらない（SQLite は 1 つのトランザクション、Redis は短命のロックで行う）
- 英語の UI でも数値の区切りと時刻の表記が日本語のままだった箇所（32 か所）を、表示言語に合わせた
- グラフを SVG / PNG に保存すると凡例が入らなかった。凡例を図の中に描くようにした
- 入力変換の検査が、キー入力のたびではなく入力が止まってから走るようにした（送信時は必ずその場で検査する）
- 重複を飛ばす取り込み（MySQL）が、値の切り詰めなど重複以外の警告を黙って通していた。取り込み結果に警告として出す
- ブックマークの `[DB]` などを、識別子の位置では引用符で囲み、文字列の中ではリテラルとして書くようにした。共有ブックマークの「読み込む」が、一覧の再取得と重なるとエディターを空にしていた
- PostgreSQL でデータベースの名前を変更すると、ときどき「database is being accessed by other users」で失敗していた。このツール自身の待機中の接続を切断してから名前を変えているが、切断の要求は接続が実際に閉じるのを待たずに戻るため、直後の ALTER DATABASE が閉じかけの接続とぶつかっていた。切断が終わるまで待つようにした（最大 5 秒）
- PostgreSQL で、パーティション分割されたテーブルを参照する外部キーが、テーブルの構造画面にパーティションの数だけ重複して表示されていた（サーバーがパーティションごとに内部の制約を追加するため）。それらを除き、宣言した 1 本だけを示すようにした
- `SESSION_STORE=redis` で、**生きているセッションが理由なくログアウトされることがあった**。ログアウトがそのセッションの接続プールを持っていないプロセスに届くと（別レプリカ、あるいは再起動後）、アカウントの索引に項目が残り、次のログインがそれを「使用中」と数えて実在するセッションを 1 つ余分に追い出していた。索引の鍵をセッション本体に保存し、どのプロセスからでも消せるようにした。更新前に作られたセッション（この鍵を持たない）も、暗号文から鍵を導出して消せるようにしてあるので、ローリング更新の最中でも同じことは起きない。あわせて、ログアウトが別レプリカで処理中のリクエストと重なると、そのリクエストの TTL 更新が「中身の無いセッション」を作り直し、同じ追い出しを TTL の間起こしていた問題も直した（更新をセッションが残っているときだけ行う原子的な操作にし、件数は中身の有無で数える）
- 許可リストにないホストへのログイン試行（`HOST_NOT_ALLOWED`）が、利用者ごとのログイン回数を消費していた。ホストを打ち間違えただけで自分の正規のログインが `429` になり得た。設定ミスは利用者の枠ではなく IP 側の失敗として数えるようにした（許可リストの総当たりは `403` と `401` の差で存在を探れるため、無制限にはしない）

### 追加

- **Docker 上の他のプロジェクトの DB を、接続先を書かずに開ける開発用モード**（`TSMYADMIN_DOCKER_DISCOVERY=1`）。ローカルの Docker デーモンが動かしている MySQL / MariaDB / PostgreSQL のコンテナを、ログイン画面に `docker: プロジェクト/サービス` の名前で並べ、公開ポートへの接続を許可する。データベース名だけをコンテナの環境変数から補い、パスワードは読まない。本番（`NODE_ENV=production`）では起動時に拒否する。ソケットは `TSMYADMIN_DOCKER_SOCKET`、届くホスト名は `TSMYADMIN_DOCKER_CONNECT_HOST`。使い方は `docs/deployment.md` の「Docker で開発用に使う」
- **エクスポート / インポートの形式ごとのオプション**。エクスポートは、CSV の見出し行の有無・全値の引用・値の中の改行の除去、JSON の 1 行表記、XML・YAML・Markdown・LaTeX・Texy!・MediaWiki・HTML・ODT・DOCX の**構造とデータの選択**（**両方を外すとダウンロードできない。これまではデータだけだったが、既定で構造の表も付く**）、XML へのビュー・ルーチン・トリガーの定義、LaTeX のキャプションとラベル、ODS の NULL の書き方。インポートは、**カラムの対応**（ファイルのフィールドを入れ先のカラムへ。空欄で飛ばす）、CSV の**行の終わり**、**空行の扱い**、ODS の割合・通貨・日付の読み方。このツールの XML エクスポートの `<structure>` は、インポートで行として読まない
- **画面まわり**を phpMyAdmin に揃えた。サイドバーに**全展開 / 全折りたたみ / 再読み込み**、幅の**ドラッグ変更**（200〜480 px、矢印キーでも）、テーブル名の**コメント表示**、設定でデータベースの下に**ルーチン・イベント**の枝。各ページに**設定の歯車**（その項目へ移動）、各タブの右端に方言ごとの**マニュアルへのリンク**、セッション期限の 2 分前に「延長」付きの**通知**、**ショートカット**（`h` `d` `t` `b` `s` `e`、補完は `Ctrl + Space`）、SQL コンソールの **Enter で実行**（Shift + Enter は改行）、**デバッグ SQL**（画面が発行した文と所要時間の一覧。直近 200 文、パスワードは伏せる）。設定に、既定のタブ（サーバー / データベース / テーブル）・挿入の既定行数・N 行ごとの見出し・DROP の確認・セルの編集の始め方・フォーカスが外れたら保存・サイドバーの幅とルーチンの枝を追加
- **表示の変換**に**ダウンロードのリンク**・**HTML の整形表示**（許可リストの要素だけを描き、`script`・イベント属性・`javascript:` を捨てる）・**画像（URL から）**を、**入力の変換**に **IPv4 → 整数**を追加。画像は環境変数 `TSMYADMIN_IMAGE_HOSTS` に書いたホストからだけ読み込み（CSP の `img-src` に加わる）、それ以外はリンクにする
- お気に入り・最近使ったテーブル・テーブルごとに選んだ列・表示カラム・サイドバーの開閉・表示のオプションを**アカウントに保存**（`SESSION_STORE=sqlite` / `redis`）。別のブラウザーでログインしても引き継がれる
- **テーブル画面**を phpMyAdmin に揃えた。表示の SQL の枠に**編集 / 再実行 / EXPLAIN / コードにする / ブックマーク**（値を書き込んだ形の文で）と、MySQL / MariaDB の**プロファイル**、設定でオンにする**すべて表示**（1 万行を超えると確認、サーバーは 10 万行で止める）。挿入に**挿入後の動作・エラーを無視して挿入・SQL のプレビュー**。構造に、カラムの**個別の値**と**構造の提案**。操作に**参照整合性の確認**と、テーブルオプション（PACK_KEYS / DELAY_KEY_WRITE / STATS_PERSISTENT / STATS_AUTO_RECALC / TRANSACTIONAL / PAGE_CHECKSUM）の追加。検索では ENUM / SET を値の一覧から選べる
- **データベース画面**を phpMyAdmin に揃えた。ルーチン・トリガー・イベントの**編集**（サーバーから読み戻した定義を作成フォームに入れて置き換える。PostgreSQL は名前と引数が同じなら `CREATE OR REPLACE`、違えば 1 トランザクションで削除して作成）、データベースの**コピーの内容**（構造とデータ / 構造のみ / データのみ）と「コピーしたら移動する」、テーブル作成時の**コメント・エンジン・照合順序**、構造一覧の**お気に入りの星**、セントラルカラムの**編集と JSON ダウンロード**、変更追跡の**バージョンの削除と SQL ダウンロード**、クエリビルダーの**カラムの前に挿入**、データベース検索の**一致した行の削除**、デザイナの**表名だけ・格子・線の形・線のラベル・線の表示・全画面**と **DIA / EPS での保存**、構造タブの**構造の提案**（文字列カラムの値から、より狭い型を提案）
- **サーバー画面**を phpMyAdmin に揃えた。ユーザーを**複数選んで一括削除**（先に権限を取り消す、MySQL は同名のデータベースも削除）、アカウントごとの**データベースごとの権限**（データベース・テーブル・ルーチン、行ごとに編集・取り消し）、権限の **WITH GRANT OPTION**（付与と、GRANT OPTION だけの取り消し）、トップ画面からの**自分のパスワードの変更**（変更後は再接続）、**レプリカ用ユーザーの作成**とソースになれるかの確認、エンジンごとの**変数**、**起動日時**、プロセス一覧の**並べ替えとクエリ全文の切り替え**、変数ごとの**マニュアルへのリンク**、データベース一覧の**サイズの集計の切り替えと並べ替え**
- **グラフ**に曲線・面・円・散布図・時系列を追加し、描いた図を **SVG / PNG で保存**できる。**表示の変換**に 16 進・一部だけ・真偽値・日付の書式・IPv4・前後の文字列を追加し、**入力の変換**（正規表現での検証、JSON / XML / SQL エディター）を挿入・編集のフォームに追加
- **SQL コンソール**: 実行した文の下に「編集」「再実行」「EXPLAIN」「コードにする」（PHP・JavaScript・Python・Java の文字列）を追加。ブックマークの `[DB]`・`[SCHEMA]`・`[USER]`・`[HOST]` は読み込むときに置き換わり、**共有のブックマーク**（サーバーの全アカウントに見え、保存した本人だけが削除できる）と、**履歴のアカウント保存**（別のブラウザーへ引き継ぎ）を追加。履歴は語と「失敗だけ」で絞り込め、行から直接ブックマークにできる
- MySQL / MariaDB で**接続の照合順序**を選べる。サーバー画面のトップに**サーバー情報**（接続先・接続の照合順序・バージョン・稼働時間）を追加。GIS の図を **SVG / PNG で保存**できる。データベースの表一覧・表の行・サーバーのデータベース一覧に**印刷**ボタン（操作用の列は紙に出さない）。表示言語は、ファイル 1 つと 2 行で足せる構成にし、手順を `docs/architecture.md` に書いた
- **設定画面**（画面上部の「設定」）。表の 1 ページの行数、SQL のセーフモード・コンソールの常駐・履歴の件数、エクスポート / インポートの既定値、サイドバーの設定を選べ、ブラウザーとアカウントに保存され、JSON ファイルで書き出し・読み込み・既定への復帰ができる。**サイドバー**はテーブル名を接頭辞でまとめ、出さないデータベースを選べ、多数のときは「さらに表示」で分ける
- **エクスポート / インポートを phpMyAdmin 並みに**。エクスポートは形式に LaTeX・Texy!・MediaWiki・HTML（印刷 / PDF 用）・ODS・ODT・DOCX・Excel 用 CSV を追加し、gzip / zip 圧縮、テーブルごとに別ファイル、ファイル名のテンプレート（`@DATABASE@` `@TABLE@` `@SERVER@` と日時）、文字コード（Shift_JIS ほか。表せない文字はエラー）、行の範囲、SQL のオプション（UPDATE / REPLACE、カラム名、複数行 INSERT と最大長、IGNORE、IF NOT EXISTS、CREATE DATABASE / USE、ビューをテーブルとして、UTC、トランザクション、LOCK TABLES、コメントの有無）を選べる。インポートは ODS・XML・MediaWiki の表と、gzip / zip 圧縮、文字コード、先頭から飛ばす件数（途中からの再開）、ドラッグ & ドロップ、CSV の囲み文字・エスケープ文字、ファイルからのテーブル作成（型は値から推定）、重複キーの飛ばす / 置き換え、`NO_AUTO_VALUE_ON_ZERO` に対応
- **サーバー全体のエクスポート / インポート**。選んだデータベース（PostgreSQL はスキーマ）を `CREATE DATABASE` 付きで 1 つの SQL に（データベースごとに別ファイルの zip も）。どのデータベースにも入っていない状態で SQL を実行するインポートも追加
- レプリケーションに**操作**を追加。レプリカのスレッドの開始・停止、止まった文の 1 件のスキップ、リセット、**ソースの設定**（パスワードはプレビューで伏せる）。PostgreSQL のスタンバイでは WAL の適用の一時停止・再開
- SQL コンソールに**整形**、**実行のオプション**（区切り文字、終了時にロールバック、外部キーの検査の切り替え）、`:名前` の**パラメーター**を追加。サーバーの**変数を変更**できる（MySQL は SET GLOBAL、PostgreSQL は ALTER SYSTEM）
- アカウントの管理を phpMyAdmin に揃えた。**ロック / 解除**、**名前の変更**、**コピー**、**制限**（1 時間あたりの上限・同時接続数・SSL / X509）、**グローバル権限**（PostgreSQL はロールの属性）の編集、**ルーチン単位の権限**、権限の **SQL ダウンロード**。作成では、ホストの選択、**パスワードの生成**、認証プラグイン、同名のデータベースの作成と全権限、ワイルドカードの付与を選べる
- サーバー画面を phpMyAdmin に揃えた。データベース一覧で**複数選んで削除**、データベース作成で**照合順序を指定**、ステータスに**通信量と接続・クエリ統計・カテゴリでの絞り込み・注意の表示**、**モニター**タブ（グラフ、スロークエリログ / 一般ログ / pg_stat_statements の集計）、**アドバイザー**タブ、プロセスの**実行中だけ表示と更新間隔**、エンジンの**InnoDB の状態**、レプリケーションの**バイナリログのイベント**
- 正規化の手がかりから**新しいテーブルを作れる**ようにした。主キー以外のカラム（または複合主キーの一部）で決まるカラムを、そのカラムをキーにした新しいテーブルへ移し（値をコピーし、元から外部キーで参照し、選べば元のカラムを削除）、番号付きの繰り返しカラムを 1 行ずつのテーブルにできる。依存関係は新しいテーブルの主キー追加で全行に対して確かめられる
- デザイナを phpMyAdmin に揃えた。**図の上で外部キーを作る**（付けるカラム、続けて参照先のカラムを選ぶと SQL の確認画面が開く）、一覧から**外部キーを削除**、**全カラムを表示**、テーブルごとの**表示カラム**（図に ◆）、配置に名前を付けて保存する**ページ**（アカウントまたはブラウザ）、図を **SVG** で保存、**PDF**（ブラウザの印刷画面）で出力
- ルーチン・トリガー・イベント・ビューの操作を phpMyAdmin に揃えた。ルーチンの行に「**実行**」（引数の値を入れて CALL / SELECT を SQL タブに用意。MySQL の OUT / INOUT はユーザー変数で受ける）、「**特性を変更**」（セキュリティ・データアクセス・コメント）、「**削除**」。トリガーに「削除」、ルーチン・トリガー・イベントの定義に「SQL をダウンロード」（イベントは「SQL タブで編集」も）。作成フォームに DEFINER / SQL SECURITY / データアクセス（MySQL）、イベントの ON COMPLETION PRESERVE を追加。ビューは OR REPLACE・カラム名・WITH CHECK OPTION・（MySQL）ALGORITHM / DEFINER / SQL SECURITY を指定でき、操作タブで既存ビューの定義を編集できる
- データベースの操作に「**既定の照合順序を変更**」（MySQL は既存のテーブル・カラムの変換つき、PostgreSQL は全文字列カラムに照合順序を付与）を追加。データベースのコピー（MySQL）で、**外部キー・次の AUTO_INCREMENT 値・アカウントの権限**も引き継げるようにした
- クエリに「**結合**」を追加（テーブルごとに INNER / LEFT / RIGHT とつなぐカラムを指定。指定しなければ従来どおり外部キーで）。組み立て（テーブル・カラム・条件・結合）を**名前を付けて保存**できる
- データベース内の検索に「**探し方**」（語句 / いずれかの語 / すべての語 / 正規表現）と「**カラム名で絞る**」を追加
- データベースに「**データ辞書**」を追加（全テーブル / ビューのカラム・参照先・コメント・インデックスを 1 ページにまとめて印刷）
- データベースのテーブル一覧で、行数の横の「**数える**」で正確な行数を数えられるようにした。MySQL では照合順序と作成 / 更新日時も表示する
- データベースの構造で選んだテーブルに、「**CREATE 文を表示**」「**メンテナンス**」（ANALYZE / CHECK / CHECKSUM / OPTIMIZE / REPAIR、PostgreSQL は ANALYZE / VACUUM）「**別の場所へコピー**」「**接頭辞を追加 / 置換**」を追加
- 追跡で、**このツールから実行した文を記録**するようにした（phpMyAdmin の Tracking statements）。記録する種類（CREATE / ALTER / RENAME / DROP / TRUNCATE / INDEX、INSERT / UPDATE / DELETE）をテーブルごとに選べ、既定は定義の変更だけ。表の編集からの変更は値を除いて記録し、記録された文はそのテーブルの行を読めるアカウントにだけ表示する。データベースに「**追跡**」タブ（追跡しているテーブルの一覧）を追加
- 操作タブに「**全カラムの照合順序を変更**」「**行の並び順を変更**」（MySQL: ORDER BY、PostgreSQL: CLUSTER）を追加。テーブルオプションに行フォーマットと CHECKSUM、メンテナンスに CHECKSUM TABLE と FLUSH TABLE（MySQL）。「テーブルをコピー」で、コピー先のデータベース / スキーマ、構造のみ / データのみ、同じ名前のテーブルの置き換え、外部キーのコピーを選べる
- パーティションの範囲・分割キーに、文字列の外の `;` を書けないようにした（プレビューの 1 文に別の文が紛れ込まないように）
- 外部キーの追加で、MySQL は**別のデータベース**の、PostgreSQL は**別のスキーマ**のテーブルを参照できるようにした。構造タブで各テーブルの**表示カラム**（参照元で名前として出すカラム）を選べる
- 構造タブに「**パーティション**」を追加。一覧（範囲 / 値・行数・容量）と、追加・空にする・削除・保守。MySQL は既存のテーブルを分割（RANGE / LIST / HASH / KEY）・解除でき、PostgreSQL は「テーブルを作成」でパーティション分割を指定し、パーティションを切り離せる
- 構造タブに「**容量と行の統計**」を追加（データ・インデックス・空き / TOAST の容量、行数、平均の行サイズ、行フォーマット、作成 / 更新日時、不要な行、最終 VACUUM / ANALYZE。サーバーが持つものだけ）と、「**この構造を印刷**」
- 表示タブに「**キーで並べ替え**」（インデックスの順、昇順 / 降順）と、外部キーの値に**参照先の名前を併記**する表示を追加。SQL タブに「**プロファイリング**」（MySQL / MariaDB。文ごとの段階別の時間）を追加
- 表示タブで**カラムの並び順**を変えられるようにした（テーブルごとに記憶）。「表示のしかた」で**長い値を全文で表示**・**バイナリを 16 進で表示**・**空間データを WKT で表示**に切り替えられる。バイナリや途中までのテキストは「**ダウンロード**」で値の全体を取り出せる（64 MB まで）
- ズーム検索で、軸ごとに**範囲**（最小・最大）を指定し、**ラベルのカラム**で点に名前を付け、**描く行数の上限**（100 / 500 / 1,000）を選べるようにした
- テーブルの検索に演算子 **IN / NOT IN / BETWEEN / NOT BETWEEN / REGEXP / NOT REGEXP / 空文字（= '' / ≠ ''）** を追加（クエリタブの条件でも使える）。「オプション」で表示するカラム・並べ替え・1 ページの行数を選べ、**DISTINCT** や **SQL で書いた条件**を使うと SELECT 文を SQL タブで開く。「検索して置換」は**正規表現**でも置き換えられる
- 構造タブで**カラムを選んで**主キー / ユニーク / インデックス（MySQL は全文 / 空間も）を付ける、**まとめて変更**、**まとめて削除**できるようにした。MySQL は**カラムの並べ替え**もできる。インデックスは種類・方式（BTREE / HASH、PostgreSQL は GIN / GiST / BRIN / SP-GiST も）・MySQL のプレフィックス長を指定でき、あとから**名前の変更**と**変更**ができる
- 表示タブで**選んだ行をまとめて編集**、CSV / JSON でダウンロード、コピーできるようにした。「このページのグラフ」で表示中の行をグラフにできる。SQL の結果から「**この SELECT からビューを作成**」でビュー作成フォームを開ける
- 挿入 / 編集フォームに**関数**（NOW、UUID、MD5、SHA-256、UPPER など）を追加。関数は決まった一覧から選び、値は引数としてバインドする（SQL として埋め込まない）。挿入は**一度に最大 10 行**、外部キーのカラムには**参照先の値を候補**として表示、バイナリのカラムには**ファイルから**値を入れられる
- カラムの追加・変更で、照合順序、MySQL の属性（UNSIGNED / ZEROFILL / BINARY）と ON UPDATE CURRENT_TIMESTAMP、**生成カラム**（式と STORED / VIRTUAL）、位置（MySQL）、追加と同時に付けるキーを指定できるようにした。生成カラムは式を読み戻すため、変更しても生成カラムのまま（これまでは変更できなかった）。PostgreSQL の照合順序も変更できる
- **phpMyAdmin との機能対応表**（`docs/phpmyadmin-parity.md`）を追加。phpMyAdmin 5.2 の全画面の入力項目と操作を照合し、残りの差と、作らないもの（とその理由）を一覧にした
- テーブルに「**追跡**」タブを追加（phpMyAdmin の Tracking をスナップショット方式で）。テーブルの定義を記録した時点のバージョンとして残し、2 つのバージョン（または現在の構造）の違いを行単位で表示する。定義はサーバーが呼び出し元のセッションで読み、定義を読めないテーブルの履歴は見せない。実行された SQL は記録しない。定義は 64 KB まで、バージョンはサーバー全体で 2,000 個まで（超えると古いものから消える）。サーバーの全アカウントで共有し、永続セッションストアが必要
- サーバーに「**ユーザーグループ**」タブを追加（phpMyAdmin の User groups）。アカウントの集まりごとに表示しないタブを決める（サーバー・データベース・テーブルの各階層。サーバーの SQL を隠すと常駐コンソールも隠れる）。変更できるのはメンバー全員を管理できるアカウントだけ。画面を整理する機能で権限ではない（隠したタブの API はアカウントの権限の範囲で使える）。永続セッションストアが必要
- テーブルの構造タブに「**表示の変換**」を追加（phpMyAdmin のブラウザ変換）。表示タブで、バイナリのカラムを画像（PNG / JPEG / GIF / WebP）として、値をリンクとして（http / https のみ。テンプレートの `{value}` に URL エンコードして差し込む）、JSON を整形して表示する。値そのものは変わらない。保存先は保存済みクエリと同じ
- データベースに「**セントラルカラム**」タブを追加（phpMyAdmin の Central columns）。繰り返し使うカラムの定義を保存し（手で追加、または既存テーブルから取り込み）、構造タブの「カラムを追加」で「セントラルカラムから入力」を選ぶとその定義から始められる。保存先は保存済みクエリと同じ（永続セッションストアならアカウント、そうでなければブラウザー）
- **個人設定をアカウントに保存**するようにした（phpMyAdmin の設定の保存先）。テーマ・表示言語・表示行数・SQL の安全モード・コンソールの開閉が、セッションを永続化している場合（`SESSION_STORE=sqlite` / `redis`）はログイン時に引き継がれ、別のブラウザーでも同じになる。`memory` では従来どおりブラウザーごと
- アカウントに保存するもの（保存済みクエリ・エクスポートのテンプレートなど）の上限 200 件を**種類ごと**にした。従来は全種類の合計で、ブックマークが増えるとテンプレートなど別の種類の古いものが消えていた
- テーブルの「構造」タブに**正規化の手がかり**を追加（phpMyAdmin の Normalize を助言として）。主キーがない、番号付きの列（`phone1`, `phone2`）、区切り文字で並べた値、外部キーのない参照（`user_id` と `users`）、主キーの一部や主キー以外の列で決まる列（先頭 500 行からの推定）を挙げる。テーブルは変更しない
- 空間データの**図形表示（GIS）**を追加（phpMyAdmin の Visualize GIS data）。表示タブの表の下で、そのページの空間カラムの値を座標どおりの縮尺で描き、ポインタを重ねると選んだラベルのカラムの値を出す。MySQL の GEOMETRY 系、PostgreSQL の組み込み図形型（point / lseg / box / path / polygon / circle）、PostGIS の geometry / geography に対応。背景の地図タイルは使わない（外部に問い合わせない）
- 画面の下に常駐する **SQL コンソール**を追加（phpMyAdmin のコンソール）。ヘッダーの「コンソール」で開き、表示中のデータベースを対象に実行する。タブを移っても入力と結果は残り、開閉はブラウザに記憶する
- テーブルの「検索」タブに**ズーム検索**を追加（phpMyAdmin の Zoom search）。数値のカラムを 2 つ選ぶと、検索条件に合う行を散布図にする（先頭 500 行まで）。点を選ぶと行の値を示し、主キーのあるテーブルでは「表示」タブでその行を開ける。同じ点は表でも選べる（キーボード操作用）
- 「デザイナ」タブから**外部キーを追加**できるようにした（付けるテーブルを選び、構造タブと同じフォームで指定。追加後は図と一覧に反映）。図の上のドラッグで線を引く操作は含まない

- サーバーに「**レプリケーション**」タブを追加（phpMyAdmin の Replication / Binary log）。役割（単独・ソース・レプリカ・中継。読む権限がなく判断できないときは「不明」）、レプリカとしての状態、接続しているレプリカ、バイナリログ（PostgreSQL は WAL セグメント）を表示する。状態の列はサーバーの版ごとに違うため、サーバーが返すまま並べる。読む権限のない部分はエラーにせず、その旨を示す

- テーブルの「検索」タブに**検索して置換**を追加（phpMyAdmin の Find and replace）。文字列のカラム 1 つについて、一致するすべての行の文字列を置き換える。大文字と小文字を区別し、実際に値が変わる行だけを更新する（MySQL の照合順序で一致とみなされても置換で変わらない行は触らない）。生成 SQL を確認してから実行し、元に戻せない旨を表示する

- サイドバーに「**お気に入り**」と「**最近使ったテーブル**」（直近 10 件）を追加（phpMyAdmin の Favorites / Recent）。テーブル名の横の星で追加・解除する。このブラウザに、接続先（サーバー・ポート・ユーザー）ごとに保存する

- サーバーに「**文字セット・照合順序**」「**エンジン**」「**プラグイン**」タブを追加（phpMyAdmin の Charsets / Engines / Plugins）。PostgreSQL では同じ位置に照合順序・アクセスメソッド・拡張を示す。どの列でも絞り込める

- テーブルの「操作」に**テーブルを移動**（同じ名前のまま行ごと。MySQL は別のデータベースへ `RENAME TABLE`、PostgreSQL は同じデータベースの別スキーマへ `ALTER TABLE … SET SCHEMA`）と、MySQL の**メンテナンスに REPAIR TABLE**（MyISAM など。InnoDB では対象外である旨が結果に出る）を追加

- エクスポート形式に **XML・YAML・Markdown（表）** を追加し、CSV の**区切り文字**（カンマ・セミコロン（Excel 向け）・タブ）を選べるようにした。XML / YAML は複数テーブルをまとめて出し、NULL・バイナリ・XML に書けない制御文字を含む文字列を区別して値を失わない。Markdown は文書向けの表でバイナリは大きさだけ。テンプレートには区切り文字も保存する

- **ビュー・ルーチン・トリガー・イベントの作成フォーム**（phpMyAdmin の「ルーチンを追加」などに相当）。ビューは DB の構造タブで名前と SELECT 文から、ルーチンは種類・引数・戻り値の型・本体から、トリガーは対象テーブル・タイミング・イベント・本体から、イベント（MySQL）は 1 回だけ / 繰り返しのスケジュールから作る。いずれも生成 SQL のプレビューを経て実行する。MySQL の BEGIN … END は途中の `;` ごと書け、DELIMITER は不要（実行時に自動で切り替える）。PostgreSQL のトリガーはトリガー関数「名前_fn」も一緒に作る

- 2 要素認証に**パスキー（WebAuthn）**を追加。`TSMYADMIN_PASSKEY_ORIGIN` を設定すると、認証アプリの代わりに、または並べてパスキーを登録でき、ログインでは「パスキーで確認」で 2 要素目を済ませられる。1 アカウントに複数登録でき、回復用コードは認証アプリと共通。方法の追加・削除・全解除には、今ある方法（アプリのコードかパスキー）での本人確認が要る。パスワードの代わりにはならない（DB への接続にパスワードそのものが要るため）。パスキーは設定したホスト名に結び付くので、配備のドメインを変えると登録済みのものは使えなくなる

- 2 要素認証の登録画面に **QR コード**を表示する（認証アプリで読み取れる）。サーバー内で生成し、秘密鍵を外部サービスに送らない。鍵と URI の表示も残す

- **2 要素認証の解除（運用者向け）**。端末と回復用コードを失った利用者のため、「ユーザー」タブで登録済みのアカウントに「2 要素認証を解除…」を出す。対象のパスワードを変えられるアカウントだけが使える（MySQL は `CREATE USER`、MySQL 8 はさらに `SYSTEM_USER`。PostgreSQL はスーパーユーザーか、対象を管理できる `CREATEROLE`）。自分の分はここからは解除できない。解除は `second_factor.reset` としてログに残る

- **2 要素認証（TOTP）**。ログインした DB アカウントごとに、認証アプリのワンタイムコードを「セキュリティ」タブで登録できる。次回のログインからコードを求め、回復用コードを 10 個発行する（1 つにつき 1 回）。受理したコードは同じ 30 秒の間でも使い回せない。解除と、鍵の置き換えには現在のコードが必要。秘密鍵は資格情報と同じ鍵で暗号化してセッションストアに保存するため `SESSION_STORE=sqlite` / `redis` が前提（`memory` では使えない）。`TSMYADMIN_REQUIRE_2FA=1` で全アカウント必須にでき、未登録のアカウントは登録以外できない（ログインするとセキュリティタブへ案内する）。永続ストアが無い配備ではタブ自体を出さない。DB のパスワードを知っている者は先に登録できてしまうため、守れるのは「登録後に漏れたパスワード」であることを `docs/security.md` に明記した。WebAuthn・SMS / メールの OTP は含まない
- データベースの「エクスポート」に**テンプレート**を追加。今の選択（対象テーブル・形式・各オプション）に名前を付けて保存し、後で呼び出せる。保存先は保存済みクエリと同じ（永続セッションストアならアカウントに暗号化して保存、無ければブラウザ）で、1 アカウント 200 件の上限も共有する。データベース（スキーマ）ごとの一覧で、テーブル単位のエクスポートタブには出さない。読み込み時に無くなっているテーブルは選択から外して名前を示す

- SQL の結果に「コピー」「印刷」「グラフ」を付けた。コピーはタブ区切り（表計算ソフトに貼るとセルに分かれる）で、HTTPS でも localhost でもない接続ではブラウザが拒むことがある。印刷はその文の結果だけを、画面では一部しか描かない長い結果も先頭 1,000 行まで印刷する。グラフは棒と折れ線（NULL でない値がすべて数値のカラムを最大 6 系列、NULL の行は系列から外す、先頭 500 行まで）で、円グラフなど他の種類は含まない

- テーブルの「権限」タブ: アカウントごとに、このテーブルへの各権限とその出どころ（テーブル・カラム・データベース全体・サーバー全体 / スーパーユーザー）を表示し、このテーブルを対象にした「権限を選ぶ…」から付与・取り消しできる（プレビューを経由）。ロール経由の権限は表示しない（PostgreSQL ではテーブル所有者の暗黙の権限と PUBLIC の権限も）。MySQL の部分的な取り消し（`partial_revokes`）は反映する

- データベースの「構造」タブのテーブル一覧に合計行（件数、行数の概算とサイズの合計）を付けた（phpMyAdmin と同じ位置）。行数やサイズを持たないビューなどは合計に含めない

- データベースの「デザイナ」タブ: テーブルと外部キーの図（表示のみ）と、同じ外部キーの一覧表。箱はドラッグまたは矢印キーで動かせ、配置はブラウザに保存する。外部キーはデータベース（スキーマ）単位の 1 回のカタログ問い合わせで取得する（テーブルの数だけ構造を読みに行かない）。別のデータベース / スキーマへの外部キーは一覧にだけ示す。phpMyAdmin のデザイナのような、図からの外部キー作成やテーブル編集は含まない

- データベースの「クエリ」タブ（phpMyAdmin のクエリ作成画面にあたる）: テーブル・出力するカラム（別名・表示・並べ替え）・条件を選んで SELECT 文を作り、SQL タブで開いて編集・実行する。条件はカラム・演算子・値の組で、グループ内は AND、グループ同士は OR。値は引用符付きのリテラルとしてサーバーが SQL に書き込む。複数のテーブルは同じデータベース（スキーマ）内の外部キーに沿って LEFT JOIN し、外部キーでつながらない組み合わせは作成を断る（直積を黙って作らないため）。自由記述の条件と LIKE パターンは意図して含めていない（SQL タブで書き足す）。MySQL の BIT カラムの値は整数で指定する（170 など）。同じ 2 テーブルを結ぶ外部キーが複数あるときは制約名の順で最初のものを使う

- データベースの「検索」タブ: 語を含む行を、選んだテーブルのすべてのカラムから探し、テーブルごとの一致件数と、一致した行を取り出す SELECT（SQL タブで開く）を表示する。大文字と小文字は区別せず、`%` や `_` はそのままの文字として扱う。値を文字として読めないカラムは対象外（MySQL はバイナリ・BIT・空間型、PostgreSQL は bytea と PostGIS の型。PostgreSQL の bit や point は検索する）。結果には検索した語を表示する。テーブルは 1 つずつ順に検索して途中で中止でき、件数は 10 万行で打ち切る。phpMyAdmin の「すべての語」「いずれかの語」「正規表現」による検索は含まない

- データベースの「操作」タブ: **名前変更**と**コピー**。MySQL には名前変更の命令がないため、新しいデータベースを作って全テーブルを 1 文（原子的）で移す。**元のデータベースは削除しない**（アカウントから見えないルーチン・イベントや、確認後に作られたテーブルを巻き込まないため）。ビュー・ルーチン・トリガー・イベントがあるデータベースは、取り残されてビューが壊れるので実行前に拒否する。コピーはテーブルの構造と（選べば）データで、外部キー・ビュー・ルーチン・トリガー・イベント・権限は対象外。PostgreSQL は `ALTER DATABASE … RENAME` と `CREATE DATABASE … TEMPLATE`（**コピーはビューや関数を含むデータベース全体**）で、実行前にその DB への tsmyadmin の待機中の接続（同じ DB アカウントのもの）だけを切断する（実行中の処理は切らず、その場合は失敗する）。サーバー自身のデータベースと、PostgreSQL ではこのセッションの接続先は対象外。名前変更は削除と同じく元の名前の入力で確認する。名前変更後に残る元のデータベースは、テーブルが見えなくても空とは限らない（見えないルーチン・イベント等）ため、削除は管理者アカウントで中身を確かめてから行うよう案内する

- テーブルの表示画面の上に、そのページを取得した SQL と所要時間を表示する（phpMyAdmin と同じ位置）。条件の値は SQL 本文に埋め込まず、プレースホルダ（MySQL は `?`、PostgreSQL は `$1`…）のまま示し、バインドした値を下に並べる。埋め込んだ形は実行できそうに見えるが、方言やサーバー設定に合わせた引用を推測で作ることになるため

- テーブルの表示画面で、行ごとに削除ボタンを付けた（phpMyAdmin と同じ位置）。これまでは行を選択してからツールバーの「選択行を削除」を押す必要があった。行のボタンはその行だけを削除し、他に選択している行は巻き込まない

- サイドバー（ナビゲーション）のビジュアル回帰テスト。これまで他のスクリーンショットは `<aside>` を丸ごとマスクしていたため、左ペインの見た目は一切検査されていなかった。データベース一覧は中断した統合テストの残骸で件数が変わるので、要素ではなく**固定矩形**を撮り、先頭 3 行がサーバー自身のスキーマであることを事前に確認する（想定外のデータベースが割り込んだ場合、読めないピクセル差分ではなくアサーション失敗になる）

- 前段（リバースプロキシ / 各クラウド）の形ごとの適合テスト（`platform-conformance.test.ts`）。nginx・Caddy、AWS ALB / App Runner、Azure App Service / Container Apps、Cloudflare、プロキシなし、クライアントが `X-Forwarded-For` を偽装した場合の 8 通りで、利用者を識別できること・1 人のレート制限が他人に波及しないこと・ユーザー名を変えながらの総当たりが止まることを確認します
- 置き場所ごとのデプロイ手引き（`docs/hosting.md` / 英訳）。さくらの VPS などの普通のサーバー、AWS（ECS Fargate / App Runner / EC2）、Azure（Container Apps / App Service / VM）。ディスクが残るかでセッションストアが決まる、という 1 点から書いてあります
- 転送されたクライアント IP に付いてくるポート番号（Azure App Service は `1.2.3.4:56789` の形で渡します）を落とすようにした。付いたままだと接続ごとに別のアドレスと見なされ、IP 単位のログインレート制限が数えられなくなっていました。IPv6 は括弧付き（`[2001:db8::1]:443`）のときだけポートとして扱います

- `TRUST_PROXY=cloudflare`: クライアント IP を `CF-Connecting-IP` から取る。Cloudflare の Worker 経由では `X-Forwarded-For` が付かないことがあり、`1` のままだと全員が同じアドレスと見なされてログインのレート制限が 1 枠になっていた。`1` と分けているのは、このヘッダーを必ず上書きしてくれるのが Cloudflare だけで、それ以外の環境ではクライアントが自分で名乗れてしまうため
- `bun run cf:check` / `cf:deploy`。`cf:check` は Cloudflare のアカウントなしで、設定・worker・コンテナイメージがビルドできるかを確かめる
- Cloudflare 用の設定を同梱した（`wrangler.jsonc` と `deploy/cloudflare/worker.ts`）。転送先ポートが `Dockerfile` の `EXPOSE` とずれると `check:static` が落ちる。`wrangler secret put` で登録した値が届くのは Worker までなので、worker 側でコンテナの環境変数に渡し直している
- Cloudflare へのデプロイ手順（`docs/cloudflare.md` / 英訳）。Containers で同梱の Dockerfile を動かす構成、ディスクが ephemeral なため `SESSION_STORE=redis` が必須になること、Workers 単体では動かない理由、Tunnel / Access を入口だけに使う別案
- `SESSION_STORE=redis`: セッションと保存済みクエリを複数レプリカで共有する（`REDIS_URL` が必須）。暗号化は sqlite と同一（`SESSION_SECRET` 由来の鍵、値ごとに自分のキーへ結合）。**ただしこれで「複数レプリカ対応」になるわけではない** — 実行中クエリのキャンセル・ログインのレート制限・DB 接続プールはプロセスごとに残るため、引き続きスティッキーセッションが必要（docs/deployment.md の「複数レプリカ」）
- セッションと保存済みクエリの暗号文を、それぞれの行に結び付けた（AES-GCM の追加認証データに `テーブル名:行 ID`）。ファイルに書き込めるが復号はできない者が、他人の行の暗号文を自分の行に上書きして自分のセッションを他人の資格情報で動かすことを防ぐ。既存のファイルは起動時にその場で読み替えるので、セッションも保存済みクエリも失われない（1 トランザクションで行うため中断しても半端な状態にならない）。開かない行は読んだ時点で破棄する（一覧に出ないのに件数上限に数えられ続ける状態にしない）
- 権限をカラム単位で付与・取り消しできるようにした（`SELECT` / `INSERT` / `UPDATE` / `REFERENCES`。MySQL・PostgreSQL の両方でカラム単位を受け付ける権限だけを提示し、`DELETE` / `TRIGGER` と同時に選ぶとプレビュー前に止まる）。PostgreSQL の権限表示はカラム単位の権限（`pg_attribute.attacl`）も読むようになり、カラム単位の権限を「すべて」と誤表示しなくなった
- 英語ドキュメント（`README.en.md`・`docs/en/{user-guide,deployment,security,operations,architecture}.md`）。アプリ内の「ヘルプ」リンクは表示言語に追従する。日本語が原文で、原文を変えたのに英訳が追随していない状態は `bun run docs:i18n`（`check:static` に含まれる）が検出する（翻訳後に `bun run docs:sync` でハッシュを打ち直す）
- 保存済みクエリをサーバー側に保存（`SESSION_STORE=sqlite` のとき）。接続ユーザーのアカウントに紐づき、別のブラウザーからログインしても同じ一覧が見えます。文はセッション資格情報と同じ鍵で暗号化して保存し（SQL 本文・クエリ名・アカウント名はファイル上に平文で残りません）、アカウントごとに 200 件を上限に古いものから削除します。`SESSION_STORE=memory` では従来どおりブラウザーに保存され、保存先はパネルに表示されます
- インポート: pg_dump の通常形式（`COPY … FROM stdin`）と pg_dump 17.6 以降の `\restrict` ヘッダーに対応、進捗表示と中止（画面を閉じても中止される）、「外部キー制約のチェックを無効にする」「1 つのトランザクションで実行する」オプション、エラーの行番号・文番号、実行されなかった文の数とロールバックの警告、別方言のダンプの検出、CSV の生成列・identity 列（`OVERRIDING SYSTEM VALUE`）対応と大文字小文字を区別しないヘッダー照合、失敗した CSV 行の特定、日本語のエラー理由
- `COOKIE_SECURE`（本番で TLS を終端しない社内ネットワーク向け）、`PORT` を `API_PORT` の代替として受け付ける（Docker イメージも `PORT` に従う）。停止シグナルの 1 秒以内の重複（`docker stop` の SIGTERM 二重送出など）を強制終了と誤認しない
- SPA を gzip 圧縮し、ハッシュ付きアセットに 1 年のキャッシュ（index.html は毎回再検証）を付与。`/assets/*` の成功はアクセスログに出さない
- Docker イメージからブラウザ専用パッケージとテストファイルを除外（約 10 MB 削減、node_modules は 18 MB）、Dependabot が `bun.lock` の依存も監視
- docs: systemd での直接起動、サブパス非対応、HSTS、配布イメージがないこと
- MariaDB の SEQUENCE をテーブル一覧に「シーケンス」として表示し、ダンプでは `CREATE SEQUENCE` と現在値からの `RESTART` を出力（テーブル既定値の `nextval()` はデータベース名を含めない）
- 操作タブに「テーブルオプション」（コメント。MySQL はエンジン・照合順序・次の AUTO_INCREMENT 値）と「メンテナンス」（ANALYZE / OPTIMIZE / CHECK TABLE、PostgreSQL は ANALYZE / VACUUM / VACUUM FULL）
- データベース構造ページで複数テーブルを選択して一括で空にする・削除する・エクスポート対象にする
- サーバーの SQL タブで対象データベースを選択できる（MySQL では既定が information_schema だった）
- PostgreSQL のカラム変更プレビューは変更された句だけを出す（型・NOT NULL・既定値・コメントの差分）
- SQL エクスポートにルーチン・トリガー・イベント（MySQL は `DELIMITER` ブロック、`DEFINER` 句の除去オプション）
- データベース一覧にサイズ（MySQL はテーブル数も）、テーブル一覧にサイズ列、テーブルオプションに現在の照合順序と AUTO_INCREMENT を表示
- ログイン画面は前回の接続先（パスワード以外）を記憶、履歴に実行時のデータベースを表示、データベース作成後はそのデータベースを開く。PostgreSQL の `DROP DATABASE` は `WITH (FORCE)`

### 変更

- Cloudflare へのデプロイで `REDIS_URL` を**任意**にした。登録すれば従来どおり `SESSION_STORE=redis`、登録しなければ `sqlite`（コンテナのディスクは休止・再デプロイで消えるため、その場合はログインと保存済みクエリも消える）。試用向けで、残したいときは Redis を登録する
- 入力欄・ボタン・SQL エディタの枠線を濃くした。見た目の刷新で白地に対し 1.7:1 まで薄くしてしまっており、WCAG 1.4.11（操作要素の境界は 3:1）を満たしていなかった
- 画面の見た目を一新した。色・角丸・影を意味のあるトークン（canvas / surface / line / ink / brand / critical）にまとめ、`.dark` で切り替える方式にしたので、`bg-zinc-800 dark:bg-zinc-900` のような手書きのペア指定がなくなった。ログイン画面はロゴと製品名を先に出し、構造画面などはカードで区切る。破壊的操作は赤ベタをやめて critical の文字と細枠にした（赤ベタは確認ダイアログの中だけ）。`<select>` は OS 標準の矢印をやめて全環境で同じ見た目にした
- SQL エディタのライトテーマの構文色を差し替えた。CodeMirror 既定の文字列色は白地でも 3.8:1 で、この文字サイズでは WCAG AA（4.5:1）に届いていなかった。7 色すべて背景・カレント行の両方で 4.5:1 以上にした
- セッションストアの `closeAll()` を二度呼んでも落ちないようにした（停止シグナルが重なった場合など。メモリストアは元から許容していた）
- セッションストア（SQLite）にロック待ち（`busy_timeout = 5000`）を設定した。他のプロセスが書き込み中だと起動時の移行がその場で失敗し、コンテナが立ち上がらないことがあった
- アカウントの識別子（セッション上限と保存済みクエリの所属に使う）の組み立てで、フィールド内の `|` と `\` をエスケープするようにした。区切り文字を含む値があると別のアカウントが同じ識別子になりうる形だったため。どちらの文字も含まないアカウントでは値が変わらないので、通常の環境では再ログインも保存済みクエリの消失も起きない（ユーザー名に `|` か `\` を含むアカウントだけは識別子が変わり、再ログインになり、サーバー側の保存済みクエリが参照されなくなる）
- `SESSION_SECRET` を変更したとき、保存済みクエリの行もセッションと同時に削除するようにした。行を探す識別子が鍵から導出されるため、変更後は誰からも参照できないまま件数上限にも数えられず、ファイルに残り続けていた
- サーバーに保存する保存済みクエリの SQL に長さの上限（100,000 文字）を設けた。ディスクに残るため、1 アカウントあたりの上限 200 件と合わせてファイルの増加を抑える（ブラウザーに保存済みの長い文はそのまま読める）
- `SESSION_MAX_PER_IDENTITY` の説明を実装に合わせた（docs/security.md が「最も古いセッションから」と書いていたが、実際は最後に使われてから最も時間が経ったもの（LRU）から閉じる。deployment.md は正しかった）
- ログイン時にクライアント側のキャッシュを破棄する。セッションが期限切れになったあと別のアカウントでログインすると、前のアカウントのデータベース一覧などが一瞬見えることがありました（ログアウト経由では破棄されていました）
- PostgreSQL の独立したシーケンス（`CREATE SEQUENCE` で作ったもの。serial / identity 列の内部シーケンスは除く）をテーブル一覧に「シーケンス」として表示し、ダンプでは `CREATE SEQUENCE`・`OWNED BY`・現在値（`setval`）を出力。`nextval('独自シーケンス')` を既定値に持つ列はそのままの既定値で出力（以前は identity 列に置き換えられ、シーケンスが作られず復元に失敗していた）
- SQL ダンプ（PostgreSQL）: マテリアライズドビューを `WITH NO DATA` で作成し、データを含むダンプでは末尾に `REFRESH MATERIALIZED VIEW` を出力（構造のみ → データのみの順に復元しても空のままにならない）。完了マーカーはオブジェクト数（`N objects`）を示す
- インポートの「中止」は実行 ID で止め、それまでに実行された文の結果を表示する（以前は通信エラーとして表示され、結果が失われていた）。CSV は 1 トランザクションで取り込むため中止ボタンを出さない。実行中はファイル・オプションを変更できず、完了後はファイル選択が解除される（同じファイルの二重取り込みを防ぐ）
- 大きなデータへの耐性: 長いテキスト（65,536 文字超）は表示・SQL 結果で先頭のみ返し全体の文字数を併記（画面からは編集不可、エクスポートは全文）、絞り込み時の件数は 100,000 行で打ち切って「以上」と表示、主キーのない MySQL テーブルのエクスポートは全件読み込まずストリーミング、エクスポートはテーブルごとにカタログを読む（数千テーブルでも先頭からすぐ送信）、SQL 実行・インポートの長い処理中はプロキシに切られないよう 15 秒ごとに空行を送る、選択テーブルが URL に収まらない場合は案内
- 日本語 UI の用語統一（カラム / 行・件 / データベース / ユーザーを作成 / システム変数 など）、送信ボタンは「〜する」、取り消せない操作の警告はデータが失われる操作だけに「データは失われます」を付ける、英語メッセージの二重表示を廃止

### 修正

- インポート: pg_dump の通常形式で各 `COPY` の上にあるコメントブロックのため COPY データが SQL として解釈され、1 行も取り込まれず以降のファイルが失われていた。空のテーブルの `COPY … FROM stdin;\n\.` も失敗していた。`ignoreForeignKeys` の `SET session_replication_role` が権限不足で失敗しても黙って続行していた（`OPTION_FAILED` として報告）。末尾に `;` のないファイルを「1 つのトランザクション」で実行すると最後の文が `COMMIT` と結合していた。スクリプト自身の `COMMIT` や MySQL の暗黙コミット（DDL）の後のエラーで「何も反映されていません」と表示していた（「最後のコミット以降を取り消しました」に）。エラーの行番号が文の上のコメント行を指していた。CSV ヘッダーの大文字小文字だけ異なる複数一致・重複カラムを分かる形で報告
- エクスポート: 要求されたテーブルがダンプ開始前に消えた場合、黙って除外して「完了」マーカーを付けていた（転送を中断するように）。SQL コンソールの CSV / JSON ダウンロードは表示上限で切られた値を含む場合に無効化（エクスポートタブへ案内）。選択テーブルの「すべて選択 / 選択を解除」ボタン
- 表示タブ: 表示上限を超える値を含む行で編集・選択が無効になる理由を表示、複製時はその列を既定値 / NULL にして注記、切られた値の「全文を表示」を「先頭 N 文字を表示」に、概算件数のテーブルで最終ページ以降に進めなかった
- プロセス: 自分の接続を終了する確認ダイアログに警告、バッジの説明を支援技術にも提供、ダイアログ内の長いクエリのはみ出し。ログインのプリセット説明に MySQL のデータベース名変更可を追記
- インポート（続き）: CRLF の pg_dump で各 COPY の先頭に空行が入り 1 列テーブルに行が増えていた、`DELIMITER` を戻さずに終わる MySQL ファイルで末尾に空文の失敗が出ていた、mysqldump の `/*!40000 ALTER TABLE … DISABLE KEYS */` による暗黙コミットを見落とし「何も反映されていません」と表示していた、文と文の間に届いた中止が「中止できませんでした」になっていた（結果に「中止したため、以降の文は実行されていません」を表示）、`COMMIT` 自体の失敗（遅延制約）が結果から消えていた、新しいファイルを選んでも中止の通知が残っていた
- エクスポート（PostgreSQL）: データのみのダンプで `REFRESH MATERIALIZED VIEW` が出ず構造→データの順の復元でマテリアライズドビューが空のままだった、テーブルを選んだダンプに列が所有するシーケンス（名前を変えた serial など）が含まれず復元できなかった、降順シーケンス（`INCREMENT BY -1`）が復元後に逆方向へ動いていた、未使用のシーケンスの位置が引き下げられていた（現在値はテーブルデータの後に設定）、serial 列のシーケンスオプション（INCREMENT / MAXVALUE / CACHE）がダンプに出ていなかった、シーケンスのコメント。シーケンスの削除が `DROP TABLE` を生成して失敗していた、名前を変えたシーケンスを持つテーブルの複製が元のシーケンスを共有していた
- 英語表示に対応しました。ブラウザの言語設定に従い、画面右上のメニューで切り替えられます（選択はブラウザに保存されます）
- **カラム変更（MySQL）でカラムの属性が失われていた**: コメントだけを変えても `MODIFY COLUMN` はカラム定義を丸ごと書き換えるため、`ON UPDATE CURRENT_TIMESTAMP` と明示的な文字セット・照合順序が消えていた。生成カラムに至っては生成式ごと失われ、ただのカラムになっていた。フォームが表示しない属性は変更操作に同梱して再出力するようにし、生成カラムは構造タブから変更できないようにした（SQL タブの `ALTER TABLE` を案内）
- SQL タブでトランザクションを開いたまま実行を終えると、黙って取り消されていた（1 回の実行ごとに接続が返却されるため）。開いたままだったことを画面に出すようにした
- MySQL で生成カラムのあるテーブルに行を挿入できなかった（`ER_NON_DEFAULT_VALUE_FOR_GENERATED_COLUMN`）。挿入・行の複製フォームは生成カラムを読み取り専用にし、値を送らない
- 挿入ボタンと DDL プレビューの実行ボタンをダブルクリックすると 2 回実行されていた
- MySQL でクエリをキャンセルしたとき「キャンセルしました」が出ないことがあった（`KILL QUERY` された `SLEEP()` はエラーにならず正常終了するため）
- CSV エクスポートに「表計算ソフトで数式として実行されないようにする」オプションを追加（既定はオフ。オンにすると値が変わるためインポートで戻す用途では使わない）
- `/api/*` のレスポンスに `Cache-Control: no-store` を付与
- 接続先ホストは allowlist 判定に使った正規化後の文字列で接続するようにした（判定と接続で別の文字列を使わない）
- シーケンスの削除・改名の説明が方言によって誤っていた（PostgreSQL は依存があると削除を拒否する / MariaDB は改名に既定値が追従しない）
- 権限タブで**権限とテーブルを選んで付与・取り消し**できるようにした（`SELECT` / `INSERT` / `UPDATE` / `DELETE` / `REFERENCES` / `TRIGGER`）。読み取り専用ユーザーを手書き SQL なしで作れる。カラム単位の権限は今後対応
- ストアドルーチンとトリガーの定義を「SQL タブで編集」できるようにした。定義を置き換えるスクリプトを組み立ててエディタに読み込む（PostgreSQL は `CREATE OR REPLACE`、MySQL は `DROP … IF EXISTS` + `DELIMITER` で本体を包み、作成時の `sql_mode` を復元して元に戻す）。本体はサーバーの方言そのままのコードなので専用フォームは用意せず、実行前に内容を確認できることを優先した
- プロセス一覧に「実行中のクエリを中断」を追加（MySQL の `KILL QUERY` / PostgreSQL の `pg_cancel_backend`）。接続・トランザクション・一時テーブルを残したまま、走っている文だけを止められる。従来の「強制終了」は接続ごと閉じる動作のまま
- **主キーのないテーブルで、編集・削除が別の行に当たることがあった**（MySQL / MariaDB）。全カラム一致のキーをカラム自身の照合順序で比較していたため、大文字小文字・アクセント・末尾の空白しか違わない行が「同じ行」と見なされ、`LIMIT 1` が先に見つけたほうを書き換えていた（`'A'` を削除すると `'a'` が消える）。両辺を utf8mb4 に変換したうえでバイト単位で比較する
- **絵文字など BMP 外の文字を含む既定値が、カラムを編集するだけで壊れていた**（MySQL / MariaDB）。カタログの `COLUMN_DEFAULT` は utf8mb3 のため該当文字が `?` になる。MySQL は `SHOW CREATE TABLE` が同じ既定値を 16 進で出すので、そこから復元する（利用者が本当に入力した `?` とも区別できる）。MariaDB はどちらの経路でも復元できないため既知の制限として明記
- SQL タブに「WHERE のない UPDATE / DELETE を確認」を追加（既定でオン）。全行が対象になる文を実行する前に確認ダイアログを出す。文字列やコメントの中は句として数えないため、確認が多めに出ることはあっても見落とすことはない
- SQL タブの結果 CSV ダウンロードにも「表計算ソフトで数式として実行されないようにする」を追加（テーブルのエクスポートと同じく既定はオフ。選択はブラウザに記憶）
- **MariaDB でカラムを編集するとカラムレベルの CHECK 制約が消えていた**。MariaDB の JSON カラムは内部的に `CHECK (json_valid(...))` を持つ `longtext` なので、コメントを変えるだけでそのカラムが JSON でなくなり、不正な JSON を受け付けるようになっていた
- MariaDB で既定値に改行・復帰・NUL を含むカラムを編集すると、その文字がバックスラッシュ 2 文字に置き換わっていた（MariaDB はカタログでこれらもエスケープして返すため）
- MySQL で `VARCHAR DEFAULT '0xFF'` のように 16 進・ビットリテラルに見える**文字列**の既定値を、バイナリの既定値と取り違えて編集できなくしていた（カラムの型で判定するようにした）
- **MariaDB でカラムを編集すると既定値が壊れていた**: MariaDB はカタログの既定値を MySQL 8 と違う形式（文字列は引用符つき、既定値なしは `NULL` という文字列、式は素のまま）で返すため、コメントを変えただけで `'abc'` が 5 文字の文字列 `'abc'` になり、既定値のない NULL 許可カラムには文字列 `NULL` が入り、`CURRENT_TIMESTAMP` のカラムは編集自体ができなかった。カタログの形式をアダプター側で正規化し、既定値が式かどうかを `ColumnDef.defaultIsExpression` で明示する
- MySQL でバイナリ・BIT の既定値（`DEFAULT 0xFF`、`DEFAULT b'101'`）を持つカラムを編集すると、既定値が文字列 `'0xFF'` に置き換わっていた
- インポートの「トランザクションを取り消しました」判定も、SQL タブと同じくサーバーへの問い合わせに統一（`SET autocommit = 0` や `COMMIT AND CHAIN` を含むスクリプトで警告が出ない / 逆に出すぎる問題）
- キャンセルした実行でもトランザクションが開いたままかどうかを報告するようにした（`KILL QUERY` / `pg_cancel_backend` は文を止めるだけで、トランザクションはそのまま残る）
- カラムの型に `DEFAULT CURRENT_TIMESTAMP` があるとき、型の小数秒精度を変えると既定値の精度が合わずエラーになっていた（`updated_at` の典型的な形）。既定値も新しい精度に合わせる
- 型の欄に書いたコメントや、`ENUM('collate')` のような文字列リテラルの中身を句として読み違えていた
- SQL タブでトランザクションが開いたまま実行が終わったかどうかを、**サーバー自身に問い合わせて**判定するようにした（MySQL は OK パケットの `SERVER_STATUS_IN_TRANS`、PostgreSQL は接続のトランザクション状態）。スクリプトを読んで暗黙コミットを再現する方式では、構文を受け付けられなかった DDL や一時テーブルなど、原理的に正しく判定できないケースが残るため
- MySQL でカラムの型を変更したときの属性の引き継ぎを、型ごとの可否で判定するようにした。照合順序は文字列型なら維持（`VARCHAR(50)` → `VARCHAR(100)` で落ちて比較・並び順が黙って変わる問題を修正）、型の欄に `COLLATE` / `CHARACTER SET` を書いた場合はそちらを優先。`ON UPDATE CURRENT_TIMESTAMP` は新しい型の小数秒精度に合わせて書き換える（`TIMESTAMP` → `TIMESTAMP(3)` で黙って消える問題を修正）
- MySQL で式の既定値を持つカラムをフォームから変更できなかった（カタログが返すエスケープと文字エンコードを元に戻すようにした。`DEFAULT (concat('日本'))` や絵文字を含む既定値が編集後も同じ値を返すことを実機で確認）
- PostgreSQL 18 の仮想生成カラムがエクスポートで通常の既定値つきカラムとして出力されていた
- `splitStatements` から到達不能な重複コードを削除（`scanToken` が先に消費するため 5 分岐が死んでいた）。「字句解析は 1 か所」が実装どおりになった
- 英語 UI の見直し: 単複の処理を追加（「Delete 1 rows?」を解消）、ラベル・ボタン・見出しから冠詞を落とす、`undo` を `roll back` に、`every privilege` を `all privileges` に、`Kind` を `Type` に、「SQL console」を「SQL tab」に、切り詰めたテキストの注記が値と地続きになっていたのを修正
- ドキュメントを実装に合わせて修正: DDL の実行はプレビューした SQL をそのまま `/sql` に送る（サーバーで再生成するのはアカウント操作だけ）、`executeSql` は 1 回だけ呼ばれる、件数の打ち切りは絞り込みの有無に関わらず効く、`HEALTHCHECK` は `API_PORT` に従う、MariaDB の検証済みバージョンは 10.11 / 11
- ドキュメントに不足していた説明を追加: ORM を使わない理由、表示上限の値を書き戻せない理由、件数を打ち切る理由、字句解析と文の分割の仕様表、行キーが `none` になる条件、エラーコード早見表、`max_connections` の見積もり式、ブラウザ側に残るデータ（SQL 履歴・保存済みクエリ）、API にレスポンス項目を足す手順の実例
- 対応データベースに MySQL 9・Percona Server 8.4・PostgreSQL 18 を追加（いずれも統合テスト全通過）。互換エンジン（TiDB 7.5・CockroachDB 24.3）は実測結果を明記（限定動作 / 非対応）
- 対応データベースを明記し、CI で MySQL 8.0 + PostgreSQL 14 / MySQL 8.4 + PostgreSQL 17、MariaDB 10.11 と 11 を毎回検証するようにしました（MySQL 9 / PostgreSQL 18 / Percona は手動検証）
- MariaDB 10.11 でユーザー一覧・権限表示・アカウント操作が照合順序の不一致（`Illegal mix of collations`）で失敗していたのを修正
- docs: 接続先 DB を再起動・停止したときの挙動（即座に 502、同じセッションのまま自動復帰、`/healthz` `/readyz` は接続先 DB の状態を示さない）を運用ドキュメントに追記
- E2E を WebKit（Safari エンジン）でも実行するようにしました（CI 含む）。Safari のフォーカス復帰・Cookie・入力欄の差分を継続的に検出します
- アカウント操作の実行結果は、ロールバックされたかどうかを専用のフィールドで返すようになりました（以前はサーバー通知に紛れ込ませていました）。表示は変わりません
- 内部整理: SQL の字句解析（リテラル・コメント・方言差）をスプリッタ 1 か所に集約し、監査ログのマスクもそれを使う。PostgreSQL の「列のシーケンス」判定 SQL を 1 か所に。`importSql` / `sqlBody` を段階ごとの関数に分割。`CALL` の複数結果の文番号、中止の待ち合わせ、コメント除去・`SET` 読み取りのテストを追加
- コードレビュー: 64 MB のインポートを 3 回トークン化していた（1 回に。アダプターは分割済みの文を受け取る）、エラー一覧を上限を超えて作っていた、復元時のシーケンス位置決定が全表走査になっていた（インデックス探索に）、平文 HTTP（`COOKIE_SECURE=0`）で `crypto.randomUUID` がなく SQL 実行・インポートが失敗していた、インポート中の「インポートする」「中止」がフォーカスを失っていた、結果領域の二重読み上げ、エクスポートのダウンロードボタンが無効化で到達不能になり理由も読まれなかった、切り詰めセルの注記を読み上げやすい文に、`SET` の代入読み取りと NDJSON 読み込みの共通化
- インポート（続き 5）: 数十万文のファイルで結果の集計が二乗時間になっていた、`SET autocommit := 0` / `SET sql_mode := …` の `:=` 代入を認識、名前に `'` を含むテーブルの serial シーケンスが一覧とダンプに二重に現れていた、PostgreSQL では `/*! */` を通常のコメントとして扱う、中止待ちのタイマー（2 か所）が残っていた
- インポート（続き 4）: 複数の結果を返す `CALL` を含むファイルで文数・成功数・`文 #N`・進捗が結果セット数で数えられ、中止も見落とされていた（各結果に文番号を付与）、`SET autocommit = 0 + 1` のような式や文字列内の `autocommit = 1` の誤読、PostgreSQL の入れ子コメントで始まる `COMMIT` 等を見落としていた（先頭コメントの除去を方言対応の共通関数に）、`oid` 列でシーケンス位置の設定が失敗していた、文と文の間で 2 秒以上待つ処理中の中止が「中止できませんでした」になっていた（ループの終了を待って判定）。SQL ダンプ: 既定値が `nextval('t_id_seq') + 1000` のような式の場合に所有シーケンスがダンプから消えていた
- SQL ダンプ（PostgreSQL、続き）: identity 列が別のシーケンスを所有していると IDENTITY のオプションがそのシーケンスのものになっていた、serial 列の既定値を別のシーケンスに付け替えたテーブルが IDENTITY として出力され復元後に採番元が変わっていた（既定値が自身の所有シーケンスを呼ぶ列だけを serial と扱う）、既定値に使われていない所有シーケンスがダンプから消えていた、範囲外の id がシーケンスを使い切った状態で復元されていた（範囲内の id だけで位置を決める）
- インポート（続き 3）: 最後の文を中断した中止が「中止」と報告されなかった、1 つのトランザクションでファイルがコメントや文字列の途中で終わっていると COMMIT が飲み込まれ「成功」と表示されながら何も残らなかった（実行前に `UNTERMINATED_END` として拒否）、複数の結果を返す `CALL` の後のエラーが `COMMIT` のものとして表示されていた（ラッパー文を行番号で識別）、`SET sql_mode = '', autocommit = 0` のような複数代入や変数からの復元を見落としていた（不明な値はコミット扱い）、最後の文の結果を配信中に届いた中止が「中止しました」になっていた。監査ログ: PostgreSQL の入れ子ブロックコメントの後の文が要約から消えていた
- インポート（続き 2）: 末尾に `;` のない MySQL ファイルが失敗するようになっていた（区切り文字の復元を、ファイル終端時点の区切り文字に合わせて付加）、`SET autocommit` は autocommit を OFF にした後に ON へ戻したときだけコミットとして数える（`ON` / `TRUE` / `@@session.` 表記も認識）、実行中の文を中断した中止（`ER_QUERY_INTERRUPTED` / `57014`）も「中止」と報告、最後の文の完了と同時に届いた中止を「中止しました」と表示していた、`COMMIT` 失敗時の成功数
- 監査ログ（続き）: 方言ごとのコメント・リテラル規則で読む（PostgreSQL では `#` はコメントでなく演算子、`--` は空白なしでもコメント、バックスラッシュは `E''` 以外でエスケープでない、`$a1$` のようなタグ）、MySQL の `/*!80000 … */` 内の文を要約に残す。serial 列が別のシーケンスも所有している場合に IDENTITY のオプションが入れ替わっていた、シーケンス値が範囲外の行で復元が止まっていた
- 表示タブ・行フォーム（続き）: 複製で表示上限を超える列に入力した値が送られず NOT NULL 列の複製ができなかった、インポート完了・中止後にキーボードフォーカスが失われていた、エクスポートの「選択を解除」がフォーカスを落としていた、概算件数のページ表示「ページ 2,118 / 2,117」を「/ 約 2,117」に、PostgreSQL のプロセス終了ダイアログでクエリの 1 行目（ラッパー）だけが見えていた、バッジと選択不可の行の説明を `aria-describedby` で提供
- 監査ログ: `;` を含むパスワードが `PREPARE` 文字列内で断片的に残っていた（文の分割を引用符対応に）、閉じ引用符のないパスワードがそのまま残っていた、`PASSWORD` の後の空白でのバックトラッキング（8 KB で 150 ms）。`/users/execute` も識別子長を検証。サーバー停止（`ER_SERVER_SHUTDOWN`）を接続断として扱いプールに戻さない。プロセス一覧のクエリ本文は先頭 65,536 文字
- 監査ログ: `PREPARE` / `EXECUTE` / `format()` に文字列として渡した認証文や `''` の入れ子、`SOURCE_PASSWORD`、接続文字列の `password=`、`AS 0x…` のハッシュがマスクされずに残っていた。インポートの監査行にファイル先頭 500 文字（行の値）が入っていた（`<import>` と文字数だけに）。平文 HTTP を拒否する前にログイン試行回数を消費していた、不正な `Host` ヘッダーで 500 になっていた、本番で `COOKIE_SECURE=0` のとき起動時に警告、NDJSON に `X-Accel-Buffering: no`、MySQL のユーザー名の上限を 32 文字で検証、アカウント操作の `COMMIT` 失敗を結果に表示しエラーメッセージ中のエンコード済みパスワードもマスク
- 表示上限（65,536 文字）がビュー定義・ルーチン本体・`SHOW CREATE TABLE` などのカタログ読み取りにも適用され、長い定義が `[object Object]` になりダンプにもそのまま書き出されていた（上限は表示行と SQL 結果だけに）。主キーのない MySQL テーブルのエクスポート中に接続が切れる（KILL、`net_write_timeout`）と応答が返らなくなっていた。途中で止めたエクスポートの残りの行が届き続けていた。長いテキストの切り詰めがサロゲートペアの途中で切れることがあった
- Docker イメージが `API_PORT=3100` を固定していたため PaaS の `PORT` が使われなかった（`HEALTHCHECK` も実際のポートに従う）。ドキュメント: 主キーのない MySQL テーブルのエクスポートに関する古い記述、`INSECURE_TRANSPORT` の対処、無通信タイムアウトとプロキシ設定、systemd 手順の順序、長いテキストと件数の打ち切り、インポートオプション、CHANGELOG の分類
- 表示タブ: NULL セルをキーボードで開いたときフォーカスが失われていた、別セルの保存エラーが次のエディターに残っていた、手で編集した URL（`page=0` など）でエラー画面になっていた、日時も折り返さない
- エクスポート: 全テーブルを選択した場合はデータベース全体のダンプ（ルーチン・イベントを含む）として扱う、一部選択時は「対象テーブルのトリガーも含める」と表示
- ログイン: 記憶した接続先のデータベース名が存在しないプリセットの読み取り専用欄に入ることがあった、ユーザー名が記憶されていればパスワード欄にフォーカス。テーブルオプションは実行後の値で再表示、サーバー SQL のデータベース選択の幅、一括操作の読み上げ（常時マウントのライブリージョン）と部分選択表示、権限表示の折り返し位置
- SQL エクスポート: 改行を含むオブジェクト名がダンプのコメント行を終わらせ、任意の SQL を復元時に実行させられた（コメント中の名前から改行を除去）
- キャンセルの初回接続に失敗した場合、同じ実行への以降のキャンセルも失敗し続けていた
- SQL エクスポート: PostgreSQL のオーバーロード関数が重複・連結されて壊れていた、`DEFINER` 除去が本文の文字列まで書き換えていた（ヘッダーのみに限定）、ルーチン・トリガー・イベントの `sql_mode`（イベントはタイムゾーンも）を保存して復元、ビューをルーチンの後・依存順に出力、MySQL のトリガーは実行順（FOLLOWS/PRECEDES）を維持、読めないルーチンはコメントで明示、PostgreSQL の `BEGIN ATOMIC` 関数本体を分割しない、`SET check_function_bodies = false`
- 権限ページ: MySQL 8.4 が `ALL PRIVILEGES` を個別の権限名で表示するため、root や「すべての権限」で作成したユーザーが「なし」と表示されていた（グローバル権限を「グローバル」バッジ付きで反映）
- PostgreSQL のアカウント操作（複数文）を 1 トランザクションで実行し、途中で失敗した場合に先行する GRANT が残らないようにした。サーバーの NOTICE / WARNING（所有者でない GRANT の「何も付与されませんでした」など）を結果とプレビューに表示
- プロセス: 自分のセッションが使う接続を終了したとき成功にもかかわらずエラー表示になっていた、tsmyadmin 自身の接続に「このツール」バッジ、待機イベントを active 以外に付けない、idle の経過時間を出さない、クエリなしを「–」で表示、確認ダイアログにユーザー・データベース・クエリを表示
- 識別子の長さ（MySQL 64 文字 / PostgreSQL 63 バイト）をプレビュー時に検証（PostgreSQL は黙って切り詰めていた）
- ログイン: MySQL のプリセットではデータベース欄を編集できる（アクセス権のないデータベースが固定されていると接続できなかった）
- 削除されたばかりのデータベースを MySQL で開くと空の一覧が表示されていた（見つからないエラーに）、DROP DATABASE の警告を方言ごとに、自分自身の判定を MySQL では `user@host` で行う
- インポート: UTF-8 として不正なファイル（`--hex-blob` なしの mysqldump、Latin-1 のテキスト）を置換文字で黙って取り込んでいた（拒否して行番号を示す）、閉じていない引用符で以降の CSV 行が 1 つのフィールドに吸い込まれていた、CSV を行ごとに保持していたメモリ使用量を削減（1 パスで挿入）
- Cookie が `Secure` の本番で平文 HTTP からログインすると、成功後に「セッションの有効期限が切れました」と表示され DB セッションだけが残っていた（`INSECURE_TRANSPORT` として拒否し、ログに `login.insecure_transport`）
- `TSMYADMIN_ALLOWED_HOSTS` の `db:abc` / `::1:5432` のように一致し得ない項目を起動時に拒否
- `/assets/*` の存在しないファイルに SPA の index.html を返していた（404 に）
- 監査ログ: `IDENTIFIED /*c*/ BY`、`PASSWORD --x⏎'…'`、`U&'…'` / `_utf8mb4'…'` などコメントや特殊なリテラルで書かれたパスワードがマスクされずに残っていた（コメント除去後に照合し、アカウント文の認証キーワード以降の文字列をすべてマスク）
- 挿入後のフォーカスが自動採番の列に置かれていた（次の列へ）、絞り込み中のインライン編集で行が結果から外れるとフォーカスが失われていた、削除・更新後の通知へのフォーカスでページ先頭へスクロールしない、挿入フォームで NOT NULL 列に NULL を選べなくした、無効化された入力の文字色のコントラストを 6.4:1 に、WebKit でマウス操作から開いたダイアログを閉じた後にフォーカスが戻らなかった、PostgreSQL でテーブル名変更の直後に旧テーブルの 404 が一瞬表示されていた、検索の LIKE 条件の説明を表の下の注記に移し狭い幅でも入力欄を確保
- テーブルオプションの実行後に「実行しました」の通知が消えフォーカスが外れていた（フォームを再マウントせず値だけ更新）、サーバーが無視した値（現在値より小さい AUTO_INCREMENT）が入力欄に残らない
- 挿入・編集フォーム: NULL / 既定値の列も入力欄に直接入力でき、入力するとチェックが外れる（無効化された入力欄が通常と見分けがつかなかった）。無効化された入力欄は背景色で区別
- 構造タブ: NOT NULL で既定値のない列の既定値を「NULL」でなく「なし」と表示
- 行のインライン編集で NULL のチェックを外すとフォーカスが入力欄へ移る、編集ダイアログや削除の成功後に古いインライン編集のエラーが残らない、削除後は通知にフォーカス、ログイン画面の初回表示でユーザー名にフォーカス、狭い幅で「いいえ」が折り返さない
- SQL ダンプ: ルーチン本文の `$` を含む識別子で区切りが壊れないよう `DELIMITER ;;` を使用、ビューの並びをテーブル名の部分一致でなく単語単位で解決、MySQL のトリガーに `DEFINER` を保持（除去オプションに従う）、PostgreSQL のビューの `WITH CHECK OPTION` / `security_barrier` / `security_invoker` / コメントと、ビューを参照する SQL 標準本文（`BEGIN ATOMIC`）の関数はビューの後に出力、存在しなくなったルーチンはコメントで飛ばす
- MySQL: インポート中の `SET sql_mode` による `NO_BACKSLASH_ESCAPES` の切り替えを分割器が追従（mysqldump の `/*!50003 … */` 形式、`@saved` 変数からの復元、複数代入、`REPLACE()` / `CONCAT()` を含む）
- SQL エディターの本文にも tabindex を付けて横スクロール時のアクセシビリティ検査を通す、インライン保存後に行が並び替わった場合はその行のセルへフォーカスを追従、無効化した行アイコンにホバー背景を出さない
- 行の編集・複製アイコンの見た目を元のサイズ・色に（Button の `icon` バリアント）、主キーのないテーブルや主キー列を編集した場合にインライン保存後のフォーカスが通知へ飛んでいた（他の列の値で行を同定）、200 行以上の SQL 結果（仮想スクロール）もキーボードでスクロール可能に
- 行の編集・複製アイコンも WebKit でフォーカスを戻す、絞り込み中のインライン編集で行が入れ替わった場合に別の行のセルへフォーカスが残らない（行キーで判定）、トリガー・イベント・ルーチンの定義が表を横に広げないよう折り返し、Enter でのフォーム送信でフォーカスが送信ボタンへ移らない
- SQL ダンプ（PostgreSQL）: SERIAL の親を継承した子テーブルが復元後に既定値を失い、共有シーケンスも進められていなかった、パーティション自身の主キーを保持
- SQL ダンプ（MySQL / MariaDB）: イベントの `COMMENT` にデータベース名が含まれると書き換えられていた（イベントは名前だけを非修飾化）、information_schema 本文へのフォールバック時に本文を書き換えない
- SQL ダンプ（PostgreSQL）: 空白を含む引用符付きインデックス名でも `ON ONLY` を外す、パーティション自身の `UNLOGGED` / ストレージパラメータ / コメントを保持、継承の子テーブルは自分で宣言した列だけを出力し継承列の独自の既定値は `ALTER TABLE ONLY` で設定（復元後に親の DROP COLUMN が子へ伝わるように）、SHOW VIEW 権限のないビューはコメントで明示してダンプを続行
- SQL ダンプ（MariaDB）: データベース名付きで作成されたトリガー・イベントの原文からヘッダーのデータベース修飾を外す（別データベースへの復元が元のデータベースに書き込んでいた）
- SQL ダンプ（PostgreSQL）: パーティションテーブルのインデックスが `ON ONLY` のまま出力され復元後に無効になっていた、パーティション自身のインデックス・制約・別スキーマの配置を保持、DROP セクションを 1 トランザクションで実行（ダンプ外の依存で止まったときに中途半端に消さない）
- SQL ダンプ（MySQL / MariaDB）: 読み取り権限のないトリガーをコメントで明示、MariaDB のビュー並び判定で `STRAIGHT_JOIN` も参照とみなす
- SQL ダンプ（PostgreSQL）: 継承の子テーブルを親の後に出力（名前順で子が先になると復元できなかった）、配列型・ドメイン経由でテーブルの行型を使う関数もテーブルの後に、引用符付きの関数名の DROP、ストレージパラメータ（TOAST を含む）を値を引用して出力
- SQL ダンプ（MariaDB）: ビュー定義の `\`o\`.\`col\`` のような修飾付き列名をビュー参照とみなしていた
- MySQL: 一覧と SHOW CREATE の間に消えたトリガー・イベントで一覧が失敗しない
- SQL ダンプ（PostgreSQL）: テーブルの行型を使う関数（`RETURNS SETOF t`、`t` 型の引数）をそのテーブルの後に出力（ルーチンをテーブルの前に移した変更で復元できなくなっていた）
- MySQL 8: 接続ごとに `information_schema_stats_expiry = 0` を設定し、AUTO_INCREMENT・行数・サイズが最大 24 時間古い値で表示されないようにした（テーブルオプションで設定した AUTO_INCREMENT が反映されないように見えていた）
- 横スクロールする SQL 表示をキーボードで到達可能に、テーブルオプションの再シードは構造の再取得を待ってから
- SQL ダンプ（MySQL / MariaDB）: トリガーとイベントの本文を `SHOW CREATE TRIGGER` / `SHOW CREATE EVENT` の原文から出力（information_schema はエスケープを処理済みの本文を返すため、`\'` や `\\` を含む本文が復元できない・変わっていた。権限がない場合は従来の本文にフォールバック）
- SQL ダンプ（PostgreSQL）: 「DROP TABLE IF EXISTS」オプションでは依存順の逆に並べた DROP をダンプ先頭にまとめて出力し `CASCADE` を使わない（ダンプに含まれない依存オブジェクトを黙って消していた。pg_dump --clean と同じ挙動）、外部キーの `MATCH FULL` / `SET DEFAULT (列)` / `NOT VALID`、`UNLOGGED`、ストレージパラメータ、`INHERITS`、`PARTITION BY` とパーティション、トリガーの `ENABLE ALWAYS` / `REPLICA`
- SQL ダンプ（MariaDB）: ビューの並び順の判定で、別名や列名がビュー名と同じ場合に誤った依存を検出していた（FROM / JOIN の位置だけを参照とみなす）
- SQL ダンプ（PostgreSQL）: CHECK / UNIQUE / EXCLUDE 制約、`DEFERRABLE` な外部キー、列の `COLLATE`、identity 列のシーケンスオプション（START / INCREMENT / CACHE …）、マテリアライズドビューのインデックス、`DISABLE TRIGGER` の状態、関数のコメントが失われていた。既定値・関数インデックスがルーチンを呼ぶテーブルを復元できるようルーチンをテーブルの前に出力
- SQL ダンプ: ビューと SQL 標準本文のルーチンの順序をサーバーのカタログ（PostgreSQL `pg_depend`、MySQL 8 `VIEW_TABLE_USAGE` / `VIEW_ROUTINE_USAGE`）で決定（列の別名がビュー名と同じ場合に順序を誤っていた。MariaDB は従来どおり定義文の参照で判定）、ビューが読む SQL 標準本文の関数をビューの前に出力
- MariaDB: PACKAGE / PACKAGE BODY をルーチン一覧に表示しダンプに含める（除外して黙って落としていた）、シーケンスの操作タブとダイアログの表題を「ビュー」でなく「シーケンス」に、MariaDB 固有のエラー番号（4000 番台）を MySQL 8 の名前で表示しない
- SQL ダンプ: イベントにも `DEFINER` を保持（除去オプションに従う）、PostgreSQL のオーバーロードごとに本文の種類を判定（`RETURN` 本文の関数だけをビューの後に出す）、PostgreSQL の前文に `SET standard_conforming_strings = on` を追加、MariaDB のシーケンス既定値の書き換えを `DEFAULT` 句に限定（コメント内の文字列を変更しない）、定義の表示で不要なカタログ照会をしない
- SQL ダンプで `DEFAULT CURRENT_TIMESTAMP` など式デフォルトを持つ MySQL 列（`EXTRA = DEFAULT_GENERATED`）が生成列と誤判定され INSERT から欠落していた
- MySQL: サーバーの `sql_mode` に `NO_BACKSLASH_ESCAPES` が含まれていても、値のプレースホルダが正しく解釈されるようセッションで無効化
- MySQL: JSON / FLOAT / DECIMAL 列を行キーとする更新・削除・エクスポートが一致しない／同じ行を繰り返す問題（型に合わせて `CAST`）、ENUM/SET キーのキーセットページング
- MySQL: `SQL_CALC_FOUND_ROWS` などトップレベル専用の修飾子、構文エラー時のメッセージが読み取り専用ラッパーの影響を受けていた
- MySQL: `2--2` のような減算を行コメントとして扱っていた（`-- ` の後に空白が必要）
- MySQL: GEOMETRY 列をバイナリとして往復（`{x, y}` への変換で SRID が失われていた）
- MySQL: 接続ごとのキャッシュがプールの再借用で効いていなかった（`USE` と timeout 設定が毎回送られていた）、リセット後の照合順序が握手時と異なっていた
- MariaDB: `max_execution_time` が無いサーバーでは `max_statement_time` へ、`STATISTICS.EXPRESSION` が無い場合は式なしへフォールバック
- 操作タブ: ビューに対して `TRUNCATE` / コピーを出さず、`DROP VIEW`（PostgreSQL は `DROP MATERIALIZED VIEW`）を生成。テーブルコピーの列一覧をサーバー側で決定（生成列を除外）
- `.env.example` をそのまま `.env` にコピーすると空の値が enum 検証で拒否されていた
- E2E: 並列ワーカーでの `SESSION_MAX_PER_IDENTITY` 超過によるセッション失効、後始末でユーザー・DB・スキーマも削除
- PostgreSQL: 主キーのないテーブルのエクスポートで行の欠落・重複が起きていた（`ORDER BY ctid` が `ctid::text` の出力列に束縛されていた）
- CSV: バイナリ列を base64 のまま文字列として取り込んでいた（列の型を見て復元）、引用符付きの `\N` を NULL と誤認、空行で中断
- エクスポートで存在しないテーブルを指定すると 200 + ダウンロード開始後に切断されていた（開始前に 404 を返す）
- PostgreSQL: データ付きテーブルコピーの後、コピー先の identity 列のシーケンスが 1 のままで次の挿入が重複エラーになっていた
- ソートの URL パラメータでカラム名に `,` / `:` を含められなかった（パーセントエンコード）。識別子に NUL を含むリクエストが 500 になっていた
- 設定: `TSMYADMIN_ALLOWED_HOSTS=`（空）の「プリセットのみ」の意味を維持、起動時エラーは常に `Invalid environment:` で始まる、プリセットの未知キー（`password` など）を拒否
- SQLite セッションストア: 開けない場合は `session_store.open_failed` を記録して終了、`SESSION_SECRET` 変更時に保存済みセッションを起動時に削除（`session_store.reset`）
- ログ: 成功した `/healthz` `/readyz` をアクセスログに出さない
- MySQL の SQL ダンプがデータベース名で修飾された文（DROP / INSERT）と修飾なしの CREATE を混在させていた → mysqldump と同じく修飾なしに統一（別名のデータベースへ復元できる。元のデータベースに触れない）
- BIGINT / DECIMAL（文字列で届く数値）が桁の途中で折り返していた、インライン編集で入力途中に別の場所をクリックすると無言で破棄していた（変更がある間は閉じない）
- 表示タブはソート未指定でも主キー（PostgreSQL は ctid）順で安定して並ぶ（ページ送りで行が飛ぶ・重複することがあった）
- サーバーの SQL タブの既定データベースをログイン時のデータベースに、権限表示の長い GRANT 文で操作列が画面外に押し出されていた、一括操作の確認は 1 件ならテーブル名・複数ならデータベース名、接続中ユーザー自身の削除・権限取り消しに警告、ビューの名前変更ラベル、ログイン失敗の英語重複、0 行のときの範囲表示、PostgreSQL のビューの行数
- Safari でインライン編集の「保存する」「NULL にする」を押すと編集がキャンセルされていた（フォーカス移動前にブラーが先行）
- プレビューダイアログ: 失敗時にフォーカスが本文へ落ちていた、変更のない場合に空の SQL でなく「変更はありません」を表示、実行中に閉じても結果を失わない
- サーバーの SQL タブで対象データベースを切り替えると入力中の SQL が消えていた、`/sql?db=` でエラー画面になっていた
- 一括操作バーをテーブルの下（チェックボックスの直後）に移動し件数を読み上げ、ヘッダーのチェックボックスに部分選択状態、AUTO_INCREMENT は 2^53 超も指定可、見出しレベルとダークエディターの行番号のコントラスト
- MariaDB: BIT 型の主キーでエクスポートが同じ行を繰り返し（無限ループ）、更新・削除が失敗していた（16 進リテラルが 0 に変換される）→ 数値にキャストして比較
- PostgreSQL: ダウンロードを途中で止めるとカーソルとトランザクションが開いたまま接続がプールに戻っていた（テーブルのロック、次のエクスポートが `cursor already exists`）→ 必ずロールバック
- 表示タブの絞り込み（=, < など）も列の型で比較（FLOAT 列に表示値を入れても 0 件だった）、PostgreSQL でカラムのコメントを消せなかった、文字列リテラルの解析（PostgreSQL の末尾バックスラッシュ、`$a1$` タグ）
- MySQL: 上限を超える `LIMIT` を書いた文が `sql_select_limit` を上書きし、結果全体を API に送っていた → その文は派生テーブルで包む。スクリプト内の `SET SESSION sql_select_limit` の後は上限を再適用
- キャンセル: 同じ実行への同時キャンセルは 1 本の専用接続を共有し、再送でも接続を開き直さない（連打で DB の `max_connections` を使い切れていた）
- ログイン失敗時に MySQL のホスト ACL メッセージ（API 側のアドレスを含む）を返さない
- ダイアログが画面左上に固定表示されていた（Tailwind の preflight が `<dialog>` の中央寄せを打ち消していた）
- ダークモードの SQL エディターでキーワードがほぼ見えなかった → テーマに追従するハイライトを追加
- PostgreSQL: ANALYZE 前のテーブルの行数を「–」でなく統計ビューから表示、エンジン列を非表示、権限表示からカタログスキーマの GRANT を除外、拡張機能の関数をルーチン一覧から除外
- ビューでは「挿入」「インポート」タブを出さない、存在しないデータベースではエラーだけを表示
- インライン編集: 別の場所をクリックすると閉じる、TEXT/JSON はテキストエリアで複数行編集（Ctrl/⌘+Enter で保存）
- SQL の履歴と保存済みクエリをサーバー（方言・ホスト・ポート）ごとに分け、結果の上に配置
- 数値が桁の途中で折り返されていた、ステータス変数の複数行の値（PEM など）を折りたたみ表示、テーブル作成後はその構造タブへ、CSV 取り込み後に「取り込んだ行を表示」リンク、ログインの接続先ラベルを短く、PostgreSQL 専用のヒントは MySQL では非表示
- MariaDB: SQL コンソールで `ORDER BY` 付きの SELECT が並び順を失っていた（派生テーブルのマージ）→ MySQL / MariaDB は `sql_select_limit` で行数を制限する方式に変更
- MySQL: BIGINT を含む複合キーで 2^53 超の値があるとエクスポートで行が欠落（行コンストラクタ内で DOUBLE 比較）→ 整数型を SIGNED / UNSIGNED にキャスト。バイナリ照合の ENUM/SET キーで大小文字違いのラベルが同一視され欠落 → `COLLATE utf8mb4_bin`
- MySQL: 先頭コメントの後の `DELIMITER` が認識されなかった（mysqldump のルーチン出力）
- 監査ログ: 失敗時にサーバーのエラーメッセージ（値を引用する `Duplicate entry 'x'` など）を記録していた → エラーコードだけを記録。MariaDB の `IDENTIFIED VIA … USING '…'` もマスク
- キャンセルを専用接続から送る（セッションのプールが埋まっていると「キャンセル」が効かなかった）、送信直前に対象がまだ実行中か再確認
- ログイン失敗の応答からサーバーのメッセージ（API 側のアドレスを含む）を除去、0.1.0 のセッションファイルも `SESSION_SECRET` 変更時に削除
- PostgreSQL: エクスポートをサーバーサイドカーソルに変更（主キーのないテーブルで `ctid` のキーセットが O(N²) だった、パーティション親などキーのない関係を 1 回の SELECT で全件読み込んでいた）。継承の親テーブルは子の行を含めずに出力（重複と欠落の原因だった）
- PostgreSQL: シーケンスの前進（`setval`）を最小値でクランプし空テーブルでは実行しない（`MINVALUE 1000` の identity 列でエラーになっていた）、SERIAL 列を持つテーブルのコピーに独自のシーケンスを付ける（元テーブルのシーケンスを共有して DROP できなくなっていた）
- キャンセル後の接続はプールに戻さず閉じる（遅れて届いた `KILL QUERY` / `pg_cancel_backend` が次の利用者の文を中断していた）
- 文字列リテラル・コメント内の `delete` などを DML と誤認して読み取りの行数上限が効かなくなっていた
- CSV エクスポートで空文字列を引用符付きで出力（1 カラムのテーブルで空行として読み飛ばされていた）、ヘッダー不一致のエラーメッセージを上限付きに、サーバーに到達できない場合は「サーバーと通信できません」と表示、エクスポート対象のテーブル名に `,` を含められるように（重複も除去）
- SQL コンソールの「キャンセル」が実行直後に押されると空振りすることがあった（`KILL QUERY` / `pg_cancel_backend` が文の到着前に届くと無効）→ 文の実行中は再送する
- MariaDB: ユーザー一覧（`mysql.user.account_locked` がない）を `global_priv` から取得、`SHOW GRANTS` のパスワードハッシュを除去、errno 1969 などに名前を補完。CI に MariaDB 11 の conformance ジョブを追加
- API: 許可外ホストへのログインは `HOST_NOT_ALLOWED`、上限超過は `PAYLOAD_TOO_LARGE`（インポートのファイル超過も 413）


## [0.1.0] - 2026-09-02

初回リリース。phpMyAdmin と同じ 3 階層（サーバー / データベース / テーブル）の画面構成。検証済み: MySQL 8.4 / PostgreSQL 17（想定: MySQL 8.0+ / MariaDB 10.6+ / PostgreSQL 13+）。

### 追加

- 接続: 運用側が定義する接続先プリセット、接続先 allowlist、ログインのレート制限、暗号化 SQLite セッションストア
- 閲覧: DB・スキーマ・テーブルのツリー（数千テーブルでも仮想化スクロール）、行のブラウズ（ソート・ページング・絞り込み・表示列の選択・外部キーの参照先 / 参照元リンク・概算件数）
- 編集: 行の挿入（続けて挿入・複製）、ダイアログ / インライン編集、一括削除。主キーがないテーブルも安全に 1 行だけ更新（PostgreSQL は `ctid`、MySQL は全カラム一致 + 影響行数検証）
- SQL コンソール: CodeMirror、複数文（MySQL `DELIMITER`、PostgreSQL ドル引用）、文ごとの結果ストリーミング、キャンセル、EXPLAIN、履歴、保存済みクエリ、結果の CSV / JSON ダウンロード
- 構造: テーブルの作成、カラムの追加・変更・削除、インデックス / 外部キーの追加・削除、名前変更・コピー、TRUNCATE / DROP（名前を再入力して確認）、データベース / スキーマの作成・削除
- エクスポート（SQL / CSV / JSON、ストリーミング）/ インポート（SQL スクリプト、CSV）
- ルーチン・トリガー・イベントスケジューラ（MySQL）の一覧と定義表示
- ユーザーアカウントの一覧・権限表示・作成・パスワード変更・削除、DB 単位の GRANT / REVOKE
- サーバー情報・ステータス変数・システム変数・プロセス一覧（KILL）
- 運用: `/healthz` `/readyz`、リクエスト ID 付き構造化ログ、監査ログ、CSP、グレースフルシャットダウン、Docker イメージ
- 品質: MySQL / PostgreSQL 両方で実行するアダプター適合テスト、API / Web ユニットテスト、Playwright（機能 × 両方言、axe、VRT）、静的検査（アーキテクチャ・SQL 安全性・ドキュメント同期）
