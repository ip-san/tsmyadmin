import type { CountKind, ImportReason, ImportWarning } from '@tsmyadmin/shared'
import type { Locale } from './ja.ts'

/** `1 row` / `2 rows` — English needs the singular that Japanese does not have. */
const plural = (n: number, one: string, many: string) => `${n.toLocaleString('en-US')} ${n === 1 ? one : many}`

/**
 * The English strings, key for key with `ja` (the `satisfies Locale` below fails the build when one drifts).
 *
 * Conventions: sentence case for labels and buttons, an imperative verb for the primary action of a form
 * ("Import", "Run"); full sentences (hints, notices) end with a period, labels and error prefixes do not;
 * a table's fields are "columns", its records "rows", and counts of anything else are plain numbers.
 * Articles are dropped in labels, buttons and titles ("Clear history", not "Clear the history") and kept in
 * full sentences. Apostrophes are typographic (’); the straight one is only a JavaScript delimiter.
 */
export const en = {
  app: { name: 'tsmyadmin', tagline: 'MySQL / PostgreSQL admin' },
  shortcuts: {
    title: 'Keyboard shortcuts',
    open: 'Shortcuts',
    search: 'Focus the sidebar table filter',
    prevPage: 'Previous page',
    nextPage: 'Next page',
    runSql: 'Run SQL',
    toggleSidebar: 'Show / hide sidebar',
    editCell: 'Edit the focused cell (browse)',
    help: 'Show this list',
    home: 'Go to the server home',
    database: 'Go to the structure of this database',
    tableStructure: 'Go to the structure of this table',
    tableBrowse: 'Go to the rows of this table',
    sql: 'Go to the SQL tab of this level',
    export: 'Go to the export tab of this level',
    complete: 'Open completion in the SQL editor',
    editHint: 'Paging keys are inactive while an input has focus.',
  },
  session: {
    expiring: (minutes: number) => `Nothing has been done for a while: the session ends in about ${minutes} min.`,
    extend: 'Keep the session',
  },
  common: {
    loading: 'Loading…',
    error: 'Error',
    retry: 'Retry',
    cancel: 'Cancel',
    save: 'Save',
    delete: 'Delete',
    close: 'Close',
    yes: 'Yes',
    no: 'No',
    null: 'NULL',
    empty: '(empty)',
    printList: 'Print this list',
    binary: (bytes: number) => `[binary, ${bytes.toLocaleString('en-US')} bytes]`,
    truncatedText: (length: number) => ` … (truncated, ${length.toLocaleString('en-US')} characters in total)`,
    bytes: (n: number) => {
      if (n < 1024) return `${n.toLocaleString('en-US')} B`
      const units = ['KB', 'MB', 'GB', 'TB']
      let v = n / 1024
      let i = 0
      // 1023.6 KB rounds to 1,024 KB: roll over to the next unit before it would print as 1024.
      while (v >= 1023.5 && i < units.length - 1) {
        v /= 1024
        i++
      }
      return `${v.toLocaleString('en-US', { maximumFractionDigits: v < 10 ? 1 : 0 })} ${units[i]}`
    },
    unknown: 'Unknown',
    theme: 'Toggle theme',
    language: 'Language',
    skipToContent: 'Skip to main content',
    reload: 'Reload',
    showMore: (n: number) => `Show all (${n.toLocaleString('en-US')} characters)`,
    showLess: 'Collapse',
    parenthesised: (s: string) => `(${s})`,
    showHead: (n: number) => `Show the first ${n.toLocaleString('en-US')} characters`,
    backHome: 'Back to the start page',
  },
  errorPage: {
    title: 'Something went wrong while rendering this page',
    body: 'If retrying the action does not help, reload the page.',
    notFoundTitle: 'Page not found',
    notFoundBody: 'The URL is wrong, or the database or table it names does not exist.',
  },
  login: {
    dockerLogin:
      "Connects with the login in this container's environment (the user and password it was started with). Nothing to type.",
    advanced: 'Advanced',
    collation: 'Connection collation',
    collationHint:
      'The rules this connection uses to compare strings and convert character sets in SQL (MySQL / MariaDB).',
    collationDefault: '(default: utf8mb4_unicode_ci)',
    code: 'One-time code',
    codeHint: 'The six digits from your authenticator app, or a recovery code.',
    usePasskey: 'Use a passkey',
    title: 'Connect to a server',
    preset: 'Server',
    presetManual: 'Enter manually',
    presetHint:
      'A server your administrator configured. Enter your user name and password (on MySQL you may also change the database).',
    dialect: 'Server type',
    mysql: 'MySQL / MariaDB',
    postgres: 'PostgreSQL',
    host: 'Host',
    port: 'Port',
    user: 'Username',
    password: 'Password',
    database: 'Database',
    databaseHint: 'PostgreSQL needs a database to connect to (postgres when left empty).',
    submit: 'Connect',
    connecting: 'Connecting…',
    sessionExpired: 'Your session has expired. Connect again.',
    passwordChanged: 'Your password was changed. Connect with the new one.',
  },
  nav: {
    routines: 'Routines',
    events: 'Events',
    noPrograms: 'None',
    resizeSidebar: 'Sidebar width (the left and right arrow keys work too)',
    expandAll: 'Expand all',
    collapseAll: 'Collapse all',
    reloadTree: 'Reload the tree',
    showMore: (n: number) => `Show ${n.toLocaleString('en-US')} more`,
    groupOf: (name: string, n: number) => `${name} (${n.toLocaleString('en-US')})`,
    favoriteTables: 'Favorites',
    recentTables: 'Recent tables',
    favorite: 'Add to favorites',
    unfavorite: 'Remove from favorites',
    server: 'Server',
    databases: 'Databases',
    tables: 'Tables',
    logout: 'Disconnect',
    breadcrumb: 'Breadcrumb',
    help: 'Help',
    helpUrl: 'https://github.com/ip-san/tsmyadmin/blob/main/docs/en/user-guide.md',
    opensNewTab: ' (opens in a new tab)',
    expand: (name: string) => `Expand / collapse ${name}`,
    showSidebar: 'Show sidebar',
    hideSidebar: 'Hide sidebar',
    filterTables: 'Filter tables',
    noTables: 'No tables',
    matchCount: (shown: number, total: number) =>
      `${shown.toLocaleString('en-US')} of ${total.toLocaleString('en-US')}`,
    connectedAs: (user: string, host: string) => `${user}@${host}`,
    currentDatabase: 'Current database',
    tree: 'Database tree',
  },
  tabs: {
    security: 'Security',
    userGroups: 'User groups',
    tracking: 'Tracking',
    snapshots: 'Snapshots',
    databases: 'Databases',
    structure: 'Structure',
    sql: 'SQL',
    search: 'Search',
    query: 'Query',
    designer: 'Designer',
    insert: 'Insert',
    browse: 'Browse',
    central: 'Central columns',
    operations: 'Operations',
    export: 'Export',
    import: 'Import',
    users: 'Users',
    privileges: 'Privileges',
    status: 'Status',
    variables: 'Variables',
    processes: 'Processes',
    advisor: 'Advisor',
    monitor: 'Monitor',
    routines: 'Routines',
    triggers: 'Triggers',
    events: 'Events',
  },
  databaseSearch: {
    deleteRows: 'Delete the matching rows…',
    deleteLabel: (table: string) => `${table}: delete the matching rows`,
    deleteTitle: (table: string) => `Delete the matching rows of ${table}`,
    deleteWarning: 'Every matching row is removed — only the rows this search found.',
    deleted: (table: string) => `Deleted the matching rows of ${table}`,
    title: 'Search the database',
    term: 'Search for',
    termHint:
      'Not case-sensitive. % and _ are searched for as the characters themselves. Columns whose values have no readable text form (binary, spatial and the like) are not searched.',
    tables: 'Tables to search',
    mode: 'Search for',
    modes: {
      phrase: 'the phrase',
      any: 'any of the words (separated by spaces)',
      all: 'all of the words (anywhere in the row)',
      regexp: "a regular expression (the server's syntax)",
    },
    column: 'Only columns named like',
    columnHint: 'Searches only columns whose name contains this',
    selectAll: 'Select all',
    selectNone: 'Clear selection',
    run: 'Search',
    stop: 'Stop',
    progress: (done: number, total: number) => `Searched ${done} of ${total} tables`,
    stopped: 'Stopped (showing the results so far)',
    noneSelected: 'Choose at least one table',
    noTables: 'This database has no tables',
    table: 'Table',
    matches: 'Matching rows',
    actions: 'Actions',
    rows: (n: number, lowerBound: boolean) => `${n.toLocaleString('en-US')}${lowerBound ? '+' : ''} rows`,
    summary: (term: string, rows: number, tables: number, lowerBound: boolean) =>
      `“${term}”: ${rows.toLocaleString('en-US')}${lowerBound ? '+' : ''} rows in ${tables.toLocaleString('en-US')} tables`,
    openInSql: 'Open in SQL tab',
    openInSqlLabel: (table: string) => `Open the matching rows of ${table} in the SQL tab`,
    noColumns: 'No searchable columns',
    searching: 'Searching…',
  },
  queryBuilder: {
    insert: 'Insert before',
    insertBefore: (label: string) => `${label}: insert before`,
    templates: {
      title: 'Saved setups',
      name: 'Name',
      saveHint: 'Saves the tables, columns, conditions and joins as they are, under a name',
      empty: 'Nothing saved for this database',
      deleteLabel: (name: string) => `Delete ${name}`,
    },
    joins: 'Joins (blank: along a foreign key)',
    joinAuto: 'Along a foreign key',
    joinKind: (table: string) => `Join of ${table}`,
    joinFrom: (table: string) => `Join of ${table}: its column`,
    joinTo: (table: string) => `Join of ${table}: a column of a table before it`,
    title: 'Build a query',
    intro:
      'Choose tables, columns and conditions to build a SELECT. Several tables are joined along their foreign keys, or as set under Joins (the kind and the columns). The SQL opens in the SQL tab to edit and run; add free-form expressions there. A setup can be saved under a name.',
    tables: 'Tables',
    tablesHint:
      'The first table you choose is the starting point; the others are LEFT JOINed in turn along the foreign keys that reach them.',
    joinOrder: (tables: string) => `Join order: ${tables}`,
    chooseTable: 'Choose at least one table',
    noTables: 'This database has no tables',
    columns: 'Output columns',
    allColumnsHint: 'Add no columns to output all of them (*).',
    column: 'column',
    chooseColumn: '(choose a column)',
    alias: 'alias',
    show: 'show',
    sort: 'sort',
    noSort: 'none',
    asc: 'ascending',
    desc: 'descending',
    addColumn: 'Add a column',
    outputLabel: (n: number) => `Output row ${n}`,
    removeOutput: (label: string) => `Remove ${label.toLowerCase()}`,
    fieldLabel: (label: string, field: string) => `${label} ${field}`,
    criteria: 'Conditions',
    group: (n: number) => `Condition group ${n} (all must hold)`,
    or: 'or',
    conditionLabel: (group: number, n: number) => `Group ${group} condition ${n}`,
    operator: 'operator',
    value: 'value',
    addCondition: 'Add an AND condition',
    addFirstCondition: 'Add a condition',
    addGroup: 'Add an OR group',
    removeCondition: (label: string) => `Remove ${label.toLowerCase()}`,
    remove: 'Remove',
    build: 'Build SQL',
    building: 'Building…',
    result: 'Generated SQL',
    built: 'SQL built',
    stale: 'Your choices have changed. Press “Build SQL” again.',
    openInSql: 'Open in the SQL tab',
  },
  designer: {
    compact: 'Table names only',
    snap: 'Snap to grid',
    showLines: 'Show lines',
    lineLabels: 'Label the lines',
    lineStyle: 'Line shape',
    lineStyles: { curve: 'Curve', straight: 'Straight', polyline: 'Right angles' },
    fullscreen: 'Full screen',
    exitFullscreen: 'Exit full screen',
    exportDia: 'Save as DIA',
    exportEps: 'Save as EPS',
    title: 'Designer',
    hint: 'Tables and the foreign keys between them. Drag a table to move it (or focus it and use the arrow keys; Shift moves further). The layout can be kept in this browser, or under a name as a page.',
    relateHint:
      'Pick the column that will hold the key, then the column it refers to (in another table). When both are picked, the SQL for the key opens for you to review.',
    relateFrom: (table: string, column: string) =>
      `A key on ${table}.${column}. Now pick the column it refers to (in another table).`,
    relateStart: 'Create keys on the diagram',
    relateStop: 'Back to moving tables',
    columnLabel: (table: string, column: string) => `${column} of ${table}`,
    allColumns: 'Show all columns',
    exportSvg: 'Save as SVG',
    exportPdf: 'Save as PDF (print)',
    exportPdfHint: 'Opens the browser’s print dialog; choose “Save as PDF” as the destination',
    drop: 'Delete',
    dropKey: (table: string, name: string) => `Delete foreign key ${name} of ${table}`,
    displayColumns: 'Display columns',
    displayColumnsHint:
      'For each table, the column shown beside the values of other tables’ foreign keys. The diagram marks it with ◆.',
    displayColumnsNeedColumns: 'Tick “Show all columns” to choose one for each table.',
    pages: {
      title: 'Saved pages',
      name: 'Page name',
      saveHint: 'Saves the current layout under a name',
      load: 'Open',
      summary: (n: number) => `Layout of ${n} tables`,
      empty: 'No pages saved for this database',
      deleteLabel: (name: string) => `Delete ${name}`,
    },
    diagram: 'Diagram of tables and foreign keys (the same as the Foreign keys table below)',
    boxLabel: (table: string) => `Table ${table} (arrow keys move it)`,
    resetLayout: 'Reset layout',
    fromTable: 'Table the key belongs to',
    relations: 'Foreign keys',
    noRelations: 'This database has no foreign keys',
    noTables: 'This database has no tables',
    table: 'Table',
    columns: 'Columns',
    references: 'References',
    refColumns: 'Referenced columns',
    constraint: 'Constraint',
  },
  databaseOps: {
    copyMode: {
      label: 'What to copy',
      both: 'Structure and data',
      structure: 'Structure only',
      data: 'Data only',
      dataHint: 'The target needs the same tables already. Only the columns it has are copied.',
    },
    switchToCopy: 'Switch to the database after copying',
    copyKeep: {
      foreignKeys: 'Copy the foreign keys too',
      autoIncrement: 'Carry the next AUTO_INCREMENT values over',
      privileges: 'Give accounts the same privileges (on this database)',
    },
    collationHintMysql: (current: string) =>
      `The default collation of new tables (now: ${current || 'unknown'}). With the box ticked every table and column is converted too (values are converted to the new character set).`,
    collationHintPostgres:
      "A PostgreSQL database's collation is set when it is created and cannot change; this gives every text column of the schema's tables the collation.",
    applyToTables: 'Apply to the existing tables and columns too',
    newName: 'New database name',
    copyName: 'Name of the copy',
    renameHintMysql:
      'MySQL has no statement for renaming a database, so a new one is created and the tables are moved into it. The old database is kept rather than dropped. A database with views, routines, triggers or events cannot be renamed.',
    renameHintPostgres: 'This fails while anyone else is connected to the database.',
    copyHintMysql:
      'Copies the tables’ structure and data. Foreign keys, views, routines, triggers, events and privileges are not copied.',
    copyHintPostgres:
      'Copies the whole database — tables, views, functions and data. It fails while anything is running in the source, and needs its owner or a superuser.',
    renameWarningMysql:
      'The old database is kept, not dropped. Seeing no tables in it does not mean it is empty: routines, events or tables your account has no privilege on can still be there. If you drop it, check its contents first with an administrator account that can see everything. An application still configured with the old name that creates its tables on startup will quietly keep writing to the old database. Privileges granted on it (GRANT) do not carry over to the new name.',
    renameWarningPostgres:
      'Idle tsmyadmin connections to this database under the same account are closed. If anything is still running there, nothing changes and the rename fails.',
    systemDatabase: 'The server’s own databases cannot be renamed or copied.',
    connectedDatabase:
      'The database this session is connected through cannot be renamed or copied. Sign in again with a different database.',
  },
  database: {
    title: (db: string) => `Database: ${db}`,
    schema: 'Schema',
    table: 'Table',
    kind: 'Type',
    kinds: { table: 'Table', view: 'View', materialized_view: 'Materialized view', sequence: 'Sequence' },
    rowEstimate: 'Rows (estimate)',
    count: 'Count',
    countOf: (table: string) => `Count the rows of ${table} exactly`,
    estimated: "The catalog's estimate",
    exactCount: 'Counted exactly',
    size: 'Size',
    tableCount: 'Tables',
    engine: 'Engine',
    comment: 'Comment',
    actions: 'Actions',
    noTables: 'This database has no tables',
    total: (count: number) => `Total (${count.toLocaleString('en-US')})`,
  },
  table: {
    distinct: {
      button: 'Distinct values',
      title: (column: string) => `Distinct values of ${column}`,
      count: 'Rows',
      none: 'There are no rows.',
      truncated: (n: number) => `Only the ${n.toLocaleString('en-US')} most frequent values are shown.`,
    },
    columns: 'Columns',
    indexes: 'Indexes',
    foreignKeys: 'Foreign keys',
    createStatement: 'CREATE statement',
    displayColumn: 'Display column',
    displayColumnAuto: '(automatic: the first text column)',
    displayColumnHint:
      "The column that names this table's rows beside another table's foreign key values (remembered by the browser)",
    stats: {
      title: 'Space and row statistics',
      print: 'Print this structure',
      space: 'Space usage',
      rowsTitle: 'Row statistics',
      data: 'Data',
      index: 'Indexes',
      free: 'Free (reclaimed by OPTIMIZE)',
      toast: 'TOAST (storage for large values)',
      total: 'Total',
      rowEstimate: 'Rows (estimated)',
      avgRow: 'Average row size',
      rowFormat: 'Row format',
      createdAt: 'Created',
      updatedAt: 'Updated',
      checkedAt: 'Last checked',
      deadRows: 'Dead rows (awaiting VACUUM)',
      lastVacuum: 'Last VACUUM',
      lastAnalyze: 'Last ANALYZE',
      hint: "Figures from the server's catalog, as of its last statistics update.",
    },
    name: 'Name',
    type: 'Type',
    nullable: 'NULL',
    default: 'Default',
    extra: 'Extra',
    comment: 'Comment',
    collation: 'Collation',
    unique: 'Unique',
    primary: 'Primary key',
    noDefault: 'None',
    indexType: 'Type',
    references: 'References',
    onUpdate: 'ON UPDATE',
    onDelete: 'ON DELETE',
    noIndexes: 'No indexes',
    noForeignKeys: 'No foreign keys',
    referencedBy: 'Referenced by (foreign keys pointing at this table)',
    noReferencedBy: 'No foreign key references this table',
    fromTable: 'Referencing table',
  },
  browse: {
    showAll: 'Show all',
    showAllConfirm: 'Show all rows',
    showAllWarning: (n: number) =>
      `This table has ${n.toLocaleString('en-US')} rows. Reading them all takes a while and slows the browser (the server stops at 100,000). Show them?`,
    showingAll: (n: number) => `showing ${n.toLocaleString('en-US')} rows`,
    backToPages: 'Back to pages',
    profiling: 'Show where the time goes (profile)',
    bookmarked: 'Saved as a bookmark',
    selection: {
      edit: 'Edit selected rows',
      csv: 'Selected rows as CSV',
      json: 'Selected rows as JSON',
      copy: 'Copy selected rows',
      chart: 'Chart this page',
      cut: 'A selected row holds a value cut for display: download or copy them from the Export tab.',
    },
    total: (n: number | null, count: CountKind = 'exact') =>
      n === null
        ? 'Row count unknown'
        : count === 'estimate'
          ? `Approx. ${plural(n, 'row', 'rows')}`
          : count === 'lower_bound'
            ? `At least ${plural(n, 'row', 'rows')}`
            : plural(n, 'row', 'rows'),
    range: (from: number, to: number) => `rows ${from.toLocaleString('en-US')}–${to.toLocaleString('en-US')}`,
    perPage: 'Rows per page',
    pageLabel: (page: number, last: number | null, count: CountKind = 'exact') =>
      last === null
        ? `Page ${page.toLocaleString('en-US')}`
        : count === 'estimate'
          ? `Page ${page.toLocaleString('en-US')} of about ${last.toLocaleString('en-US')}`
          : `Page ${page.toLocaleString('en-US')} of ${last.toLocaleString('en-US')}`,
    prev: 'Previous',
    next: 'Next',
    first: 'First page',
    last: 'Last page',
    sortAsc: 'Sort ascending',
    sortDesc: 'Sort descending',
    clearSort: 'Clear sorting',
    multiSortHint: ' (Shift-click for several columns)',
    sortOrder: (n: number) => `Sort position ${n}`,
    noRows: 'No rows',
    readOnly: 'A view is read-only: rows cannot be inserted or edited here.',
    readOnlySequence: 'A sequence takes no rows (its values come from NEXTVAL()).',
    selectAll: 'Select all',
    selected: (n: number) => `${plural(n, 'row', 'rows')} selected`,
    deleteSelected: 'Delete selected rows',
    executedSql: 'Executed SQL',
    queryTime: (ms: number) => `${ms < 1 ? ms.toFixed(2) : ms.toFixed(1)} ms`,
    boundValues: 'Bound values:',
    binaryValue: '(binary)',
    deleteExecute: 'Delete',
    deleteConfirm: (n: number) => `Delete ${plural(n, 'row', 'rows')}?`,
    editCell: 'Edit cell (double-click / Enter)',
    setNull: 'Set to NULL',
    editHint: 'Enter saves, Esc cancels',
    editHintMultiline: 'Ctrl/⌘ + Enter saves, Esc cancels',
    fkLink: (table: string, column: string) => `Open the referenced row in ${table}.${column}`,
    reverseLink: (table: string, column: string) => `Show the rows referencing this one through ${table}.${column}`,
    reverseMany: (n: number) => `Show the ${n} tables referencing this row`,
    columns: 'Columns',
    columnsShown: (shown: number, total: number) => `Columns ${shown}/${total}`,
    columnsAll: 'Show all',
    columnsNone: 'Show the first columns only',
    moveLeft: (c: string) => `Move ${c} left`,
    moveRight: (c: string) => `Move ${c} right`,
    display: 'Display',
    sortByKey: 'Sort by key',
    noKeySort: '(none)',
    byIndex: (name: string, asc: boolean) => `${name} (${asc ? 'ascending' : 'descending'})`,
    download: 'Download',
    downloadValue: (column: string) => `Download the value of ${column}`,
    fullText: 'Show long values in full',
    fkDisplay: "Show the referenced row's name beside a foreign key",
    binaryAsHex: 'Show binary values as hex',
    geometryAsWkt: 'Show spatial values as WKT',
    hexMore: (bytes: number) => `… (${bytes.toLocaleString('en-US')} bytes in all)`,
    keyHint: {
      pk: 'Rows are addressed by their primary key',
      ctid: 'Rows are addressed by ctid (no primary key)',
      'all-columns': 'Rows are addressed by every column (no primary key)',
      none: 'Not editable: this is a view or a sequence, or the table has no key that identifies rows uniquely',
    },
  },
  rows: {
    previewSql: 'Preview the SQL',
    previewTitle: 'The INSERT',
    previewNote:
      'Nothing is run. The values are not written into the statement: they are shown apart, as they are bound.',
    boundValues: 'Bound values',
    afterInsert: {
      label: 'After insertion',
      blank: 'Stay here (empty form)',
      keep: 'Stay here (keep what I typed)',
      browse: 'Back to the rows',
    },
    ignoreErrors: 'Ignore errors while inserting',
    ignoreHint:
      'INSERT IGNORE on MySQL, ON CONFLICT DO NOTHING on PostgreSQL: a row the server cannot take (a duplicate, say) is skipped instead of failing.',
    ignoredRows: (n: number) => `${n.toLocaleString('en-US')} row(s) skipped: the server did not accept them`,
    patternMismatch: 'The value does not fit the pattern set for this column',
    invalidIpv4: 'Enter an IPv4 address (192.168.0.1) or a whole number',
    invalidJson: 'That is not valid JSON',
    invalidXml: 'That is not valid XML',
    filesTooLarge:
      'The files in this row are too large together (about 512 KB at most). Put larger values in with SQL.',
    updatedRows: (n: number) => `Updated ${plural(n, 'row', 'rows')}`,
    editRowsTitle: (n: number) => `Edit ${plural(n, 'row', 'rows')}`,
    updatedBeforeError: (n: number) =>
      `${plural(n, 'row was', 'rows were')} updated; the rest were not, because of the error.`,
    function: 'Function',
    noFunction: '(no function)',
    functions: {
      now: 'NOW (current date and time)',
      current_date: 'CURRENT_DATE (today)',
      current_time: 'CURRENT_TIME (the time now)',
      uuid: 'UUID',
      md5: 'MD5',
      sha1: 'SHA1',
      sha256: 'SHA-256',
      upper: 'UPPER (to upper case)',
      lower: 'LOWER (to lower case)',
      trim: 'TRIM (strip surrounding spaces)',
    },
    fromFile: 'From a file',
    fileTooLarge: (max: number) =>
      `The file is too large (up to ${Math.round(max / 1024).toLocaleString('en-US')} KB). Put larger values in with SQL.`,
    rowCount: 'Rows at once',
    rowNumber: (n: number) => `Row ${n}`,
    inRow: (name: string, n: number) => `${name} (row ${n})`,
    insertTitle: 'Insert a row',
    editTitle: 'Edit row',
    column: 'Column',
    type: 'Type',
    value: 'Value',
    useDefault: 'Use default',
    setNull: 'NULL',
    binaryReadOnly: 'Binary values and text past the display limit cannot be edited here (use the SQL tab).',
    generatedReadOnly:
      'This is a generated column: the server computes it from the other columns, so no value can be given.',
    opaqueNotCopied: 'Binary values and text past the display limit are not copied.',
    notAddressable:
      'This row holds a binary value or text past the display limit, so it cannot be identified uniquely (use the SQL tab)',
    insert: 'Insert',
    save: 'Save',
    inserted: (n: number) => `Inserted ${plural(n, 'row', 'rows')}`,
    copyTitle: 'Duplicate the row',
    copyRow: (n: number) => `Duplicate row ${n.toLocaleString('en-US')}`,
    copyHint: 'Auto-increment / identity columns take a new value from their default.',
    updated: 'Row updated',
    deleted: (n: number) => `Deleted ${plural(n, 'row', 'rows')}`,
    nothingChanged: 'Nothing changed',
    deleteRow: (n: number) => `Delete row ${n.toLocaleString('en-US')}`,
    editRow: (n: number) => `Edit row ${n.toLocaleString('en-US')}`,
    selectRow: (n: number) => `Select row ${n.toLocaleString('en-US')}`,
    backToBrowse: 'Back to Browse',
  },
  search: {
    chooseValue: '(choose a value)',
    title: 'Search the table',
    operator: 'Operator',
    noCondition: '(no condition)',
    apply: 'Search',
    clear: 'Clear conditions',
    activeFilters: 'Filtered by',
    likeHint: 'In LIKE / NOT LIKE, % matches any string and _ any single character.',
    ops: {
      eq: '=',
      neq: '≠',
      lt: '<',
      lte: '≤',
      gt: '>',
      gte: '≥',
      contains: 'contains',
      starts_with: 'starts with',
      like: 'LIKE',
      not_like: 'NOT LIKE',
      is_null: 'IS NULL',
      is_not_null: 'IS NOT NULL',
      in: 'IN (any of)',
      not_in: 'NOT IN',
      between: 'BETWEEN (range)',
      not_between: 'NOT BETWEEN',
      regexp: 'REGEXP (regular expression)',
      not_regexp: 'NOT REGEXP',
      empty: "= '' (empty string)",
      not_empty: "≠ '' (not empty)",
    },
    listHint: 'Separate IN values with commas; give BETWEEN as “low, high”.',
    options: {
      title: 'Options (columns shown, DISTINCT, sort, rows, conditions in SQL)',
      columns: 'Columns to show',
      distinct: 'One row for each distinct result (DISTINCT)',
      whereSql: 'Add conditions in SQL (the body of a WHERE clause)',
      whereSqlHint:
        'AND-ed with the conditions above. With DISTINCT or conditions in SQL, the SELECT opens in the SQL tab.',
      sortBy: 'Sort by',
      noSort: '(no sort)',
      asc: 'Ascending',
      desc: 'Descending',
      limit: 'Rows per page',
    },
    listPlaceholder: { in: '1, 2, 3', between: '10, 20' },
  },
  sql: {
    after: { edit: 'Edit', rerun: 'Run again', explain: 'EXPLAIN', code: 'As code', bookmark: 'Bookmark' },
    code: {
      title: 'Code for an application',
      language: 'Language',
      languages: { php: 'PHP', javascript: 'JavaScript', python: 'Python', java: 'Java' },
      copy: 'Copy',
      note: 'Only the string literal. Pass values through placeholders instead of writing them into the string.',
    },
    createView: 'Create a view from this SELECT',
    run: 'Run',
    runHint: 'Ctrl/⌘ + Enter runs',
    running: 'Running…',
    cancel: 'Cancel',
    cancelling: 'Cancelling…',
    cancelled: 'Cancelled',
    openTransaction:
      'A transaction was still open when the run finished, so it was rolled back. The connection is returned after every run: keep BEGIN and COMMIT in the same run.',
    format: 'Format',
    formatHint:
      'Breaks the statement into a line per clause (the case of words and the text of strings are left as they are)',
    runOptions: {
      title: 'Run options',
      delimiter: 'Delimiter',
      delimiterHint: 'What ends a statement (the mysql client’s DELIMITER). Change it when a routine body holds ;',
      rollback: 'Roll back at the end',
      rollbackHint: {
        mysql:
          'Starts with START TRANSACTION and rolls back at the end. Statements that commit implicitly, such as table definition changes, are not undone',
        postgres: 'Starts with BEGIN and rolls back at the end. PostgreSQL definition changes are rolled back too',
      },
      foreignKeyChecks: 'Check foreign keys',
      foreignKeyChecksHint: {
        mysql: 'Off sets FOREIGN_KEY_CHECKS = 0 for this run only',
        postgres:
          'Off sets session_replication_role = replica for this run only (superuser; foreign key triggers stop firing)',
      },
      parameters: 'Parameters (values for :name)',
      parameterCount: (n: number) => `${n} parameters`,
      valueOf: (name: string) => `Value of ${name}`,
    },
    maxRows: 'Row limit',
    profiling: {
      option: 'Profiling',
      hint: 'Shows, for each statement, the time the server spent in each stage (MySQL / MariaDB SHOW PROFILE)',
      title: 'Profile',
      summary: (seconds: number) => `Profile (${seconds.toFixed(6)} s in all)`,
      state: 'Stage',
      seconds: 'Seconds',
    },
    stopOnError: 'Stop on error',
    safeMode: 'Confirm UPDATE / DELETE without WHERE',
    safeModeHint:
      'An UPDATE or DELETE without a WHERE changes every row. This asks before running one. It reads the text, so it may ask once too often; it never stays silent when it should ask.',
    safeModeTitle: 'This affects every row',
    safeModeBody: (kinds: string) =>
      `The script contains a ${kinds} with no WHERE. Every row of the table will be changed or deleted. Run it anyway?`,
    editor: 'SQL editor',
    results: 'Results',
    statement: (n: number) => `Statement ${n}`,
    rowsResult: (n: number, ms: number) => `${plural(n, 'row', 'rows')} (${ms} ms)`,
    affected: (n: number, ms: number) => `${plural(n, 'row', 'rows')} affected (${ms} ms)`,
    truncated: (max: number) => `Showing the first ${max.toLocaleString('en-US')} rows (the "Row limit" setting).`,
    errorPosition: (line: number, column: number) => ` (line ${line}, character ${column})`,
    nativeCode: 'Server error code',
    empty: 'No SQL to run',
    completed: (n: number, errors: number) =>
      errors > 0
        ? `Ran ${plural(n, 'statement', 'statements')}, ${errors.toLocaleString('en-US')} failed`
        : `Ran ${plural(n, 'statement', 'statements')}`,
    explain: 'EXPLAIN',
    explainHint: 'Show the query plan (one statement only)',
    downloadCsv: 'CSV',
    downloadJson: 'JSON',
    resultActions: (n: number) => `Result of statement ${n}:`,
    copy: 'Copy',
    copied: 'Copied to the clipboard (tab-separated)',
    copyFailed: 'Could not copy: the browser does not allow writing to the clipboard',
    print: 'Print',
    printedAt: (at: string) => `Printed: ${at}`,
    printCut: (max: number, total: number) =>
      `Only the first ${max.toLocaleString('en-US')} rows are printed (of ${total.toLocaleString('en-US')})`,
    chart: {
      toggle: 'Chart',
      kinds: {
        bar: 'Bar chart',
        line: 'Line chart',
        spline: 'Spline chart',
        area: 'Area chart',
        pie: 'Pie chart',
        scatter: 'Scatter plot',
        timeline: 'Timeline',
      },
      xAxis: 'Horizontal axis column',
      other: 'Other',
      noPositive: 'There are no positive values to make a pie of',
      noPoints: 'No row can be placed (a row needs a value on both axes)',
      noDates: 'There is no date column to make a timeline of',
      pieNote: 'The first of the chosen values is drawn as the pie (values of 0 or less are left out).',
      saveSvg: 'Save as SVG',
      savePng: 'Save as PNG',
      saveFailed: 'The picture could not be made',
      kind: 'Chart type',
      x: 'Categories (horizontal axis)',
      y: 'Values (vertical axis)',
      noNumbers: 'No numeric column to chart',
      chooseSeries: 'Choose at least one value column',
      clipped: (max: number) => `Charting the first ${max.toLocaleString('en-US')} rows`,
      caption: (x: string, ys: string) => `Chart of ${ys} by ${x} (the numbers are in the table above)`,
    },
    downloadTruncated: 'A result holding text past the display limit cannot be downloaded here (use the Export tab)',
    saved: 'Saved queries',
    noSaved: 'No saved queries',
    savedOnServer:
      'Saved with this connection account, so the same list appears in any browser. [DB], [SCHEMA], [USER] and [HOST] in a statement are replaced by the values of the place it is loaded into.',
    savedInBrowser:
      'Saved in this browser only; other devices and browsers will not see this list. [DB], [SCHEMA], [USER] and [HOST] in a statement are replaced by the values of the place it is loaded into.',
    shared: 'Shared bookmarks',
    sharedNote:
      'Visible to every account of this server. Only the account that saved one can replace or remove it. [DB], [SCHEMA], [USER] and [HOST] are replaced by the values of the place it is loaded into.',
    saveShared: 'Share the statement in the editor with every account of the server',
    sharedName: 'Shared bookmark name',
    share: 'Share',
    noShared: 'No shared bookmarks.',
    historySearch: 'Filter the history',
    historyFailedOnly: 'Failed runs only',
    historyMatches: (n: number) => `${n.toLocaleString('en-US')} shown`,
    historyOnServer: 'Kept with the account',
    historyInBrowser: 'Kept in this browser only',
    bookmark: 'Bookmark',
    saveQuery: 'Save query',
    savedName: 'Query name',
    save: 'Save',
    deleteSaved: (name: string) => `Delete ${name}`,
    history: 'History',
    historyDb: (db: string) => `on ${db}`,
    noHistory: 'No history',
    clearHistory: 'Clear history',
    debug: 'Debug SQL',
    debugNote: (max: number) =>
      `The SQL this page sent, and how long each took (in this browser tab only, the newest ${max}). Passwords in account statements are masked.`,
    noDebug: 'Nothing has been sent yet.',
    debugMs: (ms: number) => `${ms.toLocaleString('en-US')} ms`,
    debugRerun: 'Run again',
    debugClear: 'Clear the list',
    load: 'Load',
    ok: 'Succeeded',
    failed: 'Failed',
  },
  ddl: {
    tableOptions: {
      packKeys: 'PACK_KEYS (MyISAM)',
      delayKeyWrite: 'DELAY_KEY_WRITE (MyISAM)',
      transactional: 'TRANSACTIONAL (Aria)',
      pageChecksum: 'PAGE_CHECKSUM (Aria)',
      statsPersistent: 'STATS_PERSISTENT (InnoDB)',
      statsAutoRecalc: 'STATS_AUTO_RECALC (InnoDB)',
      defaultValue: 'Server default',
      ariaHint: 'MariaDB’s Aria engine only',
    },
    referenceCheck: {
      title: 'Referential integrity',
      hint: 'For each foreign key, counts the rows that name a parent which is not there. A key with a NULL in it is not checked. It reads the table once per key, so a large table takes a while.',
      button: 'Check referential integrity',
      key: 'Foreign key',
      references: 'References',
      orphans: 'Rows without a parent',
      none: 'None',
      noKeys: 'This table has no foreign keys.',
      open: 'Open in the SQL tab',
      openLabel: (name: string) => `${name}: open the rows without a parent in the SQL tab`,
    },
    titles: {
      splitTable: 'Split the table',
      moveRepeatingGroup: 'Turn repeating columns into rows',
      setServerVariable: 'Change a server setting',
      dropRoutine: 'Drop the routine',
      alterRoutine: "Change the routine's characteristics",
      dropTrigger: 'Drop the trigger',
      setDatabaseCollation: 'Change the default collation',
      maintainTables: 'Maintain the selected tables',
      renameTables: 'Rename the selected tables',
      copyTables: 'Copy the selected tables',
      convertCollation: "Change every column's collation",
      orderTable: 'Reorder the rows',
      partitionTable: 'Partition the table',
      addPartition: 'Add a partition',
      dropPartition: 'Drop a partition',
      truncatePartition: 'Empty a partition',
      detachPartition: 'Detach a partition',
      removePartitioning: 'Remove partitioning',
      maintainPartition: 'Partition maintenance',
      createTable: 'Create table',
      addColumn: 'Add column',
      modifyColumn: 'Change column',
      dropColumn: 'Drop column',
      dropColumns: 'Drop columns',
      modifyColumns: 'Change columns',
      reorderColumns: 'Reorder columns',
      setPrimaryKey: 'Set the primary key',
      renameIndex: 'Rename index',
      alterIndex: 'Change index',
      addIndex: 'Add index',
      dropIndex: 'Drop index',
      addForeignKey: 'Add foreign key',
      dropForeignKey: 'Drop foreign key',
      dropTable: 'Drop the table',
      truncateTable: 'Empty the table',
      renameTable: 'Rename table',
      createDatabase: 'Create database',
      renameDatabase: 'Rename database',
      copyDatabase: 'Copy database',
      dropDatabase: 'Drop the database',
      dropDatabases: 'Drop the selected databases',
      createSchema: 'Create schema',
      copyTable: 'Copy table',
      setTableOptions: 'Table options',
      maintainTable: 'Maintenance',
      dropTables: 'Drop the selected tables',
      dropObjects: 'Drop tables and views',
      truncateTables: 'Empty the selected tables',
      enableEvent: 'Enable event',
      disableEvent: 'Disable event',
      dropEvent: 'Drop the event',
      moveTable: 'Move table',
      replaceInColumn: 'Find and replace',
      createView: 'Create view',
      replaceRoutine: 'Replace routine',
      replaceTrigger: 'Replace trigger',
      replaceEvent: 'Replace event',
      createRoutine: 'Create routine',
      createTrigger: 'Create trigger',
      createEvent: 'Create event',
    },
    previewHint: 'The SQL below will be run. Read it before you continue.',
    nothingToChange: 'Nothing to change (no SQL to run).',
    irreversible: 'This cannot be undone.',
    /** Appended to `irreversible` for the ops that destroy stored data (not for dropping an index / key / account). */
    dataLoss: 'The table data is lost.',
    columnLoss: 'The data in this column is lost.',
    normalizeLoss:
      'The moved columns are dropped from the original table. Their values are copied into the new table first; if a statement fails, the rest do not run.',
    partitionLoss: "This partition's rows are lost.",
    copyReplaceLoss: 'A table of that name where the copy goes is dropped, with its rows.',
    databaseLoss: 'Every table in the database, and its data, is lost.',
    databaseLossForce: 'Other users connected to this database are disconnected as well.',
    typeToConfirm: (name: string) => `Type "${name}" to continue`,
    generating: 'Generating the SQL…',
    execute: 'Run',
    executed: (title: string) => `"${title}" done`,
    failedStatement: 'Failed statement',
    rolledBack: 'The statements that had succeeded were rolled back as well (nothing was applied)',
    serverNotice: 'Server notice',
    columnName: 'Column name',
    dataType: 'Type',
    nullable: 'Allow NULL',
    default: 'Default',
    defaultNone: 'None',
    defaultLiteral: 'Value',
    defaultExpression: 'Expression',
    autoIncrement: 'Auto-increment / identity',
    comment: 'Comment',
    after: 'Place after',
    afterLast: 'End of table',
    index: {
      kind: 'Kind',
      kinds: { index: 'INDEX', unique: 'UNIQUE', fulltext: 'FULLTEXT', spatial: 'SPATIAL' },
      method: 'Method',
      defaultMethod: '(default)',
      lengths: 'Index only the first N characters (empty for the whole value)',
      rename: 'Rename',
      newName: 'New name',
      editOf: (name: string) => `Index ${name}: change`,
      renameOf: (name: string) => `Index ${name}: rename`,
      dropOf: (name: string) => `Index ${name}: drop`,
    },
    proposal: {
      button: 'Propose table structure',
      title: 'Proposed types',
      none: (rows: number) =>
        `Looked at the first ${rows.toLocaleString('en-US')} rows: no text column has a narrower type to propose.`,
      basis: (rows: number) =>
        `Decided from the first ${rows.toLocaleString('en-US')} rows only. A later value that does not fit makes the change fail (no value is lost).`,
      current: 'Current type',
      proposed: 'Proposed',
      preview: 'Review the ticked proposals…',
    },
    bulk: {
      pick: 'Pick',
      selected: (n: number) => `${plural(n, 'column', 'columns')} selected`,
      primary: 'Make primary key',
      index: { unique: 'Unique', index: 'Index', fulltext: 'Full text', spatial: 'Spatial' },
      change: 'Change together…',
      drop: 'Drop together…',
      reorder: 'Reorder columns…',
      changeTitle: (n: number, of: number) => `Change column (${n} of ${of})`,
      order: 'Order',
      up: (name: string) => `Move ${name} up`,
      down: (name: string) => `Move ${name} down`,
    },
    extras: {
      collation: 'Collation',
      collationDefault: '(default)',
      attribute: 'Attribute',
      none: '(none)',
      onUpdate: 'Set CURRENT_TIMESTAMP on update (ON UPDATE)',
      generated: 'Generated column (computed from an expression)',
      expression: 'Expression',
      storage: 'Kept',
      stored: 'STORED (kept)',
      virtual: 'VIRTUAL (computed when read)',
      first: 'First',
      keepPosition: 'Where it is',
      key: 'Key',
    },
    indexName: 'Index name',
    indexColumns: 'Columns',
    fkName: 'Constraint name',
    fkColumns: 'Columns of this table',
    fkRefTable: 'Referenced table',
    fkRefColumns: 'Referenced columns',
    fkOnUpdate: 'ON UPDATE',
    fkOnDelete: 'ON DELETE',
    fkDefaultAction: '(default)',
    fkHint:
      'The referenced columns must be a primary or unique key. MySQL can reference a table in another database, PostgreSQL one in another schema of the same database.',
    fkRefDatabase: 'Referenced database',
    fkRefSchema: 'Referenced schema',
    unique: 'Unique',
    primaryKey: 'Primary key',
    tableName: 'Table name',
    addColumnRow: 'Add column',
    removeColumnRow: 'Remove this column',
    createTableTitle: 'Create table',
    operationsTitle: 'Table operations',
    viewOperationsTitle: 'View operations',
    truncateHint: 'Deletes every row (the structure stays).',
    dropHint: 'Drops the table with its structure. This cannot be undone.',
    bulkLoss: (n: number) => `All data in ${plural(n, 'table', 'tables')} will be lost.`,
    dropViewHint: 'Drops the view definition (the underlying data stays). This cannot be undone.',
    dropViewTitle: 'Drop the view',
    sequenceOperationsTitle: 'Sequence operations',
    dropSequenceHint:
      'Drops the sequence. On MariaDB, inserts into tables whose default calls it will fail afterwards; on PostgreSQL the drop is refused while a column default still depends on it. This cannot be undone.',
    dropSequenceTitle: 'Drop the sequence',
    dropSequenceButton: 'Drop sequence…',
    sequenceLoss: 'The sequence position is lost.',
    newSequenceName: 'New sequence name',
    actions: 'Actions',
    edit: 'Change',
    generatedNotEditable:
      'Generated columns cannot be changed here (the generation expression would be lost). Use ALTER TABLE in the SQL tab.',
    drop: 'Drop',
    submit: 'Next (review the SQL)',
    /** Danger buttons that open the preview dialog: the ellipsis says a confirmation follows. */
    truncateButton: 'Empty table…',
    dropButton: 'Drop table…',
    dropViewButton: 'Drop view…',
    typeSelect: 'Select a type',
    typeGroups: {
      numeric: 'Numeric',
      text: 'Text',
      binary: 'Binary and bit',
      datetime: 'Date and time',
      boolean: 'Boolean',
      json: 'JSON and UUID',
      network: 'Network',
      spatial: 'Spatial and geometric',
      other: 'Other',
    },
    typeArguments: 'Length / values / attributes',
    newTableName: 'New table name',
    newViewName: 'New view name',
    renameTitle: 'Rename',
    copyTargetName: 'Name of the copy',
    copyWithData: 'Include data',
    copyToDatabase: 'Copy to database',
    copyToSchema: 'Copy to schema',
    copyWhat: 'What to copy',
    copyWhatOptions: {
      both: 'Structure and data',
      structure: 'Structure only',
      data: 'Data only (into an existing table of that name)',
    },
    copyDropExisting: 'Drop a table of that name first',
    copyForeignKeys: (n: number) => `Copy the foreign keys too (${n})`,
    copyHint: 'Indexes and the primary key are copied. Foreign keys are not.',
    renameHint:
      'Views, foreign keys and privileges may not follow the new name. On PostgreSQL, indexes and sequences keep their own names.',
    renameSequenceHint:
      'On PostgreSQL, column defaults that call this sequence (nextval) follow the new name and its privileges are kept. On MariaDB they do not: inserts into tables whose default calls the old name fail until the default is changed.',
    databaseName: 'Database name',
    schemaName: 'Schema name',
    engineHint: 'InnoDB, for instance (empty leaves it unchanged).',
    collationHint: 'utf8mb4_0900_ai_ci, for instance. Existing columns are not converted.',
    autoIncrementNext: 'Next AUTO_INCREMENT value',
    autoIncrementHint: 'A value below the current maximum is ignored.',
    unchanged: 'unchanged',
    maintenanceTitle: 'Maintenance',
    checksumOption: 'Keep a live checksum (CHECKSUM)',
    checksumOptionHint: 'MyISAM / Aria update a checksum as rows change',
    replaceWarning:
      'Rewrites the value in every matching row, in place. It cannot be undone: export the table first if you may need it back.',
    moveTo: { mysql: 'Move to database', postgres: 'Move to schema' },
    moveHint: {
      mysql:
        'Moves the table, rows and all, under the same name (RENAME TABLE). MySQL cannot move a table with triggers to another database.',
      postgres:
        'Moves the table under the same name to another schema of this database. PostgreSQL cannot move a table to another database.',
    },
    maintenanceHint: {
      mysql:
        'ANALYZE refreshes statistics, OPTIMIZE rebuilds the storage (locking), CHECK verifies integrity, REPAIR mends a damaged table (MyISAM and the like; InnoDB is not covered, and the result says so). CHECKSUM computes a checksum of the contents; FLUSH closes the table and writes it to disk (needs the RELOAD privilege).',
      postgres:
        'ANALYZE refreshes statistics, VACUUM reclaims dead space, VACUUM FULL rewrites the table (exclusive lock).',
    },
    maintenance: {
      mysql: {
        analyze: 'ANALYZE TABLE',
        optimize: 'OPTIMIZE TABLE',
        check: 'CHECK TABLE',
        repair: 'REPAIR TABLE',
        vacuum: 'VACUUM',
        checksum: 'CHECKSUM TABLE',
        flush: 'FLUSH TABLE',
      },
      postgres: {
        analyze: 'ANALYZE',
        vacuum: 'VACUUM',
        optimize: 'VACUUM FULL',
        check: 'CHECK',
        repair: 'REPAIR',
        checksum: 'CHECKSUM',
        flush: 'FLUSH',
      },
    },
    bulkSelected: (n: number) => `${plural(n, 'table', 'tables')} selected`,
    bulkTruncate: 'Empty the selected tables…',
    bulkDrop: 'Drop the selected tables…',
    bulkExport: 'Export the selected tables',
    selectTable: (name: string) => `Select ${name}`,
  },
  export: {
    templates: {
      title: 'Export templates',
      name: 'Template name',
      saveHint: 'Saves the current choices under a name',
      empty: 'No templates for this database',
      deleteLabel: (name: string) => `Delete the template ${name}`,
      wholeDatabase: 'whole database',
      tableCount: (n: number) => `${n.toLocaleString('en-US')} tables`,
      structureOnly: 'structure only',
      dataOnly: 'data only',
      missing: (tables: string) => `Tables in the template are gone: ${tables} (left out of the selection)`,
    },
    title: 'Export',
    tables: 'Tables',
    allTables: 'all tables',
    format: 'Format',
    formats: {
      sql: 'SQL',
      csv: 'CSV',
      csvExcel: 'CSV for Excel',
      json: 'JSON',
      xml: 'XML',
      yaml: 'YAML',
      markdown: 'Markdown (table)',
      latex: 'LaTeX',
      texy: 'Texy!',
      mediawiki: 'MediaWiki',
      html: 'HTML (for printing / PDF)',
      ods: 'OpenDocument Spreadsheet (ODS)',
      odt: 'OpenDocument Text (ODT)',
      docx: 'Word (DOCX)',
    },
    csvExcelHint: 'Semicolon-separated with a UTF-8 byte-order mark (the form Excel reads correctly).',
    documentHint:
      'A table to paste into a document. A binary value shows only its size and NULL is written as NULL, so this cannot be imported.',
    htmlHint:
      'An HTML page made for printing. Open it in the browser and print with “Save as PDF” as the destination to get a PDF (fonts for Japanese and other scripts cannot be embedded, so a PDF is not written directly).',
    officeHint:
      'A file for reading and sharing. NULL is an empty cell and a binary value shows only its size, so it cannot be imported. A control character XML cannot carry becomes a replacement character.',
    ifNotExists: 'Use CREATE TABLE IF NOT EXISTS',
    viewsAsTables: 'Write views as tables (their columns and rows)',
    createDatabaseMysql: 'Add CREATE DATABASE IF NOT EXISTS and USE',
    createDatabasePostgres: 'Add CREATE SCHEMA IF NOT EXISTS',
    comments: 'Add comments and headings',
    transaction: 'Wrap everything in one transaction',
    utc: 'Write times as UTC (and set the session to UTC)',
    lockTables: 'Wrap each table in LOCK TABLES … WRITE (MySQL)',
    rowsAs: 'How the data is written',
    statement: 'Statement',
    statements: {
      insert: 'INSERT',
      update: 'UPDATE (by primary key)',
      replace: 'REPLACE (PostgreSQL: ON CONFLICT … DO UPDATE)',
    },
    columnNames: 'Name the columns in every INSERT',
    extended: 'Several rows to an INSERT',
    ignoreMysql: 'Use INSERT IGNORE',
    ignorePostgres: 'Add ON CONFLICT DO NOTHING',
    maxQuery: 'Longest statement (bytes)',
    maxQueryHint: '0 is no limit (one statement per batch of rows)',
    needsKey: 'UPDATE and REPLACE need a primary key on every table. Binary values are always written in hexadecimal.',
    rowOffset: 'Rows to skip',
    rowLimit: 'Rows to write',
    rowLimitHint: '0 is to the end. Counted from the start of each table',
    compress: 'Compression',
    compressions: { none: 'None', gzip: 'gzip (.gz)', zip: 'zip (.zip)' },
    filePerTable: 'One file per table (in a zip)',
    charset: 'Character set',
    charsets: {
      'utf-8': 'UTF-8',
      cp932: 'Shift_JIS (CP932)',
      'euc-jp': 'EUC-JP',
      'iso-8859-1': 'ISO-8859-1',
      'windows-1252': 'Windows-1252',
      gbk: 'GBK (Simplified Chinese)',
      big5: 'Big5 (Traditional Chinese)',
      'euc-kr': 'EUC-KR',
    },
    filename: 'File name template',
    filenameHint: '@DATABASE@ @TABLE@ @SERVER@ and the date parts %Y %m %d %H %M %S. The extension is added.',
    csvDelimiter: 'Delimiter',
    csvDelimiters: { comma: 'Comma (,)', semicolon: 'Semicolon (;) for Excel', tab: 'Tab' },
    markdownHint:
      'Markdown is a table to paste into documents. Binary values are written as their size only, so it cannot be imported back.',
    structure: 'Structure (CREATE TABLE)',
    dropTable: 'Add DROP TABLE IF EXISTS',
    data: 'Data (INSERT)',
    bom: 'Add a UTF-8 BOM (for Excel)',
    documentStructure: 'Structure (the list of columns)',
    documentData: 'Data (the rows)',
    documentPartsHint: 'With both, each table is written as a structure table followed by a data table.',
    noParts: 'Choose the structure, the data, or both',
    xmlObjects: 'Also include these definitions in the XML',
    xmlViews: 'Views',
    xmlRoutines: 'Routines (procedures and functions)',
    xmlTriggers: 'Triggers (of the tables exported)',
    latexCaption: 'Put a caption over each table',
    latexLabel: 'Add a label to refer to it by (\\ref gives the table number; needs the caption)',
    jsonCompact: 'Pack it into one line (no spaces or line breaks)',
    odsNull: 'How NULL is written (empty: an empty cell)',
    csvHeader: 'Write the column names as the first line',
    csvQuoteAll: 'Put every value in quotes (NULL stays \\N)',
    csvStripEol: 'Write line breaks inside a value as spaces (a record is one line; the value itself changes)',
    csvSafe: 'Stop spreadsheets from running values as formulas',
    csvSafeHint:
      'Prefixes values starting with = + - @ with an apostrophe. This changes the value, so leave it off if the file will be imported back.',
    routines: 'Include routines, triggers and events',
    triggersOnly: 'Include the triggers of these tables (routines and events come with a whole-database export)',
    stripDefiner:
      'Strip the DEFINER clause (to restore as another user; definer-rights objects then run with the restoring user’s privileges)',
    download: 'Download',
    csvSingle: 'CSV exports one table at a time.',
    nothing: 'Nothing to export',
    selectionTooLong:
      'Too many tables are selected to fit in the URL. Clearing the selection exports the whole database.',
    selectAll: 'Select all',
    selectNone: 'Clear selection',
    snapshotNote: 'A table being written to may not export as a consistent snapshot.',
    server: {
      title: 'Export the whole server',
      hint: 'Writes the databases you choose into one SQL file. Each has its own CREATE DATABASE and USE, and routines and events are included.',
      databases: 'Databases',
      schemas: 'Schemas',
      filePerTarget: 'One file per database (in a zip)',
      filePerSchema: 'One file per schema (in a zip)',
      nothing: 'Choose at least one database to export',
    },
  },
  import: {
    title: 'Import',
    file: 'File',
    fileHint: (mb: number) =>
      `.sql .csv .ods .xml .txt, also gzip or zip compressed (up to ${mb} MB, unpacked included). You can drop the file here`,
    format: 'Format',
    formats: { sql: 'SQL', csv: 'CSV', ods: 'ODS (spreadsheet)', xml: 'XML', mediawiki: 'MediaWiki table' },
    targetTable: 'Target table',
    header: 'The first row holds the column names',
    nullMarker: 'NULL marker',
    delimiter: 'Delimiter',
    stopOnError: 'Stop on error',
    ignoreForeignKeys: 'Disable foreign key checks (load the tables in any order)',
    singleTransaction:
      'Run in a single transaction (on error everything is rolled back; faster for many single-row INSERTs)',
    submit: 'Import',
    running: 'Importing…',
    cancel: 'Stop',
    cancelling: 'Stopping…',
    resultRegion: 'Import result',
    cancelled: 'Stopped. What had run so far is summarised below (statements inside the transaction were rolled back).',
    progress: (done: number, total: number) =>
      `Ran ${done.toLocaleString('en-US')} of ${plural(total, 'statement', 'statements')}`,
    sqlResult: (ok: number, failed: number, ms: number) =>
      `${plural(ok, 'statement', 'statements')} succeeded, ${failed.toLocaleString('en-US')} failed (${ms.toLocaleString('en-US')} ms)`,
    skipped: (n: number) => `${plural(n, 'statement', 'statements')} did not run`,
    rowsResult: (n: number, table: string, ms: number) =>
      `Inserted ${plural(n, 'row', 'rows')} into ${table} (${ms.toLocaleString('en-US')} ms)`,
    skippedColumns: (cols: string) => `Columns the server computes were left out of the import: ${cols}`,
    warnings: {
      CHANGED_DATABASE:
        'The script switched, created or dropped a database: rows may have been written outside the target database',
      ROLLED_BACK:
        'A transaction the script left open was rolled back (some statements counted as successful were not applied)',
      ALL_ROLLED_BACK: 'An error rolled the whole file back (nothing was applied)',
      CANCELLED: 'The run was stopped: the remaining statements did not run',
      PARTIALLY_ROLLED_BACK:
        'An error rolled back everything after the last commit. The script committed part-way (its own COMMIT, or on MySQL a CREATE / DROP / ALTER, which commit implicitly), so the statements before that point stay applied',
    } satisfies Record<ImportWarning, string>,
    errorAt: (line: number, index: number) =>
      `line ${line.toLocaleString('en-US')} (statement #${(index + 1).toLocaleString('en-US')})`,
    notes: {
      sql: 'Choose the character set below (UTF-8 by default). Run mysqldump with --hex-blob; both the plain (COPY) and --inserts forms of pg_dump are read. On MySQL, CREATE / DROP / ALTER commit implicitly, so "Run in a single transaction" cannot roll back what ran before one. Closing the page stops the run.',
      csv: 'Choose the character set below. Every row is loaded in one transaction and an error rolls the whole file back (it cannot be stopped once running). An empty field is the empty string; only an unquoted value equal to the NULL marker becomes NULL. Header names are matched case-insensitively.',
      ods: 'An OpenDocument spreadsheet (LibreOffice and Excel can write one). The first row is read as the column names, and an empty cell is NULL. A formula is read as its calculated value. Every row is loaded in one transaction.',
      xml: 'XML written by this tool or by phpMyAdmin (a run of <column name="…"> elements; null="true" is NULL). Every row is loaded in one transaction.',
      mediawiki:
        'A MediaWiki table ({| … |}). The heading row is read as the column names. Every row is loaded in one transaction.',
    },
    errors: 'Errors',
    csvNeedsTable: 'Choose the table to import into',
    viewRows: 'View imported rows',
    fileTooLarge: (mb: number) => `The file is too large (up to ${mb} MB)`,
    charset: 'Character set',
    lineEnd: 'Line ending',
    lineEndHint:
      '"Auto" takes any line break as the end of a record. Choose one and only that ends a record; the others are part of the value.',
    lineEnds: { auto: 'Auto', lf: 'LF (\\n)', crlf: 'CRLF (\\r\\n)', cr: 'CR (\\r)' },
    skipBlank: 'Do not count blank lines as rows (off: they are loaded as rows with no values)',
    columns: 'Column mapping (the columns the fields go into, left to right, comma-separated)',
    columnsHint:
      "A blank leaves that field out. Given, it replaces the names in the file's header row. Not used when a new table is made.",
    odsPercent: 'Read percentages as',
    odsPercents: { fraction: 'a fraction (25% as 0.25)', text: 'the text shown (25%)' },
    odsCurrency: 'Read currency amounts as',
    odsCurrencies: { number: 'a number ($5.00 as 5)', text: 'the text shown ($5.00)' },
    odsDate: 'Read dates as',
    odsDates: { iso: 'the value the sheet holds (2024-01-02)', text: 'the text shown' },
    skip: 'Leave out from the start',
    skipHint: 'Statements for SQL, rows for the rest. Use it to go on where an earlier run stopped',
    onDuplicate: 'A row whose primary or unique key exists',
    onDuplicates: { error: 'Fail (roll everything back)', ignore: 'Leave it out', replace: 'Replace the row' },
    createTable: 'Create the table from the file, then load it',
    createTableHint:
      'Type the new table name below. Column types are inferred from the values (edit them later on Structure).',
    newTable: 'New table name',
    enclosure: 'Enclosure',
    escape: 'Escape character',
    sheet: 'Sheet / table (name or number)',
    sheetHint: 'Empty means the first one',
    noAutoValueOnZero: 'Keep a 0 in an AUTO_INCREMENT column (NO_AUTO_VALUE_ON_ZERO, MySQL)',
    rowWarnings: (n: number) =>
      `${plural(n, 'thing', 'things')} the server let pass as a warning while leaving out duplicates`,
    skippedRows: (n: number) => `${plural(n, 'row', 'rows')} left out (the setting for leaving out from the start)`,
    created: (cols: string) => `Created the table (${cols})`,
    server: {
      title: 'Import at the server level',
      hint: 'Runs a script that holds CREATE DATABASE / USE (such as a whole-server export) from a connection that belongs to no database.',
    },
  },
  settings: {
    title: 'Settings',
    intro:
      'What you choose here is kept in this browser. Where sessions are persistent it is also kept with the account you are connected as, and follows you to another browser. The language and the theme are switched at the top of the page.',
    forThisPage: 'Settings for this page',
    sections: {
      mainPanel: 'Main panel',
      sql: 'SQL',
      navigation: 'Navigation',
      exportDefaults: 'Export defaults',
      importDefaults: 'Import defaults',
      file: 'Settings file',
    },
    browseLimit: 'Rows per page of a table',
    browseUnlimited: 'Offer “Show all rows” on a table (reading a large table whole is slow)',
    headerEvery: 'Repeat the column headings every N rows (0: never)',
    headerEveryHint: 'On a long page, the headings are inserted again after this many rows.',
    insertRowCount: 'Rows in the insert form',
    gridEdit: 'Editing a cell (Browse tab)',
    gridEdits: { doubleClick: 'With a double click', click: 'With a click', off: 'Never (use the pencil)' },
    saveOnBlur: 'Save a cell when it loses focus',
    confirmDrop: 'Ask for the name again before a drop or truncate',
    defaultServerTab: 'Tab to open after connecting',
    serverTabs: {
      databases: 'Databases',
      sql: 'SQL',
      status: 'Status',
      variables: 'Variables',
      processes: 'Processes',
      users: 'Users',
    },
    defaultDbTab: 'Tab to open on a database',
    dbTabs: { structure: 'Structure', sql: 'SQL', search: 'Search', query: 'Query' },
    defaultTableTab: 'Tab to open on a table',
    tableTabs: { browse: 'Browse', structure: 'Structure', search: 'Search', insert: 'Insert' },
    sqlEnterRuns: 'Enter runs the SQL in the editor (Shift + Enter for a new line)',
    navWidth: 'Sidebar width (px)',
    navShowRoutines: 'Show routines and events under a database in the sidebar',
    rangeHint: (min: number, max: number) => `${min.toLocaleString('en-US')} to ${max.toLocaleString('en-US')}`,
    sqlSafeMode: 'Safe mode (ask before an UPDATE / DELETE without a WHERE runs)',
    consoleDocked: 'Keep the SQL console at the foot of the page',
    sqlHistoryMax: 'Statements kept in the SQL history',
    sqlHistoryMaxHint: 'Kept per server, in this browser.',
    navGroupDelimiter: 'Separator that groups table names',
    navGroupDelimiterHint:
      'With "_", shop_orders and shop_items are grouped under "shop" in the sidebar. Empty means no grouping.',
    navPageSize: 'Items the sidebar shows at once',
    navPageSizeHint: '0 shows everything at once. The rest come with "Show more".',
    navHidden: 'Databases the sidebar leaves out',
    navHiddenHint: "They can still be opened from the server's list of databases.",
    importFormat: 'Import format to edit',
    save: 'Save',
    saved: 'Saved.',
    reset: 'Restore defaults',
    resetTitle: 'Restore the default settings',
    resetBody:
      'Puts every setting on this page back to how it was before you chose any. The language and the theme stay.',
    resetExecute: 'Restore defaults',
    download: 'Save settings to a file',
    upload: 'Load a settings file',
    fileHint: 'A JSON file, to bring the same settings to another browser or another server.',
    loaded: 'Loaded. Press Save to apply it.',
    badFile: 'That is not a settings file',
    invalid: 'A value is out of range. Check the range of each field.',
  },
  users: {
    databases: {
      title: 'Privileges per database',
      none: 'No privileges on any database or table',
      database: 'Database',
      object: 'Applies to',
      privileges: 'Privileges',
      grantOption: 'GRANT OPTION',
      edit: 'Edit',
      revoke: 'Revoke…',
      kind: { database: '(whole database)', schema: '(whole schema)', table: 'table', routine: 'routine' },
      postgresNote: (db: string) =>
        `PostgreSQL keeps privileges per database, so only the connected database, ${db}, is listed.`,
    },
    bulk: {
      selected: (n: number) => `${plural(n, 'user', 'users')} selected`,
      selectAll: 'Select all users',
      select: (name: string) => `Select ${name}`,
      drop: 'Drop the selected users…',
      title: 'Drop the selected users',
      revokeFirst: 'Revoke privileges first (on PostgreSQL: hand what they own to you and drop their grants)',
      sameNameDatabases: 'Also drop the databases with the same names (MySQL; system databases are never dropped)',
      note: 'A connection the dropped account has open already stays usable until it is closed.',
    },
    secondFactor: {
      badge: 'Two-factor',
      reset: 'Reset two-factor…',
      resetTitle: 'Reset two-factor',
      resetExecute: 'Reset',
      resetConfirm: (user: string) =>
        `Removes the second factor of ${user}. This is for someone who has lost both the device and the recovery codes: they can connect with the password alone from now on, and have to enrol again themselves. On MySQL it applies to every host row of this user name.`,
    },
    title: 'User accounts',
    name: 'Username',
    host: 'Host',
    anyHost: 'Any host (%)',
    localHost: 'Local (localhost)',
    thisHost: (host: string) => `This host (${host})`,
    customHost: 'Specify a host',
    hostPattern: 'A name or a pattern (for example 192.168.1.%)',
    plugin: 'Authentication plugin',
    pluginDefault: '(the server’s default)',
    createDatabase: 'Create a database of the same name and grant all privileges on it',
    grantWildcard: 'Grant all privileges on every database starting with name_ (wildcard)',
    generatePassword: 'Generate a password',
    generatedPassword: 'The generated password',
    account: {
      lock: 'Lock',
      unlock: 'Unlock',
      rename: 'Rename',
      copy: 'Copy',
      limits: 'Limits',
      globalPrivileges: 'Global privileges',
      roleAttributes: 'Role attributes',
      exportGrants: 'Download the privileges as SQL',
      newName: 'New name',
      renameHintMysql:
        'RENAME USER changes the name and host. The privileges go with it, but applications that connect under the old name can no longer connect.',
      renameHintPostgres:
        'ALTER ROLE … RENAME TO changes the name. The privileges go with it, but an MD5 password is cleared by the rename: set the password again afterwards.',
      copyHintMysql:
        'Makes a new account holding the same privileges (the SHOW GRANTS statements). A password cannot be copied: choose a new one.',
      copyHintPostgres:
        'Makes a new role with the same attributes, group memberships and privileges in the connected database. Choose a new password. Privileges in other databases are not copied.',
      limitsHintMysql: 'Limits per hour; 0 is no limit. SSL / X509 accept encrypted connections only.',
      limitsHintPostgres: 'The most connections the role may hold at once; 0 is no limit.',
      require: 'Connection requirement',
      requireOptions: { NONE: 'None', SSL: 'SSL', X509: 'X509 (a valid certificate)' },
      maxQueries: 'Queries per hour',
      maxUpdates: 'Updates per hour',
      maxConnections: 'Connections per hour',
      maxUserConnections: 'Simultaneous connections',
      globalHint: 'Privileges on the whole server (*.*). Only what you change is written, as GRANT / REVOKE.',
      roleHint: 'The attributes of CREATE ROLE / ALTER ROLE. Only what you change is rewritten with ALTER ROLE.',
    },
    login: 'Can log in',
    attributes: 'Attributes',
    grants: 'Privileges',
    showGrants: 'Show privileges',
    noGrants: 'No privileges',
    create: 'Create user',
    password: 'Password',
    passwordConfirm: 'Confirm password',
    passwordMismatch: 'The passwords do not match',
    superuser: 'All privileges (SUPERUSER / ALL PRIVILEGES WITH GRANT OPTION)',
    createdb: 'May create databases (CREATEDB / CREATE)',
    createrole: 'May create users (CREATEROLE / CREATE USER)',
    changePassword: 'Change password',
    drop: 'Drop',
    choose: 'Choose privileges…',
    choosePrivileges: (user: string) => `Privileges for ${user}`,
    privileges: 'Privileges',
    privilegeTarget: 'Applies to',
    wholeDatabase: 'The whole database',
    wholeTableOnly: '(whole table only)',
    privilegeColumns: 'Limit to columns (all of them when none are chosen)',
    privilegeColumnsHint:
      'Choosing columns grants the privilege on those columns alone. Revoking removes only a column-level privilege — it does not narrow a privilege held on the whole table, which always covers every column.',
    columnProblem: {
      needsTable: 'Choose the table those columns belong to as well.',
      notColumnPrivilege: 'DELETE and TRIGGER apply to the whole table: clear them to name columns.',
    },
    grantOption: 'WITH GRANT OPTION (the account may pass these privileges on)',
    grantOptionHint:
      'On grant, adds WITH GRANT OPTION. On revoke, keeps the privileges and removes only the grant option.',
    privilegesNote:
      'Only privileges that mean the same thing on both servers are listed. Anything else (MySQL INDEX, PostgreSQL TRUNCATE) goes through the SQL tab.',
    grantAll: 'Grant all privileges on this database',
    revokeAll: 'Revoke all privileges on this database',
    privilegesTitle: (db: string, schema?: string) => `Privileges on ${db}${schema ? `.${schema}` : ''}`,
    currentPrivileges: 'Current privileges',
    levels: { all: 'All', some: 'Some', none: 'None' },
    globalGrant: 'Global',
    globalGrantHint: 'A server-wide privilege (*.*): revoking it on this database does not remove it',
    privilegesHint: 'Grant or revoke all privileges on this database (on PostgreSQL, the current schema) per user.',
    ops: {
      createUser: 'Create the user',
      lockUser: 'Lock or unlock the account',
      renameUser: 'Rename the account',
      copyUser: 'Copy the account',
      setAccountLimits: 'Resource limits and connection requirements',
      changeGlobalPrivileges: 'Change global privileges',
      alterRole: 'Change the role’s attributes',
      grantRoutinePrivileges: 'Grant privileges on a routine',
      revokeRoutinePrivileges: 'Revoke privileges on a routine',
      dropUser: 'Drop the user',
      dropUsers: 'Drop the selected users',
      setPassword: 'Change password',
      grantAll: 'Grant all privileges',
      revokeAll: 'Revoke all privileges',
      grantPrivileges: 'Grant privileges',
      revokePrivileges: 'Revoke privileges',
    },
    previewHint: 'The SQL below will be run (the password is shown as ****).',
    ownPasswordHint:
      'The SQL below will be run (the password is shown as ****). Changing your own password disconnects you: connect again with the new one.',
    dropHint:
      'PostgreSQL refuses to drop a role that still holds privileges. Revoke them first on each database’s Privileges tab.',
    selfWarning: 'This is the account you are connected as: running this may disconnect you or lock you out.',
    cannotLoad:
      'The user list cannot be read. On MySQL this needs SELECT on mysql.user (or the CREATE USER privilege).',
  },
  tablePrivileges: {
    title: (table: string) => `Privileges on ${table}`,
    hintMysql:
      'For each account: what it may do to this table and where that comes from (the table, some columns, the whole database, the whole server). Choose privileges… grants and revokes, but a revoke here only removes table and column grants. Manage database-wide grants on the database’s Privileges tab and server-wide ones under Users. Privileges held through a role are not shown.',
    hintPostgres:
      'For each account: what it may do to this table and where that comes from (the table, some columns, superuser). Choose privileges… grants and revokes. The owner’s implicit privileges, and privileges held through PUBLIC or another role, are not shown.',
    scopes: { server: 'Server', database: 'Database', table: 'Table' },
    scopeHints: {
      server: 'Granted on the whole server (MySQL *.*, a PostgreSQL superuser); revoking here does not remove it',
      database: 'Granted on the whole database; revoking here does not remove it',
      table: 'Granted on this table',
      columns: 'Granted on the listed columns only',
    },
    columns: (columns: string) => `Columns: ${columns}`,
  },
  advisor: {
    title: 'Advisor',
    intro:
      'Points worth a look, read from the server’s own counters and settings. Counters are totals since start, so look after some uptime. Nothing is changed.',
    none: 'Nothing stands out.',
    levels: { warn: 'Warning', info: 'Note' },
    fix: 'What to do',
    flagged: 'Warning',
    rules: {
      uptimeShort: {
        title: 'The server has only just started',
        detail:
          'It has been up {hours} hours. With so few events the advice below is only a guide; look again after a day.',
        fix: '',
      },
      slowLogOff: {
        title: 'The slow query log is off',
        detail: 'slow_query_log is OFF (long_query_time = {time} s). Slow queries cannot be looked at afterwards.',
        fix: 'Turn it on with SET GLOBAL slow_query_log = ON (and in the configuration file to keep it after a restart).',
      },
      slowQueries: {
        title: 'A high share of queries are slow',
        detail: '{ratio} of all queries took longer than long_query_time.',
        fix: 'Read the slow query log and use EXPLAIN to look for missing indexes.',
      },
      threadCache: {
        title: 'Threads are created too often',
        detail: '{ratio} of connections create a new thread (thread_cache_size = {cache}).',
        fix: 'Raise thread_cache_size.',
      },
      maxConnections: {
        title: 'Connections are close to the limit',
        detail: 'The most connections at once, {used}, is 80% or more of max_connections ({max}).',
        fix: 'Raise max_connections, or keep the application’s connections down with a pool.',
      },
      abortedConnects: {
        title: 'Many connection attempts fail',
        detail: '{ratio} of connection attempts failed before they were established.',
        fix: 'Check the credentials, the host privileges and the network.',
      },
      bufferPool: {
        title: 'The InnoDB buffer pool may be too small',
        detail: '{ratio} of reads had to go to disk.',
        fix: 'Raise innodb_buffer_pool_size to suit the data and the memory.',
      },
      tmpDisk: {
        title: 'Temporary tables are written to disk',
        detail: '{ratio} of temporary tables were created on disk.',
        fix: 'Raise tmp_table_size and max_heap_table_size, or look at queries that return large results.',
      },
      fullJoin: {
        title: 'Some joins use no index',
        detail: '{count} joins ran without an index.',
        fix: 'Index the columns the joins use.',
      },
      fullScan: {
        title: 'Many queries read the whole table',
        detail: '{ratio} of SELECTs read the whole table.',
        fix: 'Index the columns used in WHERE and ORDER BY.',
      },
      tableOpenCache: {
        title: 'Tables are reopened too often',
        detail: 'Tables are reopened {perHour} times an hour (table_open_cache = {cache}).',
        fix: 'Raise table_open_cache.',
      },
      lockWaits: {
        title: 'Table locks are often waited for',
        detail: '{ratio} of lock requests had to wait.',
        fix: 'Move MyISAM tables to InnoDB, or shorten long transactions.',
      },
      durability: {
        title: 'Commit durability is relaxed',
        detail: 'innodb_flush_log_at_trx_commit = {value}. A power loss can lose the latest updates.',
        fix: 'Set it to 1 for data that must not be lost.',
      },
      filePerTable: {
        title: 'innodb_file_per_table is off',
        detail: 'Every table lives in the shared tablespace.',
        fix: 'Turn it on to be able to give space back per table.',
      },
      pgCacheHit: {
        title: 'The cache hit ratio is low',
        detail: '{ratio} of reads came from memory.',
        fix: 'Raise shared_buffers, or look at the server’s memory.',
      },
      pgDeadlocks: {
        title: 'Deadlocks have happened',
        detail: '{count} in this database.',
        fix: 'Touch tables and rows in the same order in every transaction.',
      },
      pgTempBytes: {
        title: 'Temporary files are used',
        detail: 'Sorts or hashes did not fit in memory and {bytes} bytes were written out (work_mem = {workMem}).',
        fix: 'Raise work_mem (it is used once per operation per connection).',
      },
      pgConnections: {
        title: 'Connections are close to the limit',
        detail: '{used} connections is 80% or more of max_connections ({max}).',
        fix: 'Use a pooler such as pgBouncer, or review max_connections.',
      },
      pgRollbacks: {
        title: 'Many transactions roll back',
        detail: '{ratio} of transactions were rolled back.',
        fix: 'Check the application’s error handling and constraint violations.',
      },
      pgFsyncOff: {
        title: 'fsync is off',
        detail: 'A crash can corrupt the data.',
        fix: 'Turn it back on.',
      },
      pgAutovacuumOff: {
        title: 'Autovacuum is off',
        detail: 'Dead rows pile up and tables bloat.',
        fix: 'Turn it back on.',
      },
      pgSharedBuffers: {
        title: 'shared_buffers is small',
        detail: 'It is {size}, close to the default.',
        fix: 'Raise it towards about 25% of memory.',
      },
    },
  },
  monitor: {
    title: 'Real-time monitor',
    every: 'Refresh',
    pause: 'Pause',
    resume: 'Resume',
    clear: 'Clear',
    waiting: 'Changes are drawn from the second reading on.',
    hintMysql:
      'Reads the status variables at an interval and draws the running totals as a change per second. Kept only while you stay on this page.',
    hintPostgres:
      'Reads the connected database’s statistics (pg_stat_database) at an interval and draws the running totals as a change per second. Kept only while you stay on this page.',
    perSecond: (v: number) => `${v.toLocaleString('en-US', { maximumFractionDigits: 1 })}/s`,
    charts: {
      queries: 'Queries per second',
      connections: 'Connections',
      traffic: 'Traffic per second',
      transactions: 'Transactions per second',
      rows: 'Rows handled per second',
    },
    series: {
      questions: 'Queries',
      connected: 'Connected',
      running: 'Running',
      received: 'Received',
      sent: 'Sent',
      commits: 'Commits',
      rollbacks: 'Rollbacks',
      returned: 'Read',
      inserted: 'Inserted',
      updated: 'Updated',
      deleted: 'Deleted',
      total: 'Connected',
      active: 'Running',
    },
  },
  diagnostics: {
    empty: 'Nothing recorded yet.',
    columns: {
      time: 'Time',
      statement: 'Statement',
      runs: 'Runs',
      totalSeconds: 'Total (s)',
      maxSeconds: 'Longest (s)',
      rowsExamined: 'Rows examined',
      rows: 'Rows returned',
      logName: 'Log',
      position: 'Position',
      eventType: 'Event',
      serverId: 'Server ID',
      endPosition: 'End position',
      info: 'Info',
    },
    status: {
      slowLog: {
        ok: '',
        disabled: 'The slow query log is off (slow_query_log = OFF). Turn it on and slow queries are grouped here.',
        notTable: 'The slow query log goes to a file. Put TABLE in log_output to have it grouped here.',
        denied: 'You may not read mysql.slow_log.',
        noExtension: '',
        unsupported: 'This is a MySQL / MariaDB feature.',
      },
      generalLog: {
        ok: '',
        disabled:
          'The general log is off (general_log = OFF). Turn it on and the statements that run are grouped here.',
        notTable: 'The general log goes to a file. Put TABLE in log_output to have it grouped here.',
        denied: 'You may not read mysql.general_log.',
        noExtension: '',
        unsupported: 'This is a MySQL / MariaDB feature.',
      },
      recentStatements: {
        ok: '',
        disabled: 'The general log is off (general_log = OFF). Turn it on and the statements that run stream in here.',
        notTable: 'The general log goes to a file. Set log_output to TABLE and they stream in here.',
        denied: 'You may not read mysql.general_log.',
        noExtension:
          'The pg_stat_statements extension is not available. With Docker, add -c shared_preload_libraries=pg_stat_statements to the container command, restart, and run CREATE EXTENSION pg_stat_statements.',
        unsupported: '',
      },
      statements: {
        ok: '',
        disabled: '',
        notTable: '',
        denied: 'You may not read pg_stat_statements.',
        noExtension:
          'The pg_stat_statements extension is not available. Add it to shared_preload_libraries, restart and run CREATE EXTENSION pg_stat_statements to see the costly statements here.',
        unsupported: 'This is a PostgreSQL feature.',
      },
      engineStatus: {
        ok: '',
        disabled: '',
        notTable: '',
        denied: 'Reading the InnoDB status needs the PROCESS privilege.',
        noExtension: '',
        unsupported: 'InnoDB is a MySQL / MariaDB storage engine.',
      },
      binlogEvents: {
        ok: '',
        disabled: 'The binary log is off.',
        notTable: '',
        denied: 'You may not read the binary log events (REPLICATION SLAVE is needed).',
        noExtension: '',
        unsupported: 'Binary logs are a MySQL / MariaDB feature.',
      },
    },
    recent: {
      title: 'Statements as they run',
      hintMysql:
        "Streams what the application ran from the general log (a table), newest first. This tool's own statements are left out. The log is a server-wide setting and grows fast: turn it off when you are done.",
      hintPostgres:
        "Shows the statements whose call count grew in pg_stat_statements, per read (the counts of each interval, not one line per execution). This tool's own statements and catalog reads are left out.",
      every: 'Read interval',
      waiting: 'Nothing has run yet. Use the application and it shows up here.',
      turnOn: 'Turn the general log on…',
      turnOff: 'Turn the general log off…',
      onHint:
        'A server-wide setting. While it is on, every statement that runs is stored in mysql.general_log. You review the SQL before it runs.',
      offHint: 'A server-wide setting. What was logged stays. You review the SQL before it runs.',
      turnedOn: 'The general log is on.',
      turnedOff: 'The general log is off.',
    },
    logsTitle: 'Recorded statements',
    slowLog: 'Slow query log',
    generalLog: 'General log',
    statements: 'Costly statements (pg_stat_statements)',
    innodb: 'InnoDB status',
    events: 'Events',
    showEvents: 'Show events',
    eventsOf: (name: string) => `Events of ${name} (first 200)`,
  },
  replication: {
    controls: {
      source: {
        title: 'Is this server ready to be a source?',
        hint: {
          mysql:
            'Checks the variables a replica needs: a non-zero server_id, log_bin ON (binlog_format ROW, and gtid_mode ON if you use GTIDs). Change them under Variables.',
          postgres:
            'Checks the settings a standby needs: wal_level replica or logical, and non-zero max_wal_senders and max_replication_slots. Changing them needs a restart.',
        },
        state: 'State',
        ready: 'OK',
        check: 'Check',
        createUser: 'Create a replica user…',
        userHint: {
          mysql: 'Creates the account a replica connects with and grants it REPLICATION SLAVE.',
          postgres:
            'Creates the role a standby connects with (LOGIN REPLICATION). pg_hba.conf needs a replication line for it as well.',
        },
      },
      title: 'Controls',
      hintMysql:
        'Start and stop the replica’s threads, skip the one statement that stopped it, reset it, and point it at a source. The SQL is shown before it runs.',
      hintPostgres:
        'Pauses and resumes a standby’s WAL replay. The source is the primary_conninfo setting (change it under Variables).',
      start: 'Start',
      stop: 'Stop',
      resume: 'Resume',
      pause: 'Pause',
      skip: 'Skip one failed statement',
      reset: 'Reset',
      changeSource: 'Set the source…',
      changeSourceHint:
        'Stops the replica, sets the source and, if asked, starts it again. For the position give the file and position from the source’s SHOW BINARY LOG STATUS (SHOW MASTER STATUS on older versions), or use GTID auto-positioning. The password is shown as **** in the preview.',
      sourceHost: 'Source host',
      sourcePort: 'Port',
      sourceUser: 'Replication user',
      logFile: 'Binary log file',
      logPos: 'Position',
      autoPosition: 'Position automatically with GTIDs',
      startAfter: 'Start after setting',
      previewHint: 'The following SQL runs (the password is shown as ****).',
      ops: {
        startReplica: 'Start the replica',
        stopReplica: 'Stop the replica',
        skipReplicaError: 'Skip one failed statement',
        resetReplica: 'Reset the replica',
        changeSource: 'Set the source',
      },
    },
    title: 'Replication',
    role: 'Role of this server',
    roles: {
      standalone: 'Standalone',
      primary: 'Source (sends)',
      replica: 'Replica (receives)',
      relay: 'Relay (receives and sends)',
      unknown: 'Unknown (not readable by this account)',
    },
    source: {
      mysql: 'State as a replica (SHOW REPLICA STATUS)',
      postgres: 'State as a replica (pg_stat_wal_receiver)',
    },
    replicas: 'Connected replicas',
    notReplica: 'This server is not a replica.',
    noReplicas: 'No replica is connected.',
    unavailable: 'This account may not read it (MySQL: REPLICATION CLIENT; PostgreSQL: pg_read_all_stats).',
    logs: { mysql: 'Binary logs', postgres: 'WAL segments' },
    logsUnavailable: {
      mysql: 'Binary logging is off, or this account may not read the logs (REPLICATION CLIENT).',
      postgres: 'This account may not read the WAL directory (it takes pg_monitor).',
    },
    logName: 'Name',
    logSize: 'Size',
  },
  catalog: {
    showVariables: 'Show variables',
    engineVariables: (engine: string) => `Variables of ${engine}`,
    noEngineVariables: (engine: string) => `No variables start with ${engine}_`,
    filter: 'Filter (any column)',
    titles: {
      collations: { mysql: 'Charsets and collations', postgres: 'Collations' },
      engines: { mysql: 'Engines', postgres: 'Access methods' },
      plugins: { mysql: 'Plugins', postgres: 'Extensions' },
    },
    hints: {
      collations: {
        mysql:
          'The character sets and collations the server has. "Default" marks the default collation of its character set.',
        postgres:
          'The collations the server has (provider: libc / ICU / builtin). Those with no encoding work with any encoding.',
      },
      engines: {
        mysql: 'Storage engines and whether this server supports them (DEFAULT is the default engine).',
        postgres: 'PostgreSQL has no storage engines; its table and index access methods are shown instead.',
      },
      plugins: {
        mysql: 'The plugins the server has loaded.',
        postgres:
          'PostgreSQL has no plugins; the extensions available, and the version installed in this database, are shown instead.',
      },
    },
    columns: {
      charset: 'Charset',
      collation: 'Collation',
      isDefault: 'Default',
      provider: 'Provider',
      encoding: 'Encoding',
      name: 'Name',
      support: 'Support',
      transactions: 'Transactions',
      comment: 'Description',
      type: 'Type',
      status: 'Status',
      library: 'Library',
      license: 'License',
      version: 'Default version',
      installedVersion: 'Installed version',
    },
  },
  secondFactor: {
    title: 'Two-factor authentication',
    intro:
      'Adds a one-time code (TOTP) from an authenticator app to the database account this session is signed in as. Once enrolled, connecting asks for a code.',
    introPasskeys:
      'Adds a one-time code (TOTP) from an authenticator app, or a passkey, to the database account this session is signed in as. Once enrolled, connecting asks for one of them.',
    required: 'This deployment requires two-factor authentication. Nothing else is available until you enrol.',
    unsupported:
      'Two-factor authentication is not available in this deployment: the secret needs somewhere to live, so run it with a persistent session store (SESSION_STORE=sqlite or redis).',
    enrol: 'Enrol an authenticator app',
    enrolPasskey: 'Enrol a passkey',
    scan: 'Scan the QR code with your authenticator app, or enter the key (or the URI), then type the six digits it shows.',
    qrLabel: 'QR code for your authenticator app',
    secretLabel: 'Key to enrol',
    recoveryTitle: 'Recovery codes',
    recoveryHint:
      'These let you sign in if you lose the app. They are shown only now — keep them somewhere safe. Each works once.',
    copyRecovery: 'Copy the recovery codes',
    copied: 'Copied (one per line)',
    copyFailed: 'Could not copy. Write down the codes shown instead',
    enrolledNotice: 'Two-factor authentication is on. Connecting will ask for a code from now on',
    disabledNotice: 'Two-factor authentication is off',
    codeLabel: 'Code',
    confirmHint: 'The six digits in your authenticator app',
    confirm: 'Finish enrolling',
    restart: 'Start over',
    enrolled: (left: number) => `Enrolled (${left.toLocaleString('en-US')} recovery codes left)`,
    proofHint: 'Changing or removing it takes the six digits from the app; a recovery code will not do',
    proofHintPasskey:
      'Changing or removing it takes the six digits from the app or one of your passkeys. With the field left empty, a passkey is asked for (a recovery code will not do)',
    proofNeeded: 'Enter the code from your authenticator app',
    passkeyFailed: 'The passkey check did not complete (it was cancelled, or no matching passkey was found)',
    methodsTitle: 'Enrolled methods',
    totpMethod: 'Authenticator app (enrolled)',
    totpMissing: 'Authenticator app (not enrolled)',
    addTotp: 'Add an authenticator app',
    removeTotp: 'Remove the authenticator app',
    passkeyMethod: (n: number, date: string) => `Passkey ${n} (added ${date})`,
    addPasskey: 'Add a passkey',
    removePasskey: 'Remove',
    recoverySaved: 'I have stored the recovery codes',
    totpAddedNotice: 'The authenticator app was added',
    totpRemovedNotice: 'The authenticator app was removed',
    passkeyAddedNotice: 'The passkey was added. It can be used from the next connection on',
    passkeyRemovedNotice: 'The passkey was removed',
    disable: 'Remove',
  },
  server: {
    connection: 'Connected to',
    connectionCollation: 'Connection collation',
    title: 'Server',
    databasesTitle: 'Databases',
    overviewTitle: 'Traffic and connections',
    editVariable: 'Change',
    editVariableHintMysql:
      'Changed with SET GLOBAL. It affects the running server only and reverts to the configuration file at the next restart. A read-only variable cannot be changed (it fails when run).',
    editVariableHintPostgres:
      'Written to postgresql.auto.conf with ALTER SYSTEM SET and the configuration reloaded. A setting that needs a restart takes effect only after one. Only a superuser can change it.',
    resetVariable: 'Put back to the default',
    figure: 'Figure',
    perHour: 'Per hour',
    traffic: {
      bytesReceived: 'Bytes received',
      bytesSent: 'Bytes sent',
      connections: 'Connections',
      abortedClients: 'Connections dropped (client side)',
      abortedConnects: 'Connections that failed to establish',
      questions: 'Queries',
      threadsConnected: 'Connections now',
      threadsRunning: 'Threads running',
      maxUsedConnections: 'Most connections at once',
      transactions: 'Transactions',
      rollbacks: 'Rollbacks',
      activeConnections: 'Connections running something',
      maxConnections: 'Connection limit',
      tempBytes: 'Written to temporary files',
      databaseSize: 'Database size',
    },
    queryStatsTitle: 'Query statistics',
    queryStatsHintMysql: 'How many statements of each kind have run since the server started.',
    queryStatsHintPostgres:
      'The rows this database has handled since start (PostgreSQL keeps no count per kind of statement by default).',
    statement: 'Kind',
    count: 'Count',
    share: 'Share',
    category: 'Category',
    allCategories: 'All',
    warning: 'Warning',
    alertsTitle: 'Warnings from the Advisor',
    alertsMore: 'See the Advisor',
    databaseName: 'Database',
    selectDatabase: (name: string) => `Select ${name}`,
    selectedDatabases: (n: number) => `${n.toLocaleString('en-US')} databases selected`,
    dropSelected: 'Drop the selected databases…',
    open: 'Open',
    infoTitle: 'Server information',
    version: 'Version',
    uptime: 'Uptime',
    countSizes: 'Count sizes and tables',
    manual: 'Manual',
    startedAt: 'Started',
    currentUser: 'Connected as',
    changeOwnPassword: 'Change my password',
    statusTitle: 'Status variables',
    variablesTitle: 'System variables',
    processesTitle: 'Processes',
    filter: 'Filter by name',
    name: 'Name',
    value: 'Value',
    description: 'Description',
    noMatch: 'Nothing matches',
    pid: 'ID',
    user: 'User',
    host: 'Host',
    database: 'DB',
    state: 'State',
    time: 'Time (s)',
    query: 'Query',
    kill: 'Kill',
    killExecute: 'Kill',
    killConfirm: (id: string) => `Kill process ${id}?`,
    killSelfWarning:
      'This is tsmyadmin’s own connection. Killing it may fail an operation in progress (the connection is re-established automatically).',
    selfConnection: 'tsmyadmin',
    selfConnectionHint: "One of tsmyadmin's own connections (including your session)",
    cancelQuery: 'Cancel query',
    cancelQueryHint: 'Stops the running statement only. The connection, its transaction and its temporary tables stay.',
    cancelled: (id: string) => `Cancelled the running query of process ${id}`,
    killed: (id: string) => `Killed process ${id}`,
    refreshEvery: 'Refresh',
    refreshManual: 'Manually',
    refreshSeconds: (s: number) => `Every ${s} s`,
    activeOnly: 'Only connections doing something',
    fullQuery: 'Show the whole statement (100 characters by default)',
    refresh: 'Refresh',
    uptimeFormat: (sec: number) => {
      const d = Math.floor(sec / 86400)
      const h = Math.floor((sec % 86400) / 3600)
      const m = Math.floor((sec % 3600) / 60)
      return `${d > 0 ? `${d} d ` : ''}${h} h ${m} min`
    },
  },
  create: {
    edit: {
      button: 'Edit',
      notEditable:
        'This definition cannot be shown in the form (a parameter without a name, a WHEN clause, …). Use “Edit in the SQL tab”.',
      title: (name: string) => `Edit ${name}`,
      hint: 'MySQL has no syntax to replace one, so the old definition is dropped and the new one created. If the new one is refused, the old one is gone: check the SQL shown, and the original definition, before you run it.',
    },
    security: {
      definer: 'DEFINER',
      definerHint: 'user@host (for example app@%). Blank: the account creating it',
      definerInvalid: 'Write it as user@host',
      sqlSecurity: 'SQL SECURITY',
      defaultOption: '(default)',
      definerOption: 'DEFINER (runs with its creator’s privileges)',
      invokerOption: 'INVOKER (runs with the caller’s privileges)',
      dataAccess: 'Data access',
    },
    review: 'Review SQL',
    name: 'Name',
    comment: 'Comment',
    body: 'Body',
    routine: {
      title: 'Create routine',
      kind: 'Kind',
      params: 'Parameters',
      addParam: 'Add parameter',
      removeParam: (n: number) => `Remove parameter ${n}`,
      mode: 'Direction',
      paramName: (n: number) => `Parameter ${n} name`,
      paramType: (n: number) => `Parameter ${n} type`,
      paramMode: (n: number) => `Parameter ${n} direction`,
      returns: 'Return type',
      language: 'Language',
      deterministic: 'Deterministic (always the same result for the same arguments)',
      bodyHint: {
        mysql: 'One statement, or a BEGIN … END block. Semicolons inside it are fine (no DELIMITER needed).',
        postgres: 'The function body (for plpgsql, a BEGIN … END block).',
      },
    },
    trigger: {
      title: 'Create trigger',
      table: 'Table',
      timing: 'Timing',
      event: 'Event',
      bodyHint: {
        mysql: 'One statement run for each row, or a BEGIN … END block. NEW / OLD refer to the row.',
        postgres:
          'A PL/pgSQL block that returns the row (BEGIN … RETURN NEW; END). A trigger function "name_fn" is created with it.',
      },
    },
    event: {
      preserve: 'Keep it after it has run (ON COMPLETION PRESERVE)',
      preserveHint: 'A one-time event is otherwise dropped once it has run',
      title: 'Create event',
      once: 'Once',
      every: 'Repeating',
      at: 'Run at',
      interval: 'Every',
      unit: 'Unit',
      units: {
        SECOND: 'seconds',
        MINUTE: 'minutes',
        HOUR: 'hours',
        DAY: 'days',
        WEEK: 'weeks',
        MONTH: 'months',
        YEAR: 'years',
      },
      starts: 'Starts (optional)',
      ends: 'Ends (optional)',
      enabled: 'Enabled',
      bodyHint: 'One statement to run, or a BEGIN … END block.',
    },
    view: {
      editTitle: "Change the view's definition",
      unreadable: 'This view’s definition could not be read. Run CREATE OR REPLACE VIEW in the SQL tab.',
      columns: "Column names (comma-separated; blank: the SELECT's own)",
      columnsHint: 'For example id, name',
      checkOption: 'Check option',
      algorithm: 'ALGORITHM',
      title: 'Create view',
      select: 'SELECT statement',
      orReplace: 'Replace a view of the same name (OR REPLACE)',
    },
  },
  dock: {
    toggle: 'Console',
    title: 'SQL console (docked)',
    close: 'Close the console',
  },
  gis: {
    title: 'Show as shapes (GIS)',
    hint: 'Draws the spatial values of the rows on this page, to scale in their own coordinates (there is no map behind them). Hover a shape to see the label column; scroll to zoom and drag to move.',
    column: 'Spatial column',
    label: 'Label',
    noLabel: '(none)',
    saveSvg: 'Save as SVG',
    savePng: 'Save as PNG',
    saveFailed: 'The picture could not be made',
    summary: (shapes: number, unreadable: number) =>
      unreadable > 0
        ? `${plural(shapes, 'shape', 'shapes')} (${plural(unreadable, 'value', 'values')} that could not be read left out)`
        : plural(shapes, 'shape', 'shapes'),
    includeLarge: (n: number) =>
      `Include the ${plural(n, 'shape', 'shapes')} far larger than the rest (leaving them out keeps the others from shrinking to dots)`,
    cut: (n: number) => `${plural(n, 'value', 'values')} too large: only the start reached this page (over 64 KB)`,
    loadCut: (n: number) => `Load them whole (${n})`,
    loading: (done: number, of: number) => `Loading ${done} / ${of}`,
    unloadable: (n: number) => `${plural(n, 'value', 'values')} could not be loaded`,
    zoomIn: 'Zoom in',
    zoomOut: 'Zoom out',
    zoomReset: 'Show all',
    caption: (column: string, shapes: number) => `${plural(shapes, 'shape', 'shapes')} of ${column}`,
  },
  central: {
    edit: (name: string) => `${name}: Edit`,
    editTitle: (name: string) => `Edit ${name}`,
    saveEdit: 'Save',
    download: 'Download as JSON',
    title: 'Central columns',
    hint: 'Column definitions this database uses again and again, kept so Structure → Add column can start from them. Tables are not changed.',
    empty: 'None yet.',
    addTitle: 'Add a definition',
    add: 'Add',
    expression: 'Default is an expression',
    fromTable: 'Take from a table',
    table: 'Table',
    columns: 'Columns',
    importSelected: 'Take the chosen columns',
    remove: (name: string) => `${name}: delete`,
    preset: 'Fill from a central column',
    noPreset: '(none)',
  },
  transform: {
    title: 'Display transformations',
    hint: 'Changes how a column shows in Browse (the value itself is unchanged). Links are http / https only, with the value URL-encoded into them.',
    column: 'Column',
    kind: 'Show as',
    kinds: {
      image: 'Image (binary)',
      link: 'Link',
      json: 'Indented JSON',
      hex: 'Show as hex',
      substring: 'Show a part only',
      boolean: 'Boolean (yes / no)',
      date: 'Date format',
      ipv4: 'IPv4 address (from an integer)',
      affix: 'Text before and after',
      download: 'Download link',
      html: 'Show HTML as formatted text (safe elements only)',
      imagelink: 'Image (from a URL; allowed hosts only)',
      pattern: 'Input: check with a regular expression',
      'json-input': 'Input: JSON editor',
      'xml-input': 'Input: XML editor',
      'sql-input': 'Input: SQL editor',
      'ipv4-to-int': 'Input: store an IPv4 address as an integer',
    },
    groupDisplay: 'Display',
    groupInput: 'Input (insert and edit forms)',
    start: 'Start (from 0)',
    length: 'Length',
    trueText: 'When true',
    falseText: 'When false',
    boolHint: 'Empty means yes / no. Reads 1 / 0 / true / false / t / f / yes / no.',
    format: 'Format',
    formatHint:
      'YYYY YY MM M DD D HH H hh h mm ss A. Text in [ ] is written as it is. An integer is read as Unix time (UTC).',
    prefix: 'Text before',
    suffix: 'Text after',
    pattern: 'Regular expression',
    patternHint: 'The value only has to match somewhere (add ^ and $ to match the whole of it).',
    message: 'Message when it does not match',
    template: 'Link to ({value} stands for the value; empty for the value itself)',
    set: 'Set',
    none: 'None set.',
    clear: (column: string) => `${column}: remove`,
    imageAlt: (column: string) => `Image in ${column}`,
  },
  userGroups: {
    title: 'User groups',
    notice:
      'Members of a group do not see the tabs chosen for it. This tidies the screen; it is not a permission (opening the URL directly still works, within the account’s own privileges). Only an account that can manage every member may change a group.',
    empty: 'No group you can manage.',
    name: 'Group name',
    members: 'Members',
    hidden: 'Hidden tabs',
    hide: { server: 'Hidden tabs (server)', db: 'Hidden tabs (database)', table: 'Hidden tabs (table)' },
    create: 'Create a group',
    edit: (name: string) => `Edit ${name}`,
    editShort: 'Edit',
    remove: (name: string) => `${name}: delete`,
    save: 'Save',
  },
  snapshots: {
    title: 'Snapshots',
    notice:
      "Saves the database as it is now (structure, data, routines) so that after trying a migration or a seed you can put it back. Snapshots are held in tsmyadmin's memory and are gone when it restarts. For a large database, use Export.",
    name: 'Name',
    namePlaceholder: 'e.g. before the migration',
    take: 'Take a snapshot',
    taking: 'Saving…',
    full: (n: number) => `A database keeps at most ${n}. Delete one you no longer need.`,
    list: 'Saved snapshots',
    none: 'None yet.',
    at: 'Taken',
    objects: 'Tables and views',
    size: 'Size',
    restore: 'Restore…',
    delete: 'Delete',
    restoreTitle: (name: string) => `Go back to "${name}"`,
    restoreHint:
      'Puts the database back as it was when saved: drops the tables and views made since, then runs the saved SQL (DROP, CREATE, INSERT). PostgreSQL does it in one transaction, so a failure leaves it as it was; MySQL keeps what had already run. Functions, triggers and events made since the snapshot stay.',
    lossWarning: 'Every change to the data since the snapshot is lost.',
    replayNote: (n: number) => `Then runs the ${n.toLocaleString('en-US')} saved statements`,
    restored: (name: string) => `Went back to "${name}".`,
    failed: 'The restore failed.',
  },
  tracking: {
    download: 'Download SQL',
    downloadVersion: (v: number) => `Version ${v}: download SQL`,
    deleteVersion: (v: number) => `Version ${v}: delete`,
    deleteVersionTitle: 'Delete a version',
    deleteVersionConfirm: 'Delete',
    deleteVersionBody: (v: number, count: number) =>
      count === 1
        ? `Version ${v} is the last one. Deleting it stops tracking (the settings and the recorded statements go too).`
        : `Deletes the record of version ${v}. The other versions stay.`,
    title: 'Change tracking',
    notice:
      'Keeps the table’s definition (its CREATE statement) as versions recorded at a point in time, to compare. Record with “Record the current structure” (nothing is recorded on its own); a change made from anywhere shows up as a difference from the latest version. Shared by every account of the server.',
    notTracked: 'This table is not tracked.',
    start: 'Start tracking (record the current structure as version 1)',
    record: 'Record the current structure',
    stop: 'Stop tracking…',
    stopConfirm: 'Stop tracking',
    stopBody: (n: number) =>
      `Deletes all ${plural(n, 'recorded version', 'recorded versions')}. This cannot be undone. The table itself is not changed.`,
    unchanged: (v: number) => `Unchanged since version ${v}`,
    changed: (v: number) => `Changed since version ${v}`,
    versions: 'Versions',
    version: 'Version',
    recordedAt: 'Recorded',
    recordedBy: 'Recorded by',
    compare: 'Compare',
    from: 'From',
    to: 'To',
    now: 'Current structure',
    versionLabel: (v: number) => `Version ${v}`,
    diff: 'Differences (- only in From, + only in To)',
    noDifference: 'No differences.',
    statements: 'Recorded statements',
    kinds: 'Statements to record',
    kindsHint:
      'Of the statements run through this tool, the kinds chosen are recorded (changes from other clients show only as differences between versions). Row-change statements hold values, so they are shown only to those who can read the table. Edits in the grid are recorded without their values.',
    kindNames: {
      create: 'CREATE TABLE',
      alter: 'ALTER TABLE',
      rename: 'RENAME',
      drop: 'DROP TABLE',
      truncate: 'TRUNCATE',
      index: 'CREATE / DROP INDEX',
      insert: 'INSERT',
      update: 'UPDATE',
      delete: 'DELETE',
    },
    saveKinds: 'Save the kinds',
    noStatements: 'No statement recorded yet.',
    logHidden:
      "The recorded statements are not shown: you cannot read this table's rows (a statement may hold row values).",
    grid: (rows: number, columns: string[]) =>
      `Edited in the grid: ${rows.toLocaleString('en-US')} row${rows === 1 ? '' : 's'}${columns.length > 0 ? ` (${columns.join(', ')})` : ''}; values not recorded`,
    cut: '(cut short)',
    kind: 'Kind',
    statement: 'Statement',
    tables: 'Tracked tables',
    noTables: "No table of this database is tracked. Start from a table's Tracking tab.",
    latest: 'Latest version',
  },
  normalize: {
    title: 'Normalization hints',
    hint: 'Points out what in the table definition and its first rows looks like a break of the first to third normal forms. Listing them changes nothing.',
    sampled: (n: number) =>
      `Read from the first ${plural(n, 'row', 'rows')}. A dependency read from values may only happen to hold in them.`,
    smallSample: (n: number, min: number) =>
      `Only ${plural(n, 'row', 'rows')}, so dependencies between values were not looked for (that takes ${min} or more).`,
    none: 'Nothing stands out.',
    levels: { '1': '1st normal form', '2': '2nd normal form', '3': '3rd normal form', fk: 'Reference' },
    noPrimaryKey:
      'There is no primary key. Make a column that identifies each row (or an auto-increment one) the primary key.',
    repeatingGroup: (columns: string) =>
      `${columns} hold the same kind of value in numbered copies. A separate table with one row per value removes the limit on how many there can be.`,
    listValues: (column: string, count: number) =>
      `${column} holds several values joined by a separator (${plural(count, 'row', 'rows')}). A separate table with one value per row is easier to search and join.`,
    missingForeignKey: (column: string, table: string) =>
      `${column} looks like it points at ${table}, but there is no foreign key. With one, it cannot refer to a row that does not exist.`,
    partialDependency: (key: string, column: string) =>
      `${column} seems to be decided by ${key}, only part of the primary key. Moving it to a table keyed by ${key} removes the repeated values.`,
    actions: {
      title: 'Move into a new table',
      make: 'Move into a new table…',
      splitLabel: (key: string, columns: string) => `${columns} into a table keyed by ${key}`,
      groupLabel: (columns: string) => `${columns} into a table with one row each`,
      formLabel: (table: string) => `Split ${table}`,
      splitHint: (key: string) =>
        `Makes a new table with one row per value of ${key} (its primary key), copies the chosen columns' values into it, and points the original at it by ${key}. If some rows really do have different values for the same ${key}, adding the primary key fails (the original is left as it was; drop the half-made new table).`,
      groupHint:
        'Turns the chosen columns into rows of a new table holding the original primary key and one value column (NULLs make no row). A foreign key back to the original is added.',
      newTable: 'New table name',
      valueColumn: 'Value column name',
      columns: 'Columns to move',
      dropMoved: 'After moving, drop these columns from the original table',
    },
    transitiveDependency: (from: string, column: string) =>
      `${column} seems to be decided by ${from}, which is not the primary key. Moving it to a table keyed by ${from} stops copies from drifting apart.`,
  },
  zoom: {
    title: 'Zoom search',
    hint: 'Pick two numeric columns to plot the rows matching the search. Pick a point to open its row.',
    x: 'Horizontal axis',
    y: 'Vertical axis',
    plot: 'Show scatter plot',
    summary: (points: number, skipped: number) =>
      skipped > 0
        ? `${points.toLocaleString('en-US')} points (${skipped.toLocaleString('en-US')} rows left out: NULL or not a number)`
        : `${points.toLocaleString('en-US')} points`,
    clipped: (max: number) => `Only the first ${max.toLocaleString('en-US')} rows are drawn. Narrow the search.`,
    caption: (x: string, y: string) => `Scatter plot of ${x} and ${y}`,
    asTable: (n: number) => `Show the points as a table (${n.toLocaleString('en-US')})`,
    pick: 'Pick',
    pickPoint: (x: string, xv: number, y: string, yv: number) =>
      `Pick the row with ${x} = ${xv.toLocaleString('en-US')}, ${y} = ${yv.toLocaleString('en-US')}`,
    picked: 'Picked row',
    open: 'Open this row in Browse',
    min: (axis: string) => `${axis} from`,
    max: (axis: string) => `${axis} to`,
    label: 'Label column',
    noLabel: '(none)',
    limit: 'Rows to plot, at most',
    noKey: 'This table has no primary key, so the row cannot be opened.',
  },
  operations: {
    convertHintMysql:
      "Converts the table's default and every text column to the collation (and its character set); values are converted to the new character set.",
    convertHintPostgres: 'Gives every text column the collation (types unchanged).',
    orderHintMysql:
      'Stores the rows again in the order of a column (InnoDB keeps primary key order, so this matters for tables without a primary key).',
    orderHintPostgres:
      'Stores the rows again in the order of an index (CLUSTER; exclusive lock; later changes do not keep the order).',
    orderColumn: 'Order by column',
    orderIndex: 'Index to order along',
    descending: 'Descending',
  },
  bulk: {
    showCreate: 'Show CREATE statements',
    maintain: 'Maintenance…',
    copy: 'Copy elsewhere…',
    copyHint: 'Copies the tables under their own names into another database (MySQL) / schema (PostgreSQL).',
    addPrefix: 'Add a prefix…',
    replacePrefix: 'Replace a prefix…',
    prefix: 'Prefix to add',
    prefixFrom: 'Prefix to replace',
    prefixTo: 'New prefix',
    prefixToHint: 'Empty removes the prefix. Tables not starting with it are left alone',
    createFailed: 'could not read the CREATE statement',
  },
  dictionary: {
    title: (name: string) => `Data dictionary: ${name}`,
    link: 'Data dictionary (every table, for printing)',
    hint: "Every table's and view's columns, keys and comments on one page.",
    print: 'Print',
    type: 'Type',
    nullable: 'Null',
    default: 'Default',
    references: 'References',
    primary: 'primary key',
    indexes: 'Indexes',
    unique: 'unique',
  },
  partitions: {
    title: 'Partitions',
    createBy: 'Partitioning',
    notPartitioned: '(none)',
    none: 'This table is not partitioned.',
    nonePostgres:
      'This table is not partitioned. PostgreSQL cannot partition an existing table: choose partitioning when you create the table.',
    partition: 'Partition the table…',
    by: (method: string, expression: string) => `Partitioned by ${method} (${expression})`,
    method: 'Method',
    expression: 'Partition key (an expression or columns)',
    expressionHint: 'For example id or YEAR(created_at); KEY lists column names',
    partitions: 'Partitions (ranges / value lists)',
    nameOf: (n: number) => `Name of partition ${n}`,
    boundOf: (n: number) => `Range of partition ${n}`,
    addRow: 'Add a row',
    removeRow: 'Remove the last row',
    count: 'Number of partitions',
    name: 'Name',
    bound: 'Range / values',
    boundHint: 'What follows the name, written as the server spells it (see the example)',
    maintain: 'Maintenance…',
    maintainOf: (name: string) => `Partition ${name}: maintenance`,
    truncate: 'Empty',
    truncateOf: (name: string) => `Partition ${name}: empty`,
    detach: 'Detach',
    detachOf: (name: string) => `Partition ${name}: detach`,
    dropOf: (name: string) => `Partition ${name}: drop`,
  },
  replace: {
    title: 'Find and replace',
    hint: 'Replaces a text in one column, in every row that holds it (case-sensitive). The SQL is shown before it runs.',
    column: 'Column',
    find: 'Find',
    with: 'Replace with',
    regex: 'As a regular expression',
    regexHint:
      "The syntax and back-references ($1 on MySQL 8, \\1 on MariaDB and PostgreSQL) follow the server. On MySQL / MariaDB the column's collation may make it case-insensitive.",
  },
  routines: {
    run: 'Run',
    runHint: 'Fill in values and a CALL / SELECT is prepared in the SQL tab. Nothing runs until you review it there.',
    privileges: 'Privileges',
    privilegesHint: 'Grants or revokes privileges on this routine for an account (the SQL is shown before it runs).',
    account: 'Account',
    privilegeList: 'Privileges',
    grant: 'Grant',
    revoke: 'Revoke',
    noParameters: 'A call without arguments is prepared.',
    openInSql: 'Open in SQL tab',
    characteristics: 'Change characteristics',
    characteristicsHint:
      'Changes the security, data access and comment without touching the body. To change the body use “Edit in SQL tab”.',
    drop: 'Delete',
    download: 'Download SQL',
    title: 'Stored procedures / functions',
    name: 'Name',
    kind: 'Type',
    kinds: { procedure: 'Procedure', function: 'Function', package: 'Package', 'package body': 'Package body' },
    language: 'Language',
    returns: 'Returns',
    parameters: 'Parameters',
    definition: 'Definition',
    comment: 'Comment',
    none: 'No routines',
    show: 'Show definition',
    hide: 'Hide definition',
    edit: 'Edit in SQL tab',
    editHint:
      'Loads a script that replaces the definition into the SQL tab. A body is code in the server’s own dialect, so there is no form for it — review the script before running it.',
    noDefinition: 'You do not have permission to view this definition',
  },
  events: {
    title: 'Event scheduler',
    name: 'Name',
    status: 'Status',
    schedule: 'Schedule',
    starts: 'Starts',
    ends: 'Ends',
    lastExecuted: 'Last run',
    comment: 'Comment',
    definition: 'Definition',
    none: 'No events',
    unsupported: 'PostgreSQL has no built-in event scheduler (use an extension such as pg_cron).',
    enable: 'Enable',
    disable: 'Disable',
    drop: 'Drop',
    schedulerHint: 'Events do not run while the server’s event_scheduler variable is OFF (see the Variables tab).',
  },
  triggers: {
    drop: 'Delete',
    title: 'Triggers',
    name: 'Name',
    table: 'Table',
    timing: 'Timing',
    events: 'Events',
    orientation: 'For each',
    definition: 'Definition',
    none: 'No triggers',
  },
  /** Prefixes: a server-side detail (DB message, validation issue) may follow after ": ", so no trailing period. */
  /** Server-named reasons of a VALIDATION error, rendered with their parameters. */
  reasons: {
    INVALID_ENCODING: (p: Record<string, string | number>) =>
      `The file holds bytes that are not valid UTF-8 (around line ${p.line ?? '?'}). Run mysqldump with --hex-blob and save text as UTF-8`,
    WRONG_DIALECT: (p: Record<string, string | number>) =>
      `This file is a ${p.dialect === 'postgres' ? 'PostgreSQL' : 'MySQL'} dump, which does not match the server you are connected to`,
    NO_STATEMENTS: () => 'No statements were found in the file',
    CSV_NO_TABLE: () => 'Choose the table to import the CSV into',
    CSV_EMPTY: () => 'The CSV file is empty',
    CSV_UNKNOWN_COLUMNS: (p: Record<string, string | number>) =>
      `The header names columns the table does not have: ${p.columns ?? ''}`,
    CSV_AMBIGUOUS_COLUMNS: (p: Record<string, string | number>) =>
      `A header name matches several columns differing only in case (spell it as the table does): ${p.columns ?? ''}`,
    CSV_DUPLICATE_COLUMNS: (p: Record<string, string | number>) =>
      `The header names the same column twice: ${p.columns ?? ''}`,
    CSV_NO_COLUMNS: () => 'No columns to import',
    CSV_FIELD_COUNT: (p: Record<string, string | number>) =>
      `Line ${p.line ?? '?'} has more fields (${p.fields ?? '?'}) than there are columns (${p.columns ?? '?'})`,
    CSV_BINARY: (p: Record<string, string | number>) =>
      `Line ${p.line ?? '?'}: column ${p.column ?? ''} is binary, so its value must be base64`,
    CSV_UNTERMINATED_QUOTE: (p: Record<string, string | number>) =>
      `The quote opened on line ${p.line ?? '?'} is never closed (everything after it would be one field, so the import stopped)`,
    CSV_ROW_FAILED: (p: Record<string, string | number>) =>
      `Line ${p.line ?? '?'} could not be imported (the whole file was rolled back): ${p.message ?? ''}`,
    OPTION_FAILED: (p: Record<string, string | number>) =>
      `The option "${p.option === 'ignoreForeignKeys' ? 'Disable foreign key checks' : 'Run in a single transaction'}" could not be applied (on PostgreSQL, disabling foreign key checks needs superuser rights): ${p.message ?? ''}`,
    UNTERMINATED_END: () =>
      'The file ends inside a comment or a string literal, so nothing could be committed in a single transaction. Close it, or turn that option off',
    IDENTIFIER_TOO_LONG: (p: Record<string, string | number>) =>
      `The name "${p.name ?? ''}" is too long (at most ${p.max ?? '?'} ${p.max === 63 ? 'bytes' : 'characters'})`,
    CSV_ROWS_FAILED: (p: Record<string, string | number>) =>
      `One of the lines ${p.from ?? '?'}–${p.to ?? '?'} could not be imported (the whole file was rolled back): ${p.message ?? ''}`,
    ARCHIVE_INVALID: (p: Record<string, string | number>) =>
      `The compressed file could not be opened: ${p.message ?? ''}`,
    ARCHIVE_TOO_LARGE: (p: Record<string, string | number>) => `The file unpacks to more than ${p.mb ?? '?'} MB`,
    ARCHIVE_MULTIPLE: () => 'The zip holds several files (only SQL files can be run together)',
    ROWS_NO_SHEET: (p: Record<string, string | number>) =>
      p.sheet ? `The file has no sheet or table named "${p.sheet}"` : 'The file holds no table to import',
    ROWS_PARSE: (p: Record<string, string | number>) => `The file could not be read: ${p.message ?? ''}`,
    TABLE_EXISTS: (p: Record<string, string | number>) =>
      `The table ${p.table ?? ''} exists already (give a new table another name)`,
    CREATE_INVALID_NAME: (p: Record<string, string | number>) =>
      `The table ${p.name ?? ''} could not be created (the table name or a column name in the file may not be usable): ${p.message ?? ''}`,
  } satisfies Record<ImportReason, (p: Record<string, string | number>) => string>,
  errors: {
    UNAUTHENTICATED: 'Your session has ended. Connect again',
    VALIDATION: 'Some of the input is not valid',
    CONNECTION_FAILED: 'The database server cannot be reached',
    AUTH_FAILED: 'Authentication failed',
    NOT_FOUND: 'The database, table or object was not found',
    QUERY_FAILED: 'The query failed',
    CONFLICT: 'Another change landed at the same time, so this one was not applied. Try again',
    KEY_MISMATCH:
      'The row could not be identified uniquely, so the change was rolled back (it may have been changed by someone else)',
    UNSUPPORTED: 'This operation is not supported',
    FORBIDDEN: 'This operation is not allowed',
    HOST_NOT_ALLOWED: 'Your administrator has not allowed connections to this server',
    INSECURE_TRANSPORT: 'Connect over HTTPS (a session cannot be kept over plain HTTP)',
    PERMISSION_DENIED: 'The database user lacks the privilege this operation needs',
    SECOND_FACTOR_REQUIRED: 'Enter your one-time code',
    SECOND_FACTOR_INVALID: 'That code is not valid (it may already have been used, or have expired)',
    RATE_LIMITED: 'Too many attempts. Wait a while and try again',
    PAYLOAD_TOO_LARGE: 'The request is too large',
    INTERNAL: 'Something went wrong on the server',
    NETWORK: 'The server cannot be reached. Check your network connection',
  },
} as const satisfies Locale
